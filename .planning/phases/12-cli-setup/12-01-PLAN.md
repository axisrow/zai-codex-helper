---
phase: 12-cli-setup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/zai_codex_helper/services/setup.py
  - src/zai_codex_helper/cli/parser.py
  - tests/test_setup.py
autonomous: true
requirements: [SETUP-01, SETUP-02, SETUP-03, SECR-01, SECR-03]

must_haves:
  truths:
    # SC-1 (SETUP-01, SECR-01) — full interactive onboarding flow
    - "Running `zai-codex-helper setup` interactively walks the user through every onboarding step in order: provider choice (default zai) → API key (ZAI_API_KEY env, else getpass never echoed) → write moonbridge-zai.yml at 0600 → build Moon Bridge → shell helpers opt-in → apply the chosen provider → LaunchAgent offer → summary + restart warning."
    - "The API key is NEVER printed, logged, or echoed: it flows ZAI_API_KEY env → getpass.getpass() → YamlBackend.write_canonical() at 0600, and captured stdout+stderr across a full setup run contain no occurrence of the key literal."
    # SC-2 (SETUP-02) — scriptable via shared confirm()
    - "The same flow runs headless with --yes (and the equivalent --no-input): every prompt goes through the single shared services/io.py confirm() helper, which returns True without prompting, so setup is fully scriptable; in this mode ZAI_API_KEY env is REQUIRED (raise ZaiCodexHelperError if absent)."
    # SC-3 (SETUP-03) — idempotent double-setup
    - "Running setup twice over an existing install yields byte-identical written files (config.toml, moonbridge-zai.yml, .zshrc) — idempotent canonical overwrite by composition, never append."
    # D-78 boundary — LaunchAgent is OFFER only
    - "On LaunchAgent consent, setup PRINTS the install-service command and does NOT call launchctl or write the plist (Phase 13 owns that); a spy on subprocess.run asserts no launchctl invocation across the whole flow."
  artifacts:
    - "src/zai_codex_helper/services/setup.py — the onboarding orchestrator (pure-ish; takes injected input_fn/build_fn/confirm_fn for testability)."
    - "src/zai_codex_helper/cli/parser.py — _handle_setup real handler replaces the Phase 1 _stub('setup'); --no-input flag added alongside --yes."
    - "tests/test_setup.py — @pytest.mark.integration tests pinning SC-1/SC-2/SC-3 + the SECR-03 no-leak spy + the D-78 no-launchctl spy + the --no-input-env-required error."
  key_links:
    - "_handle_setup → run_setup (services/setup.py) — the handler is a thin shell resolving Paths.default() and forwarding args; all step logic lives in the orchestrator."
    - "run_setup → confirm (services/io.py) — EVERY prompt routes through the shared helper so --yes/--no-input reuse one path (D-79)."
    - "run_setup → YamlBackend.write_canonical (backends/yaml.py) — the API key lands at 0600 via the existing secrets backend (D-77)."
    - "run_setup → build_moonbridge (services/moonbridge.py) — the Phase 11 idempotent build primitive (D-76 step 3)."
    - "run_setup → apply_zai/apply_openai pipeline (services/providers.py + cli/parser._apply_provider_pipeline) — the chosen provider is applied so config.toml reflects the default after setup."
---

<objective>
Deliver the `zai-codex-helper setup` onboarding orchestrator — the Phase 12 capstone that composes every prior phase (Paths, atomic_write, backup, TomlBackend, provider transforms, YamlBackend/ShellBackend, dependency detection, Moon Bridge build) into a single interactive, scriptable, idempotent end-to-end flow.

Purpose: A new user runs one command (`zai-codex-helper setup`) and is guided through choosing a default provider, supplying an API key safely, opting into shell helpers, building Moon Bridge, and (optionally) the LaunchAgent — fully scriptable for automation, and safe to re-run. This is the integration payoff for phases 2-11; it adds NO new domain logic, only orchestration of already-proven primitives. Honors D-76..D-82 verbatim.

Output:
- `src/zai_codex_helper/services/setup.py` — the orchestrator (the run_setup entry point + the ordered onboarding steps, with injected seams for input/build/confirm so tests run zero real IO).
- `src/zai_codex_helper/cli/parser.py` — `_handle_setup` real handler replacing the Phase 1 stub, plus a `--no-input` root flag mirroring `--yes`.
- `tests/test_setup.py` — integration tests proving all 3 ROADMAP SCs + the SECR-03 no-leak spy + the D-78 no-launchctl spy + the D-79 --no-input-env-required error.
</objective>

<execution_context>
@/Users/axisrow/Projects/zai-codex-helper/.claude/gsd-core/workflows/execute-plan.md
@/Users/axisrow/Projects/zai-codex-helper/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@/Users/axisrow/Projects/zai-codex-helper/.planning/PROJECT.md
@/Users/axisrow/Projects/zai-codex-helper/.planning/ROADMAP.md
@/Users/axisrow/Projects/zai-codex-helper/.planning/STATE.md
@/Users/axisrow/Projects/zai-codex-helper/.planning/phases/12-cli-setup/12-CONTEXT.md
@/Users/axisrow/Projects/zai-codex-helper/.planning/REQUIREMENTS.md
@/Users/axisrow/Projects/zai-codex-helper/CLAUDE.md

# Existing code this plan composes (read all before implementing)
@/Users/axisrow/Projects/zai-codex-helper/src/zai_codex_helper/cli/parser.py
@/Users/axisrow/Projects/zai-codex-helper/src/zai_codex_helper/services/io.py
@/Users/axisrow/Projects/zai-codex-helper/src/zai_codex_helper/services/moonbridge.py
@/Users/axisrow/Projects/zai-codex-helper/src/zai_codex_helper/services/deps.py
@/Users/axisrow/Projects/zai-codex-helper/src/zai_codex_helper/services/providers.py
@/Users/axisrow/Projects/zai-codex-helper/src/zai_codex_helper/services/paths.py
@/Users/axisrow/Projects/zai-codex-helper/src/zai_codex_helper/backends/yaml.py
@/Users/axisrow/Projects/zai-codex-helper/src/zai_codex_helper/backends/shell.py
@/Users/axisrow/Projects/zai-codex-helper/src/zai_codex_helper/errors.py
@/Users/axisrow/Projects/zai-codex-helper/src/zai_codex_helper/__main__.py

# Prior phase summaries whose primitives this plan composes
@/Users/axisrow/Projects/zai-codex-helper/.planning/phases/11-moon-bridge-install/11-01-SUMMARY.md
@/Users/axisrow/Projects/zai-codex-helper/.planning/phases/10-dependency-detection/10-01-SUMMARY.md
@/Users/axisrow/Projects/zai-codex-helper/.planning/phases/09-remaining-file-backends/09-01-SUMMARY.md
@/Users/axisrow/Projects/zai-codex-helper/.planning/phases/07-use-zai-use-openai/07-01-SUMMARY.md

# Existing test patterns to mirror (read before writing tests/test_setup.py)
@/Users/axisrow/Projects/zai-codex-helper/tests/test_use_zai_use_openai.py
@/Users/axisrow/Projects/zai-codex-helper/tests/conftest.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create services/setup.py onboarding orchestrator (D-76..D-82)</name>
  <files>src/zai_codex_helper/services/setup.py</files>
  <read_first>
    - .planning/phases/12-cli-setup/12-CONTEXT.md (D-76 the full step order; D-77 SECR-01/03 key handling; D-78 LaunchAgent OFFER only; D-79 --yes/--no-input; D-80 idempotence by composition; D-81 orchestrator location + injected fns; D-82 scope discipline)
    - src/zai_codex_helper/services/io.py (the shared confirm() helper signature — `confirm(prompt, *, input_fn=input) -> bool`; EOFError maps to False)
    - src/zai_codex_helper/services/moonbridge.py (build_moonbridge(paths, *, force=False, runner=subprocess.run) -> Path; idempotent skip when binary exists + executable)
    - src/zai_codex_helper/services/providers.py (apply_zai / apply_openai pure transforms; check_postconditions)
    - src/zai_codex_helper/backends/yaml.py (YamlBackend.write_canonical(content, mode=0o600) — default 0600 is LOAD-BEARING; reads via yaml.safe_load)
    - src/zai_codex_helper/backends/shell.py (ShellBackend.write_canonical(content, mode=0o644) — marker-fenced idempotent block)
    - src/zai_codex_helper/services/paths.py (Paths fields: codex_dir, config_toml, moonbridge_yml, zshrc)
    - src/zai_codex_helper/errors.py (ZaiCodexHelperError — raise, do NOT catch; main() owns D-11 formatting)
    - tests/test_yaml_backend.py lines around the canonical shape (the YAML body fixture: a dict with keys ZAI_API_KEY, model, server={host,port} — mirror this exact shape so the written file matches what doctor/Phase 14 will expect)
    - CLAUDE.md §"Interactive Prompts" (getpass.getpass() for secrets; input() for the provider choice) and §"File Permissions" (moonbridge-zai.yml 0600)
  </read_first>
  <behavior>
    - Provider choice: with --yes/--no-input the choice is "zai" (default) without prompting; interactive mode accepts the user's typed choice validated against {"zai","openai"} with "zai" as the default on empty/EOF.
    - API key precedence: ZAI_API_KEY env wins; else (interactive) getpass.getpass() supplies it (never echoed); else (--yes/--no-input with no env) raise ZaiCodexHelperError naming the env var.
    - moonbridge-zai.yml is written at 0600 with the canonical body (ZAI_API_KEY + model "glm-5.2" + server {host 127.0.0.1, port 38440}).
    - build_moonbridge is called exactly once (idempotent skip if the binary already exists); a missing-Go error from build_moonbridge propagates as ZaiCodexHelperError (setup does NOT auto-install).
    - Shell helpers opt-in is a confirm() prompt; on yes the ShellBackend marker block is written idempotently.
    - The chosen provider is applied (apply_zai for "zai", apply_openai for "openai") via the SAME write path the use-handlers use, so config.toml reflects the default after setup.
    - LaunchAgent step is OFFER only: confirm() → on yes print "run: zai-codex-helper install-service" (Phase 13); setup NEVER calls launchctl and NEVER writes the plist.
    - --dry-run previews the planned steps without writing (prints the step list; skips every mutating call).
    - Idempotence: a second run with the same inputs produces byte-identical written files (backup sentinel already set, YamlBackend/ShellBackend upsert replace-not-append, build_moonbridge skip, provider pipeline idempotent).
  </behavior>
  <action>
    Create `src/zai_codex_helper/services/setup.py` exposing `run_setup(paths, *, yes=False, dry_run=False, input_fn=input, getpass_fn=getpass.getpass, confirm_fn=confirm, build_fn=build_moonbridge, environ=os.environ, print_fn=print) -> int` — the pure-ish orchestrator (D-81: takes injected input/build/confirm fns so tests run zero real IO; getpass comes from stdlib `getpass`).

    Step order (D-76 — each prompt goes through `confirm_fn`/`input_fn`/`getpass_fn`; honor `dry_run` by printing the planned step via `print_fn` and skipping the mutating call):

    1. PROVIDER CHOICE. If `yes` is True → provider = "zai" (D-79 default). Else prompt via `input_fn("Default provider [zai/openai] (default zai): ")`; strip+lowercase; empty string OR EOFError (caught) → "zai"; any value not in {"zai","openai"} → re-prompt once or default to "zai" (keep it simple: default on invalid).
    2. API KEY (SECR-01, D-77). Read `environ.get("ZAI_API_KEY")`. If present → use it (NEVER print/log it). Else if `yes` is True → raise `ZaiCodexHelperError("ZAI_API_KEY env not set; pass it or run setup interactively")` (D-79: env REQUIRED in headless mode, no stdin available). Else (interactive) → call `getpass_fn("ZAI API key: ")`; an empty result raises `ZaiCodexHelperError("API key is required")`.
    3. WRITE moonbridge-zai.yml at 0600 (D-77, SECR-02). Build the canonical body dict (mirror the Phase 9 fixture shape exactly): keys `ZAI_API_KEY` (the resolved key), `model` ("glm-5.2"), `server` ({"host": "127.0.0.1", "port": 38440}). Instantiate `YamlBackend(paths)` and call `write_canonical(body)` — the backend's default mode=0o600 is LOAD-BEARING; do NOT pass an override. Under `dry_run`, print "would write moonbridge-zai.yml" and skip. The key MUST NOT appear in any `print_fn` call.
    4. BUILD MOON BRIDGE (D-76 step 3, D-69). Call `build_fn(paths)` — the injected `build_moonbridge` (Phase 11) is idempotent (skips when the binary exists + executable), so calling it every run is safe. A `ZaiCodexHelperError` from build_fn (Go missing / clone failed) propagates — setup does NOT catch it and does NOT auto-install Go/brew (D-82). Under `dry_run`, print "would build Moon Bridge (skipped)" and skip.
    5. SHELL HELPERS OPT-IN (D-76 step 4, ShellBackend). `confirm_fn("Add shell helpers to .zshrc?")` — if `yes` is True, treat as consented without prompting (pass an input_fn that returns "" so confirm() returns False normally — instead, when `yes` is True, bypass confirm_fn and treat as True per D-79). On consent, write a minimal marker-fenced block via `ShellBackend(paths).write_canonical(SHELL_HELPERS_BODY)` where `SHELL_HELPERS_BODY` is a module-level constant: a short comment + an alias line pointing at the `zai-codex-helper` console script (e.g. an alias `codex-zai` for `zai-codex-helper use zai` and `codex-openai` for `use openai` — keep minimal; the binary itself is launched by the LaunchAgent in Phase 13, NOT by .zshrc). Under `dry_run`, print "would write shell helpers" and skip.
    6. APPLY THE CHOSEN PROVIDER (D-76 step 5). Reuse the EXACT Phase 7 write pipeline so setup and `use` share one path: import `apply_zai`/`apply_openai` from `services.providers` and the existing `_apply_provider_pipeline` helper from `cli.parser` (it is module-level and reusable — paths is already resolved here, but `_apply_provider_pipeline` re-resolves `Paths.default()`; that is fine because the autouse test fixture repoints HOME at tmp_path, so the production call and the test sandbox both resolve consistently). If reusing `_apply_provider_pipeline` would create an awkward cli↔services circular dependency, instead inline the SAME pipeline steps (seed-if-missing → backup_once → read → apply_zai/apply_openai → write_canonical → check_postconditions) using `TomlBackend(paths)` so the orchestrator stays in the services layer (PREFERRED — avoids cli importing services importing cli). Pick the inline approach. Under `dry_run`, print "would apply provider <choice>" and skip. Note: `_apply_provider_pipeline` emits its own restart warning to stderr — when inlining, call `_emit_restart_warning` from `cli.parser` OR inline a short summary; to keep the layering clean, the orchestrator should print a final SUMMARY (step 8) and let the handler emit the restart warning, so DO NOT call `_emit_restart_warning` inside the orchestrator. The orchestrator returns; the handler decides warnings.
    7. LAUNCHAGENT OFFER (D-78 — OFFER ONLY). `confirm_fn("Install the LaunchAgent for auto-start?")` — if `yes` is True, treat as consented (D-79). On consent → `print_fn("Run: zai-codex-helper install-service")` (Phase 13 owns launchctl). On decline → skip silently. CRITICAL: setup does NOT call `subprocess.run` with launchctl anywhere, and does NOT write a plist (D-78, D-82). Under `dry_run`, print "would offer LaunchAgent" and skip.
    8. SUMMARY. `print_fn` a short summary: the chosen provider, that moonbridge-zai.yml was written, that Moon Bridge was built (or skipped), whether shell helpers were added, and the install-service next step if the LaunchAgent was consented. The API key MUST NOT appear anywhere in the summary (SECR-03).

    Return 0 on success. Do NOT call `sys.exit`. Do NOT catch `ZaiCodexHelperError` (D-11 — `main()` owns formatting).

    Module docstring MUST cite D-76..D-82 and explicitly state the scope boundaries (no launchctl/plist → Phase 13; no doctor → Phase 14; no models_cache entry → Phase 15; no auto-install Go/brew; never echo/log the API key — SECR-03).

    Export `run_setup` and `SHELL_HELPERS_BODY` in `__all__`.
  </action>
  <verify>
    <automated>cd /Users/axisrow/Projects/zai-codex-helper && python -c "from zai_codex_helper.services.setup import run_setup, SHELL_HELPERS_BODY; print('import ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `services/setup.py` exists and exports `run_setup` + `SHELL_HELPERS_BODY`.
    - The orchestrator signature takes injected `input_fn`/`getpass_fn`/`confirm_fn`/`build_fn`/`environ`/`print_fn` (D-81 testability).
    - The API-key resolution order is ZAI_API_KEY env → getpass (interactive) → raise on --yes/--no-input without env (D-77, D-79).
    - moonbridge-zai.yml is written via `YamlBackend.write_canonical(body)` with NO explicit mode override (default 0o600 is load-bearing).
    - The chosen provider is applied through the same seed→backup→read→transform→write→check pipeline as Phase 7 (inline in services to avoid a cli↔services cycle).
    - LaunchAgent consent prints "install-service" and invokes NO subprocess / writes NO plist (D-78).
    - No `print_fn` call includes the resolved API key.
    - `ZaiCodexHelperError` is raised (not caught) on missing env in headless mode and propagates from build_fn.
  </acceptance_criteria>
  <done>
    `services/setup.py` imports cleanly; the orchestrator composes YamlBackend + build_moonbridge + ShellBackend + the provider pipeline behind injected seams; the step order matches D-76; SECR-03 no-leak and D-78 no-launchctl are structurally enforced (the orchestrator never references the API key in a print call and never references launchctl/plist).
  </done>
</task>

<task type="auto">
  <name>Task 2: Wire _handle_setup in cli/parser.py + add --no-input flag (D-81)</name>
  <files>src/zai_codex_helper/cli/parser.py</files>
  <read_first>
    - src/zai_codex_helper/cli/parser.py (the existing _handle_use_zai / _handle_restore handler shape: lazy imports inside the body, resolve Paths.default(), delegate, return 0, do NOT catch ZaiCodexHelperError, do NOT sys.exit; the build_parser() root flags --debug/--yes/--dry-run; the `for name in ("setup", "doctor", "install-service", "uninstall-service")` stub loop that this task replaces for "setup" only)
    - src/zai_codex_helper/services/setup.py (the run_setup signature from Task 1 — the handler forwards args.yes/args.no_input/args.dry_run)
    - src/zai_codex_helper/__main__.py (the D-11 error contract — main() catches ZaiCodexHelperError; the handler must let it propagate)
    - .planning/phases/12-cli-setup/12-CONTEXT.md D-79 (--yes/--no-input both map to confirm()=True) and D-81 (handler in cli/parser.py)
  </read_first>
  <action>
    Edit `src/zai_codex_helper/cli/parser.py`:

    1. Add a real `_handle_setup(args)` handler following the EXACT shape of `_handle_use_zai` / `_handle_restore`: lazy imports inside the body (import `run_setup` from `services.setup` and `Paths` from `services.paths`); resolve `paths = Paths.default()`; call `run_setup(paths, yes=args.yes or args.no_input, dry_run=args.dry_run)`; return the int it returns (0 on success). Do NOT catch `ZaiCodexHelperError`; do NOT call `sys.exit`. The `yes=args.yes or args.no_input` mapping honors D-79 (--yes and --no-input both force confirm()=True); `args.no_input` is added in step 3.

    2. Replace the `setup` stub wiring in `build_parser()`: remove "setup" from the `for name in ("setup", "doctor", "install-service", "uninstall-service")` loop (change the tuple to `("doctor", "install-service", "uninstall-service")`), and add an explicit `p_setup = subparsers.add_parser("setup", help="guided end-to-end onboarding")` block with `p_setup.set_defaults(func=_handle_setup)`. Leave doctor/install-service/uninstall-service as stubs (their phases).

    3. Add a `--no-input` root flag alongside `--yes`: `parser.add_argument("--no-input", action="store_true", help="non-interactive: no prompts, env vars required")`. Place it immediately after the `--yes`/`-y` block. (The handler reads `args.no_input`.) `dest` defaults to `no_input`.

    4. Update the module docstring's "Phase 12" note (the existing docstring already mentions Phase 12 wiring) to record that `setup` is now a real handler delegating to `services.setup.run_setup` (D-81) and that doctor/install-service/uninstall-service remain stubs.

    Do NOT change the `_handle_use_zai`/`_handle_use_openai`/`_handle_restore`/`_handle_status` handlers or `_apply_provider_pipeline`/`_emit_restart_warning`. Do NOT add launchctl/plist logic (D-82 — Phase 13).
  </action>
  <verify>
    <automated>cd /Users/axisrow/Projects/zai-codex-helper && python -c "
from zai_codex_helper.cli.parser import build_parser
a = build_parser().parse_args(['--no-input','setup'])
assert a.cmd == 'setup' and a.no_input is True and a.func.__name__ == '_handle_setup'
a2 = build_parser().parse_args(['--yes','setup'])
assert a2.yes is True
print('parser ok')
"</automated>
  </verify>
  <acceptance_criteria>
    - `build_parser().parse_args(['setup']).func.__name__ == '_handle_setup'` (real handler, NOT a stub closure).
    - `build_parser().parse_args(['--no-input','setup']).no_input is True`.
    - `build_parser().parse_args(['--yes','setup']).yes is True`.
    - doctor / install-service / uninstall-service STILL resolve to stub closures (unchanged).
    - `_handle_setup` lets `ZaiCodexHelperError` propagate (no try/except, no sys.exit) and returns the int from `run_setup`.
    - `--no-input` appears in `--help` output.
  </acceptance_criteria>
  <done>
    `zai-codex-helper setup` dispatches to the real `_handle_setup` which delegates to `services.setup.run_setup`; `--no-input` and `--yes` both force headless mode; the three later subcommands remain stubs; the D-11 error contract is preserved.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Write tests/test_setup.py pinning SC-1/SC-2/SC-3 + SECR-03 no-leak + D-78 no-launchctl + D-79 env-required</name>
  <files>tests/test_setup.py</files>
  <read_first>
    - tests/test_use_zai_use_openai.py (the established pattern for integration tests driving `main([...])` against `tmp_path/.codex`, reading files BACK FROM DISK, `capsys` for stdout/stderr, `@pytest.mark.integration`; the `_write` helper; `_config_toml(tmp_path)` helper)
    - tests/conftest.py (the autouse `_isolate_home` fixture — HOME repointed at tmp_path, .codex pre-created; tests never touch real $HOME)
    - src/zai_codex_helper/services/setup.py (the run_setup signature — the injected seams `input_fn`/`getpass_fn`/`confirm_fn`/`build_fn`/`environ`/`print_fn`; the canonical YAML body shape)
    - src/zai_codex_helper/cli/parser.py (the _handle_setup handler + the --no-input/--yes flags from Task 2)
    - .planning/phases/12-cli-setup/12-CONTEXT.md (the 3 ROADMAP SCs and D-77/D-78/D-79/D-80 specifics the tests must pin)
    - tests/test_yaml_backend.py (the canonical moonbridge-zai.yml body: ZAI_API_KEY + model + server={host,port})
  </read_first>
  <behavior>
    - SC-1: a full interactive `main(["setup"])` run (with getpass + confirm + build_moonbridge mocked via monkeypatch on the orchestrator's seams OR via monkeypatching the module-level references) writes moonbridge-zai.yml at 0600 with the canonical body, writes the .zshrc marker block, applies the Z.ai provider to config.toml, and prints the install-service hint.
    - SC-2: `main(["--yes","setup"])` with ZAI_API_KEY env set runs to completion with zero prompts (confirm always True, no getpass call) and produces the same on-disk state as the interactive path.
    - SC-3 (D-80): running setup twice yields byte-identical config.toml, moonbridge-zai.yml, and .zshrc (snapshot after run #1, re-run, assert equal).
    - SECR-03 no-leak: a spy on `print` (capsys) and on `getpass.getpass` captures every value the orchestrator could leak; assert the key literal does NOT appear in captured stdout OR stderr across a full run.
    - D-79 env-required: `main(["--yes","setup"])` with ZAI_API_KEY UNSET raises ZaiCodexHelperError (surfaces via main() as exit 1 + one-line `error:`).
    - D-78 no-launchctl: a spy on `subprocess.run` (monkeypatch) asserts no call's argv contains "launchctl" across the whole setup flow; on LaunchAgent consent the captured stdout contains "install-service".
  </behavior>
  <action>
    Create `tests/test_setup.py` mirroring the style of `tests/test_use_zai_use_openai.py` (module docstring citing the SCs + D-IDs; `from zai_codex_helper.__main__ import ZaiCodexHelperError, main`; `from zai_codex_helper.cli.parser import build_parser`; a module-level `_write(path, data)` helper; `@pytest.mark.integration` on the end-to-end tests, `@pytest.mark.unit` on pure dispatch checks).

    Test inventory (one test per behavior above; mock via monkeypatch on the orchestrator's injected seams where possible — prefer calling `run_setup` directly with injected fns for the unit-tier tests, and drive `main([...])` for the end-to-end SC tests):

    1. `@pytest.mark.integration test_setup_interactive_full_flow_sc1(tmp_path, capsys, monkeypatch)` — SC-1. Set `monkeypatch.setenv("ZAI_API_KEY", "sk-test-SETUP-01")` (a recognizable literal). Mock `build_moonbridge` to a no-op that touches the binary path (create an empty file at `paths.codex_dir/"moon-bridge"` so the idempotency check passes) — monkeypatch `zai_codex_helper.services.setup.build_moonbridge` (the default `build_fn`) OR pass `build_fn` via a direct `run_setup` call. Drive `main(["setup"])` with a fake input sequence for the provider choice + confirm prompts: monkeypatch `zai_codex_helper.services.setup.input` (the default input_fn) to a function returning "zai" then "y" for each confirm; monkeypatch `zai_codex_helper.services.setup.getpass` (the default getpass_fn module) so `getpass.getpass` returns a sentinel — but since ZAI_API_KEY env is set, getpass is NOT called. Assert: rc==0; moonbridge-zai.yml exists with mode 0o600 and contains "ZAI_API_KEY" + "glm-5.2" + the server block (read via yaml.safe_load); .zshrc contains the marker fence (`# >>> zai-codex-helper >>>`); config.toml has model="glm-5.2" + model_provider="zai-moonbridge".
    2. `@pytest.mark.integration test_setup_yes_flag_scriptable_sc2(tmp_path, monkeypatch)` — SC-2 / SETUP-02. `monkeypatch.setenv("ZAI_API_KEY", "sk-test-SETUP-02")`; mock build_moonbridge. `main(["--yes","setup"])`. Assert rc==0; the same on-disk state as SC-1 (Z.ai applied, yml at 0600, .zshrc marker); AND assert that `input`/`getpass`/`confirm` were NEVER called by the orchestrator (spy on them via monkeypatch and assert call_count==0, or assert via the injected seams when calling run_setup directly).
    3. `@pytest.mark.integration test_setup_twice_byte_identical_sc3(tmp_path, monkeypatch)` — SC-3 / SETUP-03 / D-80. Seed ZAI_API_KEY env; mock build_moonbridge. Run `main(["--yes","setup"])` twice. Snapshot config.toml + moonbridge-zai.yml + .zshrc bytes after run #1; re-run; assert all three files are byte-identical after run #2. Additionally assert .zshrc contains EXACTLY ONE marker fence (`text.count("# >>> zai-codex-helper >>>") == 1`).
    4. `@pytest.mark.integration test_setup_api_key_never_leaked_secr03(tmp_path, capsys, monkeypatch)` — SECR-03. Use a distinctive key literal "sk-LEAK-CANARY-1234567890" via ZAI_API_KEY env. Mock build_moonbridge. `main(["--yes","setup"])`. Assert rc==0; then `out, err = capsys.readouterr()` and assert "sk-LEAK-CANARY-1234567890" NOT in out AND NOT in err. (This is the highest-signal SECR-03 test — the canary would surface in ANY accidental print/log of the key.)
    5. `@pytest.mark.integration test_setup_no_input_requires_env_d79(tmp_path, monkeypatch, capsys)` — D-79. Ensure ZAI_API_KEY is UNSET (`monkeypatch.delenv("ZAI_API_KEY", raising=False)`). `rc = main(["--yes","setup"])`. Assert rc==1; `out, err = capsys.readouterr()`; assert err starts with "error:" and mentions "ZAI_API_KEY"; assert "Traceback" not in err.
    6. `@pytest.mark.integration test_setup_no_launchctl_call_d78(tmp_path, monkeypatch, capsys)` — D-78. Set ZAI_API_KEY env; mock build_moonbridge; install a recording spy on `subprocess.run` via `monkeypatch.setattr("zai_codex_helper.services.moonbridge.subprocess.run", fake_runner)` (the only subprocess seam in the composed primitives — build_moonbridge's runner) so the spy captures every subprocess call. `main(["--yes","setup"])`. Assert NO captured call's argv contains "launchctl"; assert captured stdout contains "install-service" (the LaunchAgent offer consent path prints it under --yes).
    7. `@pytest.mark.unit test_setup_is_real_handler_not_stub()` — dispatch check: `build_parser().parse_args(["setup"]).func.__name__ == "_handle_setup"`.
    8. `@pytest.mark.unit test_no_input_flag_parsed()` — `build_parser().parse_args(["--no-input","setup"]).no_input is True`.

    For the build_moonbridge mock: the cleanest seam is to pass `build_fn` directly when calling `run_setup` (unit-tier), OR monkeypatch `zai_codex_helper.services.setup.build_moonbridge` (the default value of the `build_fn` parameter — but note Python binds default arg values at def time, so monkeypatching the module attribute AFTER import does NOT change the already-bound default; instead pass `build_fn` explicitly through a thin wrapper, OR monkeypatch the reference the orchestrator actually calls). The orchestrator should look up `build_fn` via the parameter (default `build_moonbridge`) — to make monkeypatching work end-to-end via `main([...])`, the orchestrator MUST call the `build_fn` it was passed (the handler passes nothing → the default applies → the default is the module-level `build_moonbridge` captured at def time). SIMPLEST FIX for testability through `main`: the handler passes `build_fn=build_moonbridge` resolved at call time inside `_handle_setup`'s lazy import block, OR the orchestrator resolves `build_fn` lazily. Document whichever pattern is used and make the end-to-end `main([...])` tests mock via the path that actually works (prefer monkeypatching `zai_codex_helper.services.moonbridge.subprocess.run` so build_moonbridge's idempotency skip handles the rest — create the binary file first so build is skipped entirely and NO subprocess runs).
  </action>
  <verify>
    <automated>cd /Users/axisrow/Projects/zai-codex-helper && python -m pytest tests/test_setup.py -x -q 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - All 8 tests pass under `python -m pytest tests/test_setup.py -x`.
    - `python -m pytest -m "not e2e"` (the full non-e2e suite) still passes — no regressions in phases 1-11.
    - The SECR-03 canary test fails if the orchestrator ever prints the key (verifiable by temporarily adding a print(key) and re-running — the test catches it).
    - The D-80 idempotence test asserts byte-identical config.toml + moonbridge-zai.yml + .zshrc AND exactly one .zshrc marker fence.
    - The D-78 test asserts no launchctl subprocess call AND that "install-service" is printed under --yes.
    - The D-79 test asserts exit 1 + one-line `error:` mentioning ZAI_API_KEY when env is unset under --yes.
  </acceptance_criteria>
  <done>
    tests/test_setup.py exists with 8 tests (6 integration + 2 unit) pinning SC-1, SC-2, SC-3, SECR-03 (no-leak canary), D-78 (no-launchctl + install-service printed), D-79 (env-required under --yes); the full non-e2e suite passes with no regressions.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| stdin → orchestrator | User-typed provider choice + getpass-supplied API key cross here (interactive mode only). |
| env (`ZAI_API_KEY`) → orchestrator | Automation path: the secret crosses from the process environment into the written YAML file. |
| orchestrator → filesystem (~/.codex, ~/.zshrc) | The orchestrator writes moonbridge-zai.yml (secret at 0600), config.toml, .zshrc; a bug here corrupts precious user state. |
| orchestrator → subprocess (build_moonbridge's git/go) | The Moon Bridge build spawns network + compiler subprocesses (Phase 11 primitive — the boundary is already modeled there). |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-12-01 | Information Disclosure | API key handling (run_setup step 2 + summary) | critical | mitigate | D-77: key flows env→getpass→YamlBackend@0600 only; NEVER passed to print_fn; SECR-03 canary test (`test_setup_api_key_never_leaked_secr03`) spies on capsys stdout+stderr and asserts the canary literal is absent. |
| T-12-02 | Information Disclosure | moonbridge-zai.yml file mode | high | mitigate | YamlBackend.write_canonical defaults mode=0o600 (D-56, Phase 9); the orchestrator passes NO mode override (load-bearing). SC-1 test asserts on-disk mode == 0o600. |
| T-12-03 | Tampering | setup re-run overwrites user config | high | mitigate | D-80 idempotence by composition: backup_once sentinel (Phase 4) + YamlBackend/ShellBackend upsert replace-not-append (Phase 9) + build_moonbridge skip (Phase 11) + provider pipeline idempotence (Phase 7). SC-3 byte-identical double-setup test pins it. |
| T-12-04 | Denial of Service | --no-input without ZAI_API_KEY env | medium | mitigate | D-79: raise ZaiCodexHelperError naming the env var; surfaces via main() as exit 1 + actionable one-line error. D-79 test pins it. |
| T-12-05 | Elevation of Privilege | auto-installing Go/brew without consent | high | mitigate | D-82: setup NEVER auto-installs system toolchains; build_moonbridge's Go-missing error surfaces the brew one-liner as MESSAGE TEXT only (Phase 11 D-71); the user runs it. No install subprocess is spawned by setup. |
| T-12-06 | Tampering | LaunchAgent plist / launchctl invocation from setup | high | mitigate | D-78: setup OFFERS the LaunchAgent (prints "install-service") and does NOT call launchctl or write the plist — that is Phase 13's boundary. T-12-06 test (`test_setup_no_launchctl_call_d78`) spies on subprocess.run and asserts no launchctl argv. |
| T-12-SC | Tampering | (none — setup adds NO new package installs) | low | accept | Phase 12 adds no new third-party dependencies (stdlib getpass + already-audited tomlkit/pyyaml/httpx). No package-legitimacy gate required for this phase. |
</threat_model>

<verification>
- `python -m pytest tests/test_setup.py -x` — all 8 setup tests pass.
- `python -m pytest -m "not e2e"` — full non-e2e suite green (no regressions in phases 1-11).
- `python -c "from zai_codex_helper.cli.parser import build_parser; a=build_parser().parse_args(['--no-input','setup']); assert a.func.__name__=='_handle_setup' and a.no_input is True"` — parser wiring correct.
- Manual: `zai-codex-helper setup --help` lists `--no-input` and shows setup as a real subcommand (not "(stub)").
</verification>

<success_criteria>
- SC-1 (SETUP-01, SECR-01): `setup` interactively walks provider → API key (env or getpass, never echoed) → moonbridge-zai.yml@0600 → Moon Bridge build → shell helpers opt-in → apply provider → LaunchAgent offer → summary. Pinned by `test_setup_interactive_full_flow_sc1`.
- SC-2 (SETUP-02): `--yes`/`--no-input` run the same flow headless via the shared confirm() helper; ZAI_API_KEY env required. Pinned by `test_setup_yes_flag_scriptable_sc2` + `test_setup_no_input_requires_env_d79`.
- SC-3 (SETUP-03): double-setup yields byte-identical files. Pinned by `test_setup_twice_byte_identical_sc3`.
- SECR-03: API key never printed/logged. Pinned by `test_setup_api_key_never_leaked_secr03` (canary spy).
- D-78: LaunchAgent is offer-only (no launchctl/plist). Pinned by `test_setup_no_launchctl_call_d78`.
- D-82 scope discipline: NO launchctl/plist (Phase 13), NO doctor (Phase 14), NO models_cache entry (Phase 15), NO auto-install. Verified by code inspection + the no-launchctl spy.
</success_criteria>

<output>
Create `.planning/phases/12-cli-setup/12-01-SUMMARY.md` when done.
</output>
