---
phase: 15-polish-release-hardening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/zai_codex_helper/services/diff_preview.py
  - src/zai_codex_helper/services/setup.py
  - src/zai_codex_helper/cli/parser.py
  - src/zai_codex_helper/services/lifecycle.py
  - tests/test_dry_run_diff.py
  - tests/test_no_hardcoded_secrets.py
  - .gitignore
  - scripts/pre-commit-secret-scan.sh
  - .pre-commit-config.yaml
  - .github/workflows/ci.yml
  - tests/test_e2e_live.py
autonomous: true
requirements: [CONF-07, SECR-03, TEST-01, TEST-02, TEST-03, TEST-04, TEST-05]

must_haves:
  truths:
    # SC-1 (CONF-07, D-95)
    - "Passing --dry-run to `use zai` / `use openai` / `setup` prints a unified_diff of the would-be file contents vs the current file AND writes NOTHING — every file under the isolated HOME is byte-identical before vs after the invocation (snapshot assertion)"
    - "`install-service --dry-run` prints a 'would write plist + bootstrap' summary (no plist write, no launchctl call) — D-95 NOTE allows install-service summary depth"
    - "When the target file content would equal the current content, --dry-run prints '(no changes)' and writes nothing"
    # SC-2 (SECR-03, D-96)
    - "`grep -rnE 'sk-[A-Za-z0-9]{20,}|ZAI_API_KEY[[:space:]]*=[[:space:]]*[\"'\\''']' src/` returns ZERO matches (no hardcoded keys) — asserted by a test-gated grep audit (D-37 tomlkit-only pattern)"
    - "The API key is never routed to any print/log/summary path across the full `setup` run (re-confirm Phase 12 capsys spy — already passes; this phase KEEPS it green)"
    - ".gitignore covers *.env, auth.json, and the real user secrets files (.codex/moonbridge-zai.yml, moonbridge-zai.yml) so they can never land in the repo"
    - "A pre-commit secret-scan hook (grep-based, no external tool) fails if a staged file matches the secret-like patterns — defense-in-depth"
    # SC-3 (TEST-05, D-97)
    - ".github/workflows/ci.yml is valid YAML (yaml.safe_load parses it), defines a python matrix of exactly [3.10, 3.11, 3.12, 3.13] x [macos-latest, ubuntu-latest], builds the wheel (python -m build), pip-installs dist/*.whl (NOT editable), runs `zai-codex-helper --help` (exit 0), then pip-installs '.[dev]' and runs `pytest -m \"not e2e\"`"
    - "e2e is documented in ci.yml as local-only (the -m \"not e2e\" gate is load-bearing — e2e needs a live ZAI_API_KEY + running Moon Bridge). ci.yml lives at .github/workflows/ci.yml per D-99 (location: diff_preview.py + models_cache.py in services/, ci.yml + .gitignore + pre-commit hook at repo root)."
    # SC-3 (TEST-04 — e2e harness EXISTS, runs locally only)
    - "A tests/test_e2e_live.py harness EXISTS, marked `@pytest.mark.e2e`, implementing TEST-04: use zai → live `codex exec 'Respond exactly: OK'` → assert Z.ai response → use openai → `codex exec` again → assert OpenAI response. It is EXCLUDED from CI by the `pytest -m \"not e2e\"` gate (pyproject.toml addopts) and runs ONLY when invoked explicitly with `-m e2e` locally (requires ZAI_API_KEY env + a running Moon Bridge). The harness GUARDS on the env/binary being present and `pytest.skip`s cleanly when they are absent (so a local `pytest -m e2e` run without the prerequisites skips instead of failing)."
  artifacts:
    - src/zai_codex_helper/services/diff_preview.py
    - src/zai_codex_helper/services/setup.py  # dry-run branches emit real diffs
    - src/zai_codex_helper/cli/parser.py       # use zai / use openai dry-run branches emit real diffs
    - src/zai_codex_helper/services/lifecycle.py  # install-service dry-run summary branch
    - tests/test_dry_run_diff.py               # snapshot byte-identical + diff printed
    - tests/test_no_hardcoded_secrets.py       # grep audit gate
    - .gitignore                                # *.env / auth.json / moonbridge-zai.yml additions
    - scripts/pre-commit-secret-scan.sh        # grep-based secret scan
    - .pre-commit-config.yaml                  # wires the grep scan into pre-commit (local repo hook)
    - .github/workflows/ci.yml                 # matrix CI
    - tests/test_e2e_live.py                   # TEST-04 e2e harness (local-only, e2e-marked)
  key_links:
    - "diff_preview.compute_diff(path, target_bytes) → difflib.unified_diff(current_bytes.splitlines(), target_bytes.splitlines(), fromfile=str(path)+' (current)', tofile=str(path)+' (target)') — the single shared diff primitive every dry-run branch calls"
    - "use zai --dry-run → _apply_provider_pipeline computes the transformed doc WITHOUT write_canonical, serializes via tomlkit.dumps(), calls compute_diff(config_toml_path, serialized), prints → zero file mutation"
    - "setup --dry-run → run_setup dry-run branches compute the canonical yml/shell/config bytes (without writing) and call compute_diff for each, preserving the D-77 rule that the API key NEVER enters print_fn (the diff MUST redact or skip the secrets-bearing yml body)"
    - "pre-commit-secret-scan.sh → greps staged files for sk-… / ZAI_API_KEY= literal patterns; exit 1 on hit"
    - "ci.yml `pytest -m \"not e2e\"` → the TEST-05 gate (e2e excluded; unit+integration+smoke in CI)"
---

<objective>
Deliver three of the four Phase 15 release-hardening deliverables (D-95, D-96, D-97) plus the e2e harness (TEST-04) — everything EXCEPT the models_cache spike (D-98, Plan 02). This plan makes `--dry-run` a REAL diff preview (CONF-07), hardens secrets (SECR-03 — grep audit, .gitignore, pre-commit scan), lands the wheel-install CI matrix (TEST-05), and creates the local-only e2e harness (TEST-04).

Purpose: CONF-07 is "preview WITHOUT writing" — the current `--dry-run` branches in setup.py and parser.py only SKIP the write and print a "would do X" string. SC-1 mandates the diff IS the value. SECR-03 closes the never-in-git half of the secrets story (Phase 12 already proved never-logged). TEST-05 is the CI gate before milestone archive. TEST-04 is the e2e harness the CI gate excludes but which must EXIST for local validation.

Output: A `diff_preview.py` helper, the dry-run branches rewired to emit real diffs, a grep-based secrets test + pre-commit hook, .gitignore additions, a GitHub Actions matrix workflow, and a local-only e2e harness.
</objective>

<execution_context>
@/Users/axisrow/Projects/zai-codex-helper/.claude/gsd-core/workflows/execute-plan.md
@/Users/axisrow/Projects/zai-codex-helper/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-polish-release-hardening/15-CONTEXT.md

# Existing code this plan modifies / reads
@src/zai_codex_helper/cli/parser.py
@src/zai_codex_helper/services/setup.py
@src/zai_codex_helper/services/lifecycle.py
@src/zai_codex_helper/services/paths.py
@src/zai_codex_helper/backends/toml.py
@src/zai_codex_helper/backends/yaml.py
@src/zai_codex_helper/backends/shell.py
@.gitignore
@pyproject.toml
@tests/conftest.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: diff_preview helper + wire --dry-run real diffs into use/setup/install-service (D-95, CONF-07)</name>
  <files>src/zai_codex_helper/services/diff_preview.py, src/zai_codex_helper/cli/parser.py, src/zai_codex_helper/services/setup.py, src/zai_codex_helper/services/lifecycle.py, tests/test_dry_run_diff.py</files>
  <read_first>
    - src/zai_codex_helper/cli/parser.py — `_apply_provider_pipeline` (the D-45 write path; the dry-run branch hooks BETWEEN step f `doc = transform(doc)` and step g `backend.write_canonical(doc)`); `_handle_setup` (forwards `args.dry_run`); `_handle_install_service` (delegates to lifecycle.install_service, currently has NO dry_run param — Task 1 must add one).
    - src/zai_codex_helper/services/setup.py — `run_setup` dry-run branches currently print "would write moonbridge-zai.yml" / "would write shell helpers" / "would apply provider zai" (lines ~208-253). These are EXACTLY the three diff-preview sites (yml + shell + config.toml). The `_apply_provider_inline` helper is the inlined Phase 7 pipeline. The D-77 rule: API key NEVER enters print_fn — the yml diff MUST redact the ZAI_API_KEY value (print a redacted placeholder line for that key, real diff for model/server block).
    - src/zai_codex_helper/services/lifecycle.py — `install_service(paths, *, runner=subprocess.run)` (line 174). D-95 NOTE explicitly allows install-service to show a "would write plist" SUMMARY rather than a full plist diff. Task 1 adds an optional `dry_run: bool = False` param; when True, install_service returns 0 after printing the summary, WITHOUT writing the plist or calling launchctl.
    - src/zai_codex_helper/services/paths.py — the `Paths` dataclass fields (config_toml, moonbridge_yml, zshrc, launchagents_dir).
    - tests/conftest.py — the `_isolate_home` autouse fixture (HOME → tmp_path, .codex/ pre-created). Dry-run tests snapshot every file under tmp_path before/after to prove zero mutation.
  </read_first>
  <behavior>
    # diff_preview.compute_diff (the shared primitive)
    - compute_diff(path: pathlib.Path, target_text: str) -> str: returns a unified_diff string. If the path does NOT exist, diff against empty (every target line is an addition). If `target_text` equals the current file's text (path exists and contents are byte-identical), return the literal "(no changes)".
    - The diff uses `difflib.unified_diff(current_lines, target_lines, fromfile=f"{path} (current)", tofile=f"{path} (target)", lineterm="")`; join with "\n".
    - A separate `redact_secrets(text: str) -> str` helper replaces any `ZAI_API_KEY:` value (YAML shape `ZAI_API_KEY: <anything>`) with `ZAI_API_KEY: <redacted>` so the yml diff is safe to print (D-77).
    - # use zai / use openai --dry-run (parser.py)
    - `_apply_provider_pipeline(transform, warn_stream, dry_run=False)`: when dry_run is True, run steps a–f (paths, backend, seed-if-missing, backup_once is STILL a write — SKIP it in dry-run, print "would back up config.toml"; read, transform) but STOP before step g write_canonical. Serialize `tomlkit.dumps(doc)` and call `compute_diff(paths.config_toml, serialized)`; print the diff via print_fn/stdio; skip step h check_postconditions (no write to validate against) and step i restart warning (or print "would restart-warn"). Return without writing.
    - `_handle_use_zai` / `_handle_use_openai` forward `args.dry_run` to `_apply_provider_pipeline`.
    - # setup --dry-run (setup.py run_setup)
    - The three dry-run sites (yml, shell, config.toml) compute their canonical target bytes WITHOUT writing and call compute_diff:
      * yml site: serialize the yml_body dict via `yaml.safe_dump(yml_body, sort_keys=False, default_flow_style=False, allow_unicode=True)`, apply `redact_secrets(...)` to the serialized string, then compute_diff(paths.moonbridge_yml, redacted_serialized). Prints the redacted diff (D-77 — key never printed).
      * shell site: compute_diff(paths.zshrc, ShellBackend-rendered SHELL_HELPERS_BODY fenced block). The target is what ShellBackend.write_canonical WOULD produce — Task 1 may add a `ShellBackend.render_fence(body) -> str` read-only helper OR inline the fenced-block computation (marker fence + body). Pick the helper approach (single source of truth for the fence shape).
      * config.toml site: the inline provider pipeline mirrors use zai's dry-run — compute the transformed doc, `tomlkit.dumps(doc)`, compute_diff(paths.config_toml, serialized).
    - "would build Moon Bridge (skipped)" stays as-is (build is not a file write — no diff to show); the LaunchAgent summary stays as-is (Phase 13 owns the plist).
    - # install-service --dry-run (lifecycle.py)
    - `install_service(paths, *, runner=subprocess.run, dry_run=False)`: when dry_run is True, print a summary ("would write plist to <launchagents_dir/dev.zai.moonbridge.plist>", "would run: launchctl bootstrap gui/<UID> <plist>", "would verify via launchctl print + port probe") and return 0 WITHOUT writing the plist or calling launchctl/runner. The handler `_handle_install_service` forwards `args.dry_run`.
    - # tests (test_dry_run_diff.py)
    - Test 1 (use zai no-mutation + diff printed): seed tmp_path/.codex/config.toml with an OpenAI-default doc (comments + a [project_*] block — reuse the Phase 5 round-trip fixture shape); snapshot every file byte under tmp_path; invoke `_handle_use_zai` with dry_run=True (build a namespace where args.dry_run=True); snapshot again; assert byte-identical AND the captured stdout contains a unified-diff header (`---`/`+++` lines) mentioning `glm-5.2` or `zai-moonbridge` (the change the diff should surface).
    - Test 2 (use zai no-changes case): seed config.toml that ALREADY has the Z.ai desired state; run `use zai --dry-run`; assert stdout contains "(no changes)" and zero mutation.
    - Test 3 (setup dry-run redacts the key): invoke run_setup with dry_run=True, a fake ZAI_API_KEY env value `sk-test-FAKE-DO-NOT-USE`, capsys; assert the captured stdout does NOT contain `sk-test-FAKE-DO-NOT-USE` (D-77 still holds), DOES contain the redacted `ZAI_API_KEY: <redacted>` diff line, and zero mutation.
    - Test 4 (install-service dry-run summary): invoke `install_service(Paths.from_home(tmp_path), dry_run=True, runner=...)` with a spy runner; assert the runner was NEVER called (no launchctl), no plist exists under launchagents_dir, and stdout mentions "would write plist" / "would run: launchctl bootstrap".
    - Test 5 (use openai dry-run): symmetric to Test 1 for the revert direction (seed Z.ai doc, dry-run use openai, assert diff shows the revert + zero mutation).
  </behavior>
  <action>
    Create `src/zai_codex_helper/services/diff_preview.py` with `compute_diff(path, target_text) -> str` and `redact_secrets(text) -> str` (the YAML ZAI_API_KEY value redaction). Wire `dry_run` through `_apply_provider_pipeline` (add the param, branch before write_canonical, emit the config.toml diff), `_handle_use_zai`/`_handle_use_openai` (forward args.dry_run), `run_setup` (replace the three "would write X" prints with real compute_diff calls — yml uses redact_secrets first, shell uses a new ShellBackend.render_fence helper, config.toml mirrors the parser path), and `install_service` (add `dry_run=False` param, summary-only branch). Per D-95, use zai/use openai (config.toml) and setup (yml/zshrc/config.toml) are the full-diff priority; install-service is summary depth. NEVER route the real API key value through print — always redact_secrets first (D-77). Do NOT remove the existing restart warning for the non-dry-run path — it still fires on real writes. Keep `_apply_provider_inline` in setup.py in sync with `_apply_provider_pipeline`'s dry-run branch (both must produce the same config.toml target bytes so the preview matches what a real write would do). Do NOT add a `--dry-run` subcommand flag to install-service's argparse (it already inherits the root `--dry-run` flag from Phase 1 D-02); only the handler forwarding is new. Per D-100: NO new CLI commands, NO new top-level flags.
  </action>
  <verify>
    <automated>set -o pipefail; python -m pytest tests/test_dry_run_diff.py -v -m "not e2e" && python -m pytest tests/test_use_zai_use_openai.py tests/test_setup.py tests/test_service_lifecycle.py -v -m "not e2e"</automated>
  </verify>
  <done>
    - `use zai --dry-run` against an OpenAI-default config prints a unified diff showing the Z.ai change AND leaves every file byte-identical (snapshot assertion passes).
    - `setup --dry-run` prints the yml diff with the API key REDACTED (canary `sk-test-FAKE-DO-NOT-USE` absent from stdout), the shell-helpers diff, and the config.toml diff — zero mutation.
    - `install-service --dry-run` prints the "would write plist / would bootstrap" summary, writes NO plist, calls NO launchctl.
    - The "(no changes)" path prints exactly that when target == current.
    - All pre-existing use/setup/install-service tests still pass (no regression on the real write path).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Secrets hardening — grep audit test + .gitignore + pre-commit secret scan (D-96, SECR-03)</name>
  <files>tests/test_no_hardcoded_secrets.py, .gitignore, scripts/pre-commit-secret-scan.sh, .pre-commit-config.yaml</files>
  <read_first>
    - .gitignore — current coverage (line 150-158 has `.env`/`.venv`/`env/` but NOT `*.env`, `auth.json`, or the real secrets-file patterns). Confirm what is MISSING before adding (do not duplicate).
    - tests/test_setup.py — the Phase 12 capsys spy test that proves the API key never appears in stdout/stderr across a full setup run. Task 2 RE-CONFIRMS this stays green (the never-logged half is already proven; this task closes the never-in-git half).
    - pyproject.toml `[tool.pytest.ini_options]` markers — `unit`/`integration`/`smoke`/`e2e`. The new grep-audit test is `@pytest.mark.unit`.
    - The D-37 tomlkit-only grep gate (referenced in CONTEXT) — the established project pattern for grep-based source audits. The new test mirrors it: walk `src/`, regex-match, assert zero hits.
  </read_first>
  <behavior>
    # test_no_hardcoded_secrets.py
    - Test 1 (no hardcoded key in src/): walk every `.py` under `src/zai_codex_helper/`; for each file's text, assert `re.search(r'sk-[A-Za-z0-9]{20,}', text)` is None AND `re.search(r'ZAI_API_KEY\s*=\s*["\']', text)` is None (literal assignment, not the `environ.get("ZAI_API_KEY")` read). A test fixture file with a fake canary is used to prove the assertion CAN fail (a SELF-TEST: temporarily seeding the canary makes the test red; remove it and it's green — this is the TDD red→green proof). Use `pathlib.Path("src").rglob("*.py")`.
    - Test 2 (never-logged re-confirm): this is a thin re-assertion that the Phase 12 capsys spy test exists and still passes — Task 2 does NOT re-implement it; it documents that test_no_hardcoded_secrets.py and test_setup.py's SECR-03 spy together cover both halves (never-logged + never-hardcoded). A comment in test_no_hardcoded_secrets.py points at test_setup.py.
    - # .gitignore additions
    - Append a "Phase 15 — secrets defense-in-depth" section adding (only the missing ones — read the file first to avoid duplicates): `*.env`, `auth.json`, `.codex/moonbridge-zai.yml`, `moonbridge-zai.yml`, `*.bak`. (`.env` is already there; `*.env` is the wildcard catch. `.codex/` itself should NOT be ignored — only the secrets file inside it.)
    - # scripts/pre-commit-secret-scan.sh
    - A bash script that greps staged files (`git diff --cached --name-only`) for `sk-[A-Za-z0-9]{20,}` and `ZAI_API_KEY=[\"\']` literal patterns; if any match, print the offending file + line and `exit 1`; else `exit 0`. Uses `grep -rE` over the staged files (filter to text files via `git diff --cached --name-only`). NO external tool dependency (no gitleaks/detect-secrets) per D-96.
    - # .pre-commit-config.yaml
    - A minimal local-repo hook config wiring `scripts/pre-commit-secret-scan.sh` as a pre-commit hook (language: system, entry: bash scripts/pre-commit-secret-scan.sh, pass_filenames: false — the script reads staged files itself).
    - # tests for the hook itself
    - Test 3 (the hook script is executable and well-formed): `subprocess.run(["bash", "-n", "scripts/pre-commit-secret-scan.sh"]).returncode == 0` (syntax check); `os.access("scripts/pre-commit-secret-scan.sh", os.X_OK)` is True (executable bit set). A second assertion seeds a temp git index with a canary file and asserts the script exits 1 (the self-test proof).
  </behavior>
  <action>
    Create `tests/test_no_hardcoded_secrets.py` with the two source-grep assertions (hardcoded-key patterns → zero hits across `src/`) PLUS the hook-script well-formedness/self-test. Edit `.gitignore` to append the Phase 15 secrets section (only the patterns NOT already present — read the file first). Create `scripts/pre-commit-secret-scan.sh` (grep-based, exit 1 on secret-like match in staged files, exit 0 otherwise; `chmod +x` it). Create `.pre-commit-config.yaml` wiring the script as a local pre-commit hook. The grep pattern MUST match `sk-` followed by 20+ word chars AND a literal `ZAI_API_KEY=<quote>` assignment — but MUST NOT match `environ.get("ZAI_API_KEY")` (the legit env read) or docstrings that merely mention the name. Use anchored/narrow patterns: `ZAI_API_KEY\s*=\s*["'][^"']+["']` catches an assignment with a literal value; the env read has no `=`. Per D-100: NO new CLI commands, NO new runtime dependencies (the hook is bash + grep, stdlib-only).
  </action>
  <verify>
    <automated>set -o pipefail; python -m pytest tests/test_no_hardcoded_secrets.py -v -m "not e2e" && bash -n scripts/pre-commit-secret-scan.sh && test -x scripts/pre-commit-secret-scan.sh && python -c "import yaml; yaml.safe_load(open('.pre-commit-config.yaml'))"</automated>
  </verify>
  <done>
    - The grep-audit test passes (zero `sk-…` / zero literal `ZAI_API_KEY="…"` in `src/`).
    - The test is proven to FAIL when a canary is seeded (the self-test fixture documents the red path).
    - `.gitignore` covers `*.env`, `auth.json`, the secrets yml, `*.bak` (deduped against existing entries).
    - `scripts/pre-commit-secret-scan.sh` is executable, passes `bash -n`, and exits 1 on a staged canary.
    - `.pre-commit-config.yaml` parses as valid YAML and wires the local hook.
    - The Phase 12 setup spy test (`test_setup.py`) STILL passes (never-logged half re-confirmed green).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: CI matrix workflow — wheel install + --help + pytest-not-e2e on Python 3.10–3.13 (D-97, TEST-05)</name>
  <files>.github/workflows/ci.yml, tests/test_ci_workflow.py</files>
  <read_first>
    - pyproject.toml — `[project.scripts]` (the `zai-codex-helper` console script the wheel-install step must surface); `[project.optional-dependencies] dev` (the `.[dev]` install the pytest step needs — pytest, pytest-httpserver, build, hatchling, ruff); `requires-python = ">=3.10"` (the floor the matrix must respect); `[tool.pytest.ini_options]` `addopts = ["-m", "not e2e"]` (already excludes e2e by default — CI's explicit `-m "not e2e"` is belt-and-suspenders).
    - .gitignore — `dist/` is ignored (line 13), so the workflow's `python -m build` output is NOT committed; the workflow runs `pip install dist/*.whl` in-place.
    - The CLAUDE.md "Installation" comment: "CI прогоняет unit + integration + smoke; e2e прогоняется локально автором" — the TEST-05 contract this workflow implements.
  </read_first>
  <behavior>
    # ci.yml structure (D-97 verbatim)
    - `name: CI`; `on: [push, pull_request]`.
    - A `build-and-test` job with `strategy.matrix`: `python-version: ["3.10", "3.11", "3.12", "3.13"]`, `os: [macos-latest, ubuntu-latest]`; `runs-on: ${{ matrix.os }}`.
    - Steps (order is load-bearing per D-97):
      1. `actions/checkout@v4`
      2. `actions/setup-python@v5` with `python-version: ${{ matrix.python-version }}`
      3. `python -m pip install --upgrade pip build`
      4. `python -m build` (produces `dist/*.whl`)
      5. `pip install dist/*.whl` (the BUILT WHEEL — NOT `pip install -e .`; proves the wheel + console script work for a real user)
      6. `zai-codex-helper --help` (assert exit 0 — the wheel's console script is invokable)
      7. `pip install ".[dev]"` (pulls pytest, pytest-httpserver, build, hatchling, ruff)
      8. `pytest -m "not e2e"` (unit + integration + smoke; e2e excluded — needs live ZAI_API_KEY + Moon Bridge, local-only per TEST-05)
    - A comment block at the top of ci.yml documenting: "e2e (live codex exec through Z.ai) is EXCLUDED from CI — run `pytest -m e2e` locally with a live ZAI_API_KEY and a running Moon Bridge (TEST-04)."
    - # test_ci_workflow.py (the validation test — no GitHub Actions runner needed)
    - Test 1 (ci.yml is valid YAML): `yaml.safe_load(open(".github/workflows/ci.yml"))` succeeds (no YAML parse error).
    - Test 2 (matrix is exactly 3.10–3.13 × macos/ubuntu): assert the parsed matrix `python-version` list equals `["3.10", "3.11", "3.12", "3.13"]` and `os` equals `["macos-latest", "ubuntu-latest"]`.
    - Test 3 (wheel-install + --help + pytest-not-e2e steps present): assert the job's step `run` commands include `python -m build`, `pip install dist/*.whl` (NOT `pip install -e .`), `zai-codex-helper --help`, and `pytest -m "not e2e"`. Assert NO step runs `pytest -m e2e` or `pytest --markers e2e` in CI (the local-only gate).
    - Test 4 (e2e exclusion documented): assert a comment or step-name in ci.yml mentions that e2e is local-only (proven via the parsed `name` fields or a raw-text substring check on the file).
  </behavior>
  <action>
    Create `.github/workflows/ci.yml` implementing the D-97 matrix and the 8 ordered steps. The job MUST install the built wheel (step 5 `pip install dist/*.whl`), NOT do an editable install — this is the load-bearing distinction that proves the wheel works for a real pip user. The `zai-codex-helper --help` step (6) runs BEFORE `pip install ".[dev]"` (step 7) to prove the console script is invokable from the wheel alone (dev deps must not be required for `--help`). The `pytest -m "not e2e"` step (8) is the TEST-05 gate. Create `tests/test_ci_workflow.py` that parses ci.yml via `yaml.safe_load` and asserts the matrix + step structure. Mark the test `@pytest.mark.unit` (it parses a file, no IO beyond reading the repo's own yaml). Per D-100: NO new CLI commands; this is pure release hardening.
  </action>
  <verify>
    <automated>set -o pipefail; python -m pytest tests/test_ci_workflow.py -v -m "not e2e" && python -c "import yaml; ci = yaml.safe_load(open('.github/workflows/ci.yml')); assert ci['jobs']['build-and-test']['strategy']['matrix']['python-version'] == ['3.10', '3.11', '3.12', '3.13']; print('ci.yml matrix OK')"</automated>
  </verify>
  <done>
    - `.github/workflows/ci.yml` exists, parses as valid YAML, and defines the 4-Python × 2-OS matrix.
    - The job installs the built wheel (not editable), runs `zai-codex-helper --help`, then runs `pytest -m "not e2e"`.
    - e2e is documented as local-only in a ci.yml comment.
    - `tests/test_ci_workflow.py` validates all of the above statically (the test passes without a GitHub Actions runner).
  </done>
</task>

<task type="auto">
  <name>Task 4: e2e harness — use zai → live codex exec → use openai → codex exec (TEST-04, local-only)</name>
  <files>tests/test_e2e_live.py</files>
  <read_first>
    - pyproject.toml `[tool.pytest.ini_options]` — `addopts = ["--strict-markers", "-m", "not e2e"]`. This means `pytest` (the default CI invocation) EXCLUDES e2e tests automatically. The harness runs ONLY when invoked explicitly: `pytest -m e2e tests/test_e2e_live.py`. The `e2e` marker is registered (line 81: `"e2e: live codex exec through Z.ai (LOCAL ONLY, never in CI)"`).
    - src/zai_codex_helper/cli/parser.py — the `use zai` / `use openai` handlers (the harness invokes the CLI entry point OR calls the handlers directly; the harness MUST run the REAL write path against the REAL `~/.codex/config.toml`, NOT the isolated tmp_path — e2e is the only test tier that touches the real HOME, so it MUST NOT use the autouse `_isolate_home` fixture's isolation. The harness sets a module-level `pytestmark = pytest.mark.e2e` and uses `Paths.default()` explicitly.).
    - CLAUDE.md "Installation" comment: "e2e прогоняется локально автором (требует живого ключа и сервиса)" — TEST-04's contract. The harness needs (a) `ZAI_API_KEY` env set, (b) a built Moon Bridge binary at `~/.codex/moon-bridge`, (c) the Moon Bridge service running on `127.0.0.1:38440`, (d) the `codex` CLI on PATH.
    - tests/test_moonbridge.py — the existing e2e-marked test (the Moon Bridge build e2e). Mirror its guard/skip pattern: check prerequisites at the top, `pytest.skip(...)` if absent.
  </read_first>
  <behavior>
    # tests/test_e2e_live.py — TEST-04 harness
    - Module-level `pytestmark = pytest.mark.e2e` (so it is ALWAYS excluded by the `-m "not e2e"` gate; only an explicit `-m e2e` run picks it up).
    - A `@pytest.fixture(autouse=True)` guard at the top of the module that checks the four prerequisites and `pytest.skip`s the whole module if any is absent: (1) `ZAI_API_KEY` in os.environ, (2) `~/.codex/moon-bridge` exists + executable, (3) `127.0.0.1:38440` is reachable (a short httpx GET `/v1/models` with a 2s timeout), (4) `shutil.which("codex")` is not None. The skip message names WHICH prerequisite is missing so the author can fix it. THIS IS LOAD-BEARING: a clean skip (not a fail) means `pytest -m e2e` without prerequisites is green-by-skip, not red.
    - Test 1 (use zai → codex exec → Z.ai response): run `zai-codex-helper use zai` via `subprocess.run([sys.executable, "-m", "zai_codex_helper", "use", "zai"], check=True)`; then run `codex exec "Respond exactly: OK"` via subprocess (capture stdout); assert the response indicates Z.ai handled it (the assertion is documented as best-effort — the exact response shape depends on the live model; assert exit 0 + non-empty stdout as the baseline, and if feasible assert the response mentions glm-5.2 or a Z.ai signature). Restore state at the end (run `use openai` in a finally block so the author's default is not left flipped).
    - Test 2 (use openai → codex exec → OpenAI response): the symmetric revert. Run `use openai`, then `codex exec "Respond exactly: OK"`, assert exit 0 + non-empty stdout. This test RESTORES the OpenAI default (so after a full `pytest -m e2e` run, the author's config is back to OpenAI — the safe default).
    - The tests run against the REAL `~/.codex/config.toml` (NO `_isolate_home` — the autouse fixture is OVERRIDDEN at module scope with a no-op, since e2e must touch the real HOME by definition). Document this in a comment: e2e is the ONE tier that does NOT isolate HOME.
    - A `conftest.py`-level note is NOT needed — the module-scope autouse override is sufficient (pytest resolves the closest fixture).
  </behavior>
  <action>
    Create `tests/test_e2e_live.py` with `pytestmark = pytest.mark.e2e` (module-level — always excluded from CI by the `addopts -m "not e2e"` gate). Add a module-scope autouse fixture that overrides the project-wide `_isolate_home` (a no-op for this module — e2e touches the real HOME) AND guards on the four prerequisites (ZAI_API_KEY env, moon-bridge binary, port 38440 reachable, codex CLI on PATH), `pytest.skip`-ing the whole module with a named-prerequisite message if any is absent. Implement Test 1 (use zai → codex exec → Z.ai) and Test 2 (use openai → codex exec → OpenAI, which also restores the default). Both tests use `subprocess.run` to invoke the real CLI + the real `codex` CLI; both assert exit 0 + non-empty stdout. Wrap Test 1 in a try/finally that runs `use openai` at the end so the author's config is never left flipped. Per D-100: NO new CLI command, NO new runtime dependency (subprocess + shutil + httpx are already available). The harness is purely a TEST artifact — it does not change src/.
  </action>
  <verify>
    <automated>set -o pipefail; python -m pytest tests/test_e2e_live.py -m e2e --co -q && python -m pytest -m "not e2e" -q --co tests/test_e2e_live.py 2>&1 | grep -q "no tests ran" && echo "e2e correctly EXCLUDED from default run" || echo "WARN: e2e not excluded as expected"</automated>
  </verify>
  <done>
    - `tests/test_e2e_live.py` exists with `pytestmark = pytest.mark.e2e`.
    - The default `pytest` run (CI's `pytest -m "not e2e"`) collects ZERO tests from this file (it is excluded by the gate).
    - `pytest -m e2e tests/test_e2e_live.py --co` collects Test 1 and Test 2.
    - Without prerequisites, `pytest -m e2e` SKIPS the module cleanly (named-prerequisite message), does NOT fail.
    - With prerequisites, Test 1 flips to Z.ai + live codex exec + restores OpenAI in finally; Test 2 flips to OpenAI + live codex exec.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `--dry-run` output → user terminal | The diff preview crosses from the tool's internal would-be-state to stdout. If a secret value appears in the diff, it leaks to the terminal (and any piped capture). D-77 / SECR-03 apply: the yml diff MUST redact the ZAI_API_KEY value before printing. |
| staged files → git history | The pre-commit secret scan is the last gate before a file with an accidental `sk-…` literal crosses into the commit. A bypass (hook disabled) leaves the repo exposed; the grep-audit test + .gitignore are defense-in-depth behind it. |
| CI runner → built wheel | CI installs `dist/*.whl` (not editable). If the wheel build is misconfigured (missing `[project.scripts]`), `zai-codex-helper --help` fails — the step-6 check catches this before milestone archive. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-15-01 | Information Disclosure | `--dry-run` yml diff output (setup dry-run branch) | high | mitigate | `redact_secrets()` replaces the `ZAI_API_KEY:` value with `<redacted>` BEFORE `compute_diff` prints; test_no_hardcoded_secrets / test_dry_run_diff Test 3 seeds a canary and asserts it is absent from captured stdout (D-77). |
| T-15-02 | Information Disclosure | hardcoded key in `src/` | critical | mitigate | grep-audit test asserts zero `sk-…` / zero literal `ZAI_API_KEY="…"` across `src/`; canary self-test proves the assertion fires. |
| T-15-03 | Information Disclosure | accidental commit of `moonbridge-zai.yml` / `auth.json` / `*.env` | high | mitigate | `.gitignore` additions (defense layer 1) + `pre-commit-secret-scan.sh` grep hook on staged files (defense layer 2 — catches accidents that bypass .gitignore, e.g. `--force`). |
| T-15-04 | Tampering | wheel-install CI step | medium | mitigate | CI installs `dist/*.whl` (not editable) and runs `zai-codex-helper --help` — a broken console-script entry point fails the build before milestone archive (D-97). |
| T-15-05 | Denial of Service | pre-commit hook false-positive on legit `environ.get("ZAI_API_KEY")` reads | low | accept | The grep pattern is anchored to a literal assignment (`=`), not the env-read call shape; the narrow pattern avoids blocking legit source. Documented in the hook script comment. |
| T-15-SC | Tampering | (no package-manager installs in this plan — stdlib difflib/yaml/json + bash/grep only) | n/a | accept | No `pip install`/`npm install` of new runtime deps in this plan; ruff/pytest/build are already in dev deps. Skipped SC row per planner-authority limits (no new packages). |
</threat_model>

<verification>
- `python -m pytest -m "not e2e"` — the full CI-tier suite passes (unit + integration + smoke), including the three new test files.
- `python -m pytest tests/test_dry_run_diff.py tests/test_no_hardcoded_secrets.py tests/test_ci_workflow.py -v` — the Phase 15 hardening tests specifically.
- `python -m pytest tests/test_e2e_live.py --co -m e2e` — collects the e2e harness tests (they are EXCLUDED from the default `pytest` run by the `-m "not e2e"` gate; this just confirms they EXIST and are discoverable when explicitly selected).
- `grep -rnE 'sk-[A-Za-z0-9]{20,}|ZAI_API_KEY[[:space:]]*=[[:space:]]*["'\'']' src/ | grep -v '^#' | grep -c .` returns 0 (manual confirmation of the audit gate).
- `bash -n scripts/pre-commit-secret-scan.sh && test -x scripts/pre-commit-secret-scan.sh` — hook is syntactically valid and executable.
- `python -c "import yaml; yaml.safe_load(open('.github/workflows/ci.yml'))"` — ci.yml is valid YAML.
- Manual smoke (optional): `python -m build && pip install --force-reinstall dist/*.whl && zai-codex-helper --help` succeeds locally (mirrors the CI step-6 contract).
</verification>

<success_criteria>
- **SC-1 (CONF-07, D-95):** `--dry-run` on `use zai` / `use openai` / `setup` prints a real unified_diff (with the API key REDACTED in the yml case) AND leaves every file byte-identical under the isolated HOME. `install-service --dry-run` prints a summary (no plist write, no launchctl).
- **SC-2 (SECR-03, D-96):** The grep audit returns zero hardcoded keys across `src/`; `.gitignore` covers `*.env`/`auth.json`/`moonbridge-zai.yml`/`*.bak`; the pre-commit grep hook is in place and exits 1 on a staged canary; the Phase 12 never-logged spy stays green.
- **SC-3 (TEST-05, D-97):** `.github/workflows/ci.yml` defines the 3.10–3.13 × macos/ubuntu matrix, installs the built wheel, runs `--help`, and runs `pytest -m "not e2e"` — validated statically by `test_ci_workflow.py`.
- **TEST-04 (e2e harness):** `tests/test_e2e_live.py` exists with `@pytest.mark.e2e`, excluded from CI by the `-m "not e2e"` gate, skips cleanly without prerequisites, runs `use zai`/`use openai` + live `codex exec` locally with prerequisites.
- **SC-4 is owned by Plan 02** (models_cache spike — Wave 2, after this plan).
- D-100 honored: no new CLI commands, no PyPI publish, no Core Value change.
</success_criteria>

<output>
Create `.planning/phases/15-polish-release-hardening/15-01-SUMMARY.md` when done.
</output>
