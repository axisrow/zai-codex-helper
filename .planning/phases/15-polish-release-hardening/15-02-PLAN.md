---
phase: 15-polish-release-hardening
plan: 02
type: execute
wave: 2
# Wave 2 (NOT Wave 1): Plan 01 and Plan 02 BOTH modify src/zai_codex_helper/services/setup.py
# (Plan 01 rewires the 3 existing dry-run branches in run_setup; Plan 02 inserts a new
# models_cache step into run_setup). Same-wave plans must have ZERO files_modified overlap,
# so Plan 02 runs strictly after Plan 01. Plan 02's setup step uses diff_preview.compute_diff
# (which Plan 01 created) — a genuine cross-plan dependency on top of the file-overlap rule.
depends_on: [15-01]
files_modified:
  - src/zai_codex_helper/services/models_cache.py
  - src/zai_codex_helper/backends/json_backend.py
  - src/zai_codex_helper/services/setup.py
  - tests/test_models_cache.py
  - tests/fixtures/models_cache_seed.json
autonomous: true
requirements: [SEC-02]

must_haves:
  truths:
    # SC-4 (SEC-02, D-98 — SPIKE-FIRST, verified schema)
    - "The real ~/.codex/models_cache.json schema is DOCUMENTED in src/zai_codex_helper/services/models_cache.py module docstring: top-level dict with keys ['fetched_at', 'etag', 'client_version', 'models']; 'models' is a LIST of model-entry dicts; each entry is keyed by its 'slug' field (e.g. {'slug': 'gpt-5.5', 'display_name': 'GPT-5.5', 'default_reasoning_level', 'supported_reasoning_levels': [{'effort': 'low'|'medium'|'high'|'xhigh', 'description'}, ...], 'context_window', 'max_context_window', ...}). This documentation is the spike deliverable."
    - "model_catalog_json was EVALUATED as an alternative: the real schema has NO 'model_catalog_json' key (the walk found only fetched_at/etag/client_version/models), so the decision is DOCUMENTED as 'models_cache.json is the correct target; model_catalog_json is not used by this Codex version' (per D-98's 'evaluate as alternative' mandate)."
    # The list-aware merge (the load-bearing fix the spike revealed)
    - "Updating models_cache.json with the glm-5.2 entry PRESERVES every existing entry in the 'models' list (the user's 5 models survive) — the merge is LIST-AWARE (match by 'slug', replace-or-append the single glm-5.2 element), NOT a wholesale list overwrite. The current JsonBackend.deep_merge would CLOBBER the list because it overwrites lists wholesale — this plan adds a list-aware merge path for the 'models' key."
    - "Running the models_cache update twice produces byte-identical output (idempotent — the glm-5.2 entry is replaced, not duplicated; no second 'glm-5.2' slug appears in the list)."
    - "The glm-5.2 entry content (slug, display_name, default_reasoning_level='xhigh' or per real schema, supported_reasoning_levels, context_window, etc.) is sourced from the REAL schema observed in the author's ~/.codex/models_cache.json (the gpt-5.5 entry shape), NOT a guess. Where glm-5.2-specific values (context_window, etc.) are unknown, they are DOCUMENTED as best-effort in the entry dict (the warning may persist if Codex's expectation differs — D-98 caveat)."
    - "The models_cache update MAY be invoked standalone (a `services.models_cache.update_models_cache(paths)` function) OR wired into `setup` (so one command fixes everything). Per D-98 the planner decides: setup-integration is cleaner. The update is OPT-IN from setup (a new dry-run-safe step) OR called automatically after the provider is applied — DOCUMENTED either way. In dry-run, the step uses Plan 01's `diff_preview.compute_diff` (hard dependency — Plan 02 runs in Wave 2, after Plan 01)."
  artifacts:
    - src/zai_codex_helper/services/models_cache.py  # the spike doc + update_models_cache function
    - src/zai_codex_helper/backends/json_backend.py   # list-aware merge addition (deep_merge_lists or new method)
    - src/zai_codex_helper/services/setup.py          # setup wiring (D-98: setup-integration)
    - tests/test_models_cache.py                      # idempotent list-aware merge tests
    - tests/fixtures/models_cache_seed.json           # a seed file mirroring the real schema shape (5 models, no glm-5.2)
  key_links:
    - "services/models_cache.update_models_cache(paths) → reads models_cache.json via JsonBackend, builds the glm-5.2 entry dict (from the REAL schema fields), calls a LIST-AWARE merge on the 'models' key (match-by-slug replace-or-append, preserving all other entries), writes back via JsonBackend (atomic, crash-safe)."
    - "backends/json_backend.py: a new helper (e.g. `merge_model_list(existing: list, override_entries: list, key='slug') -> list`) that replaces entries with matching slug and appends new ones — DISTINCT from deep_merge (which clobbers lists). The JsonBackend.write_canonical still uses deep_merge for the top-level dict (fetched_at/etag/client_version untouched), but the 'models' key uses the list-aware merge. This separation is the load-bearing fix."
    - "setup.py run_setup: a new step (after the provider apply, before the LaunchAgent offer) calls update_models_cache; in dry-run mode it computes the would-be list and emits a diff via diff_preview.compute_diff (the Plan 01 primitive — Plan 02 depends on the helper existing BUT NOT on Plan 01's task completion, since diff_preview.py is standalone; if Plan 02 runs first, the setup dry-run for models_cache falls back to a 'would update models_cache' summary)."
    - "tests/fixtures/models_cache_seed.json: a 5-model seed (gpt-5.5, gpt-5-codex, o4, etc. — mirrors the real observed slugs) with NO glm-5.2, used to prove the merge adds glm-5.2 WITHOUT clobbering the 5 existing entries."
---

<objective>
Deliver SC-4 / D-98 / SEC-02: the models_cache.json glm-5.2 entry, implemented ONLY after verifying the real schema (the spike). This plan runs in **Wave 2** (after Plan 01) because both plans modify `src/zai_codex_helper/services/setup.py` (same-wave plans must have zero files_modified overlap), AND because Plan 02's setup dry-run branch for models_cache imports Plan 01's `diff_preview.compute_diff` helper.

Purpose: The real `~/.codex/models_cache.json` (178KB, present on the author's machine) has a schema that BREAKS the naive JsonBackend.deep_merge: the `models` field is a LIST of dicts keyed by `slug`, and deep_merge overwrites lists wholesale — so a `write_canonical({"models": [glm_entry]})` call would CLOBBER the user's 5 existing models. D-98 explicitly mandates a spike-first, non-clobbering merge; this plan delivers exactly that, with the real schema documented in the module docstring (the spike deliverable).

Output: A `models_cache.py` service (spike doc + update function), a list-aware merge helper added to `json_backend.py`, setup wiring (D-98: setup-integration is cleaner), and idempotent list-aware merge tests.
</objective>

<execution_context>
@/Users/axisrow/Projects/zai-codex-helper/.claude/gsd-core/workflows/execute-plan.md
@/Users/axisrow/Projects/zai-codex-helper/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-polish-release-hardening/15-CONTEXT.md

# Real schema (observed on the author's machine — the SPIKE result, load-bearing)
# Top-level: {"fetched_at": str, "etag": str, "client_version": str, "models": [LIST]}
# Each models[] entry: {"slug": "gpt-5.5", "display_name": "GPT-5.5", "description": str,
#   "default_reasoning_level": "medium", "supported_reasoning_levels": [{"effort": "low"|"medium"|"high"|"xhigh", "description": str}, ...],
#   "shell_type", "visibility", "supported_in_api", "priority", "context_window", "max_context_window", ...} (30+ fields)
# glm-5.2 is NOT present in the observed file (0 glm entries).

# Existing code this plan modifies / reads
@src/zai_codex_helper/backends/json_backend.py
@src/zai_codex_helper/services/setup.py
@src/zai_codex_helper/services/paths.py
@tests/test_json_backend.py
@tests/conftest.py

# Plan 01 dependency (Plan 02 is Wave 2, after Plan 01 ships diff_preview.py):
# This plan's setup dry-run branch imports diff_preview.compute_diff from Plan 01.
# The list-aware merge logic (Task 1) has ZERO Plan 01 dependency; only the
# setup-integration step (Task 2) reuses Plan 01's diff primitive.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: List-aware model-list merge in JsonBackend (preserve user entries, replace-by-slug) (D-98, SEC-02)</name>
  <files>src/zai_codex_helper/backends/json_backend.py, tests/test_models_cache.py, tests/fixtures/models_cache_seed.json</files>
  <read_first>
    - src/zai_codex_helper/backends/json_backend.py — the existing `deep_merge(base, override)` recurses ONLY when both sides are dicts; at a list leaf it OVERWRITES wholesale (the comment at line 27-29 explicitly says "lists are NOT element-merged, they are overwritten"). For `models_cache.json`, the `models` key is a list — so `deep_merge` would CLOBBER the user's existing 5 models. This task adds a SEPARATE list-aware merge path for the `models` key WITHOUT changing `deep_merge`'s contract (other callers may rely on list-overwrite semantics).
    - src/zai_codex_helper/backends/json_backend.py `JsonBackend.write_canonical(content, mode=None)` — the SC-3 idempotent deep-merge entry point. Task 1 must decide WHERE the list-aware logic lives: (a) a new top-level helper `merge_model_list(existing, override_entries, key='slug')`, and (b) `write_canonical` is extended so that when `content` has a `models` key, it routes that key through `merge_model_list` instead of `deep_merge`'s list-overwrite. The non-`models` keys (fetched_at/etag/client_version) still use deep_merge (unchanged).
    - tests/test_json_backend.py — the existing deep_merge purity/non-mutation/TypeError tests. Task 1 ADDS tests for the new list-aware path; it does NOT modify the existing deep_merge tests (the contract is unchanged for non-models_cache callers).
    - The REAL schema (above): `models` is a list of dicts keyed by `slug`. The merge matches by `slug`, replaces the entry if the slug exists, appends if it does not. Order: preserve the existing list order, with a new entry appended at the end (deterministic — appended entries always go last, which makes the byte-snapshot idempotence test stable across runs).
  </read_first>
  <behavior>
    # merge_model_list (new helper in json_backend.py)
    - `merge_model_list(existing: list, override_entries: list, key: str = 'slug') -> list`: returns a NEW list. For each entry in `existing`: if no override entry has the same `key` value, keep it as-is; if an override entry matches, REPLACE it with the override entry (wholesale — the override wins at the entry level, mirroring deep_merge's leaf semantics). After iterating `existing`, APPEND any override entries whose `key` value was NOT in `existing` (the new slugs, e.g. glm-5.2), in their original order. The returned list is deterministic: existing order preserved, new entries appended.
    - Purity: returns a new list; does NOT mutate `existing` or `override_entries` (proven by a test mirroring `test_json_deep_merge_does_not_mutate_inputs`).
    - TypeError if either arg is not a list (mirrors deep_merge's defensive contract).
    - # write_canonical extension (list-aware for the 'models' key)
    - `JsonBackend.write_canonical(content, mode=None)`: compute `merged = deep_merge(current, content)` AS BEFORE for the top-level dict, BUT if both `current` and `content` have a `models` key and both values are lists, OVERRULE the deep_merge result for that key with `merge_model_list(current['models'], content['models'])`. This is the surgical fix — deep_merge still handles fetched_at/etag/client_version and any other dict keys; only the models-list leaf gets the list-aware path.
    - # tests (test_models_cache.py)
    - Test 1 (list-aware merge preserves existing entries): seed `models_cache.json` via the fixture (5 models: gpt-5.5, gpt-5-codex, o4, o3, gpt-4.1 — NO glm-5.2); call `JsonBackend.write_canonical({"models": [glm_entry]})`; re-read; assert the result has 6 models (the 5 originals + glm-5.2), every original slug survives, glm-5.2 is appended last.
    - Test 2 (idempotent — replace, not duplicate): call `write_canonical({"models": [glm_entry]})` TWICE on the same seed; assert byte-identical output after the 2nd call AND exactly ONE glm-5.2 entry in the list (no duplicate slug).
    - Test 3 (replace-by-slug on existing glm-5.2): seed with the 5 models + an existing glm-5.2 entry with a STALE context_window; call write_canonical with the fresh glm-5.2 entry; assert exactly 6 entries, the glm-5.2 entry has the FRESH context_window, the other 5 are untouched, and glm-5.2 is in its ORIGINAL position (not appended — it was an existing slug).
    - Test 4 (top-level keys untouched): after the merge, assert `fetched_at`, `etag`, `client_version` are byte-identical to the seed (deep_merge still handles them; the list-aware path only touches `models`).
    - Test 5 (purity): `merge_model_list(existing, override)` does not mutate `existing` or `override` (snapshot both before/after).
    - Test 6 (TypeError on non-list): `merge_model_list("not a list", [])` raises TypeError.
    - # fixture (tests/fixtures/models_cache_seed.json)
    - A JSON file mirroring the real observed schema: `{"fetched_at": "2026-06-29T00:00:00Z", "etag": "seed-etag", "client_version": "0.1.0", "models": [<5 model entries with slug + display_name + default_reasoning_level + supported_reasoning_levels + context_window + max_context_window>]}`. The 5 entries mirror the real observed slugs (gpt-5.5, gpt-5-codex, o4, o3, gpt-4.1) with representative field values. Loaded by the tests via `pathlib.Path("tests/fixtures/models_cache_seed.json")`.
  </behavior>
  <action>
    Add `merge_model_list(existing, override_entries, key='slug') -> list` to `src/zai_codex_helper/backends/json_backend.py` (export it in `__all__` alongside `deep_merge`). Extend `JsonBackend.write_canonical` so the `models` key uses `merge_model_list` when both sides have a list at that key (the surgical list-aware override of deep_merge's list-overwrite). Document the rationale in the write_canonical docstring: "models_cache.json's 'models' field is a LIST keyed by 'slug' — a wholesale list-overwrite (deep_merge's default) would clobber the user's existing entries; merge_model_list replaces-by-slug and appends new slugs, preserving every existing entry (SC-4 / D-98 non-clobbering mandate)." Do NOT change `deep_merge`'s contract (other callers may depend on list-overwrite). Create `tests/fixtures/models_cache_seed.json` mirroring the real schema (5 models, no glm-5.2). Create `tests/test_models_cache.py` with the 6 tests above. Mark tests `@pytest.mark.unit`. The glm-5.2 entry used in tests is a representative dict (slug + display_name + reasoning levels + context_window) — the REAL entry values live in `services/models_cache.py` (Task 2).
  </action>
  <verify>
    <automated>set -o pipefail; python -m pytest tests/test_models_cache.py tests/test_json_backend.py -v -m "not e2e"</automated>
  </verify>
  <done>
    - `merge_model_list` preserves all existing entries, replaces by slug, appends new slugs deterministically.
    - `write_canonical` on a 5-model seed with a glm-5.2 entry yields 6 models (5 originals + glm-5.2) — the user's entries SURVIVE (the D-98 non-clobbering mandate).
    - Double-write is byte-identical (idempotent — no duplicate glm-5.2 slug).
    - Replace-by-slug keeps the original position; append puts new slugs last.
    - Top-level keys (fetched_at/etag/client_version) are untouched.
    - All pre-existing `test_json_backend.py` tests still pass (deep_merge contract unchanged).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: models_cache.py service (spike doc + update_models_cache) + setup wiring (D-98, SEC-02)</name>
  <files>src/zai_codex_helper/services/models_cache.py, src/zai_codex_helper/services/setup.py, tests/test_models_cache.py</files>
  <read_first>
    - The REAL schema (context block above) — the module docstring of `models_cache.py` MUST document it verbatim. This IS the spike deliverable (D-98: "SPIKE FIRST: read the REAL schema"). The schema was observed at `/Users/axisrow/.codex/models_cache.json` (178KB, 5 models, glm-5.2 absent).
    - src/zai_codex_helper/services/setup.py `run_setup` — the step order (D-76: provider → key → yml → build → shell → provider-apply → LaunchAgent offer → summary). D-98 says "setup-integration is cleaner" — Task 2 adds a new step AFTER provider-apply and BEFORE the LaunchAgent offer: update models_cache. In dry-run mode, it computes the would-be list and either emits a diff (if Plan 01's diff_preview.py exists) or a summary count.
    - src/zai_codex_helper/services/paths.py — `paths.models_cache` (the JsonBackend target).
    - src/zai_codex_helper/backends/json_backend.py (post-Task-1) — `JsonBackend(paths).write_canonical({"models": [glm_entry]})` now does the list-aware merge.
  </read_first>
  <behavior>
    # services/models_cache.py — the SPIKE-DOCUMENTED entry + update function
    - Module docstring documents the REAL schema verbatim (top-level keys, models list shape, entry fields observed on gpt-5.5) AND the model_catalog_json evaluation result ("the real file has NO model_catalog_json key; models_cache.json is the correct target; model_catalog_json is not used by this Codex version" — D-98 evaluation mandate).
    - `GLM_52_ENTRY: dict` — the glm-5.2 model entry, with fields sourced from the REAL observed schema (slug='glm-5.2', display_name='GLM-5.2', default_reasoning_level='xhigh' — the Z.ai default the helper writes, supported_reasoning_levels with effort low/medium/high/xhigh, context_window and max_context_window documented as best-effort/unknown values per D-98 caveat). The entry mirrors the gpt-5.5 shape exactly (same key set) so Codex accepts it.
    - `update_models_cache(paths) -> None`: reads paths.models_cache via JsonBackend, calls `JsonBackend(paths).write_canonical({"models": [GLM_52_ENTRY]})` (Task 1's list-aware merge handles the preserve-existing + replace-or-append). Idempotent by composition (Task 1's merge is idempotent).
    - # setup wiring (D-98: setup-integration)
    - `run_setup` gains a new step (call it STEP 6.5, after the provider-apply at line ~253, before the LaunchAgent offer at line ~258): call `update_models_cache(paths)`. In `dry_run` mode, the step prints a diff via Plan 01's `diff_preview.compute_diff(paths.models_cache, would_be_json)` (hard import — Plan 01 ships first in Wave 1).
    - The setup summary (step 8) gains a line: "models_cache.json: glm-5.2 entry added/refreshed." (or the dry-run variant).
    - # tests (append to test_models_cache.py)
    - Test 7 (update_models_cache preserves + adds): seed paths.models_cache with the fixture (5 models); call `update_models_cache(paths)`; re-read; assert 6 models, glm-5.2 present, the 5 originals survive.
    - Test 8 (update_models_cache idempotent): call twice; assert byte-identical + exactly one glm-5.2.
    - Test 9 (setup wires it in): invoke `run_setup(paths, yes=True, environ={"ZAI_API_KEY": "..."})` with mocked build_fn; assert after setup, paths.models_cache has the glm-5.2 entry AND the user's pre-existing entries survive. (Seed paths.models_cache with the fixture first.)
    - Test 10 (setup dry-run summary for models_cache): invoke run_setup with dry_run=True; assert zero mutation of paths.models_cache (byte-identical to seed) AND the captured stdout contains a unified-diff header mentioning glm-5.2 (Plan 01's diff_preview.compute_diff is available — Plan 02 runs in Wave 2).
  </behavior>
  <action>
    Create `src/zai_codex_helper/services/models_cache.py` with the SPIKE-DOCUMENTED module docstring (the real schema verbatim + the model_catalog_json evaluation result), the `GLM_52_ENTRY` constant (fields sourced from the real observed gpt-5.5 shape — do NOT guess; where glm-5.2-specific values like context_window are unknown, set a documented best-effort value and note it in the entry's comment per D-98), and `update_models_cache(paths)`. Wire a new step into `run_setup` (after provider-apply, before LaunchAgent offer) that calls `update_models_cache`; in dry-run, emit the diff via `diff_preview.compute_diff(paths.models_cache, would_be_json)` (Plan 01's helper — hard import, Plan 02 runs after Plan 01). Add the summary line to step 8. Add Tests 7–10 to `tests/test_models_cache.py`. The GLM_52_ENTRY dict MUST mirror the real gpt-5.5 entry's key set (slug, display_name, description, default_reasoning_level, supported_reasoning_levels, shell_type, visibility, supported_in_api, priority, context_window, max_context_window, and the other observed fields) — use the real gpt-5.5 entry as the structural template and substitute glm-5.2 values. Per D-100: NO new CLI command (models_cache is wired INTO setup, not a new subcommand).
  </action>
  <verify>
    <automated>set -o pipefail; python -m pytest tests/test_models_cache.py tests/test_setup.py -v -m "not e2e"</automated>
  </verify>
  <done>
    - `src/zai_codex_helper/services/models_cache.py` exists and its module docstring documents the REAL schema verbatim (fetched_at/etag/client_version/models-list shape, entry fields) + the model_catalog_json evaluation result.
    - `update_models_cache(paths)` adds glm-5.2 to a 5-model seed → 6 models, the 5 originals survive, idempotent on double-call.
    - `run_setup` calls `update_models_cache` as a new step; after a full `setup` run, paths.models_cache has glm-5.2 + the user's entries.
    - In dry-run, the models_cache step does NOT mutate the file (byte-identical) and prints a unified diff via Plan 01's compute_diff.
    - The Phase 12 setup tests still pass (the new step is non-disruptive — it operates on models_cache, not on the yml/config.toml/shell paths the existing tests assert against).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User's `models_cache.json` (5 existing models) → tool's merge | The list-aware merge crosses from the user's hand-curated cache into the tool's mutation. A wholesale list-overwrite would DESTROY the user's 5 models (data loss). The list-aware merge (match-by-slug, preserve-existing) is the load-bearing safety property. |
| Tool's `GLM_52_ENTRY` guess → Codex's expectation | The entry's field values (context_window, etc.) for glm-5.2 are partially best-effort (the real file had no glm-5.2 to observe). If Codex's validator rejects the entry, the warning may persist. This is the D-98 documented caveat, NOT a security threat. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-15-06 | Tampering / Data Loss | `JsonBackend.write_canonical` wholesale-list-overwrite of `models` | high | mitigate | `merge_model_list` (Task 1) replaces-by-slug and appends; existing entries survive by construction; Tests 1–3 prove the 5-model seed yields 6 (not 1) models. The pre-Task-1 `deep_merge` path would have CLOBBERED — this plan is the fix. |
| T-15-07 | Tampering | top-level `fetched_at`/`etag`/`client_version` corruption | low | mitigate | The list-aware override is SURGICAL: only the `models` key is rerouted; deep_merge still handles the other keys, preserving the cache's provenance metadata. Test 4 asserts byte-identical top-level keys. |
| T-15-08 | Information Disclosure | `GLM_52_ENTRY` containing a hardcoded secret | n/a | accept | The entry is model METADATA (slug, display_name, context_window); it contains NO API key (the key lives in moonbridge-zai.yml, never models_cache.json — confirmed in json_backend.py docstring line 41-47). No SECR-03 surface. |
| T-15-SC | Tampering | (no package-manager installs — stdlib json + difflib only) | n/a | accept | No `pip install` of new runtime deps in this plan. Skipped SC row per planner-authority limits (no new packages). |
</threat_model>

<verification>
- `python -m pytest tests/test_models_cache.py tests/test_json_backend.py tests/test_setup.py -v -m "not e2e"` — the list-aware merge + setup wiring all pass.
- Manual confirmation against the REAL file: `python -c "from zai_codex_helper.services.models_cache import GLM_52_ENTRY, update_models_cache; ..."` against a COPY of the real `~/.codex/models_cache.json` in a sandbox (NOT the real file) yields 6 models (the 5 originals + glm-5.2), proving the merge is non-clobbering on the actual schema.
- The module docstring of `services/models_cache.py` contains the literal schema documentation (the spike deliverable — D-98's "SPIKE FIRST: read the REAL schema").
</verification>

<success_criteria>
- **SC-4 (SEC-02, D-98):** The models_cache.json update is implemented AFTER verifying the real schema (the spike doc IS the proof). The glm-5.2 entry is merged via JsonBackend using a LIST-AWARE, idempotent, non-clobbering merge (the user's existing entries survive). `model_catalog_json` was evaluated and documented as not-used (the real file has no such key).
- The real schema is documented verbatim in `services/models_cache.py` (the spike deliverable).
- Double-run is byte-identical (idempotent); the user's 5 models survive a glm-5.2 update.
- The update is wired into `setup` (D-98: setup-integration is cleaner) and is dry-run-safe.
- D-100 honored: no new CLI command (wired into setup), no PyPI publish.
</success_criteria>

<output>
Create `.planning/phases/15-polish-release-hardening/15-02-SUMMARY.md` when done.
</output>
