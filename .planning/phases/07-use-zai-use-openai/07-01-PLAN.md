---
phase: 07-use-zai-use-openai
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/zai_codex_helper/cli/parser.py
  - tests/test_use_zai_use_openai.py
autonomous: true
requirements:
  - PROV-01
  - PROV-02
  - PROV-04
  - CONF-06

must_haves:
  truths:
    # SC-1 (PROV-01) — after `use zai`, ON DISK
    - "After `main(['use','zai'])` against a real seeded config.toml under tmp HOME, reading config.toml from disk yields model='glm-5.2', model_provider='zai-moonbridge', model_reasoning_effort='xhigh' (D-45, D-39)."
    # SC-2 (PROV-02) — after `use openai`, revert + Z.ai block survives
    - "After `main(['use','openai'])` against a config.toml that has the zai-moonbridge block, reading config.toml from disk yields model='gpt-5.5', NO model_provider key, AND the [model_providers.zai-moonbridge] block is still present (reversible — D-40)."
    # SC-3 (PROV-04) — hard-to-miss restart warning on stderr
    - "Every successful `use zai` / `use openai` write is followed by a restart warning on STDERR (not stdout) that conveys: config written, Codex Desktop App does NOT live-reload config.toml, restart required; plain text + ANSI, no Rich (D-47)."
    # SC-4 (CONF-06) — idempotence byte-identical on double-write
    - "Running `main(['use','zai'])` twice produces byte-identical config.toml on disk (no duplicate [model_providers.zai-moonbridge] blocks accumulate); same for `use openai` (D-48)."
    # D-45 / D-31 handler contract
    - "_handle_use_zai and _handle_use_openai follow the D-31 restore-handler pattern: resolve Paths.default(), do the work, let ZaiCodexHelperError propagate, return 0 on success; they do NOT catch ZaiCodexHelperError, do NOT call sys.exit."
    # D-11 error contract end-to-end
    - "A postcondition violation (check_postconditions raising ZaiCodexHelperError) surfaces through main() as one-line `error: <msg>` on stderr + exit 1, no traceback unless --debug."
  artifacts:
    - "src/zai_codex_helper/cli/parser.py defines _handle_use_zai(args)->int and _handle_use_openai(args)->int, plus a shared _apply_provider_pipeline(...) helper and a _emit_restart_warning(stream) helper."
    - "tests/test_use_zai_use_openai.py with @pytest.mark.integration on-disk end-to-end tests pinning all 4 SCs + @pytest.mark.unit handler-dispatch tests."
  key_links:
    - "parser.py `use zai` / `use openai` sub-subs are re-wired from `_stub(...)` to `set_defaults(func=_handle_use_zai)` / `set_defaults(func=_handle_use_openai)` (D-03 — Phase 1 stubs become real)."
    - "_apply_provider_pipeline calls: TomlBackend(Paths.default()) -> seed-if-missing -> backend.backup_once() -> backend.read() -> apply_zai|apply_openai -> backend.write_canonical(doc) -> check_postconditions(doc) -> _emit_restart_warning (D-45 order)."
    - "Restart warning is written to sys.stderr so it is visible even when stdout is piped (D-47)."
---

<objective>
Wire the two commands that ARE the product: `zai-codex-helper use zai` makes Z.ai
(`glm-5.2`, `xhigh`) the Codex default in `~/.codex/config.toml`, and
`zai-codex-helper use openai` reverts to OpenAI (`gpt-5.5`) — end-to-end, on disk,
with a hard-to-miss restart warning. This is the Core Value, the apex phase that
glues Phases 2–6 into one write pipeline.

Purpose: Deliver PROV-01 (use zai), PROV-02 (use openai reversible), PROV-04
(restart warning), and CONF-06 (byte-identical idempotence) — the four ROADMAP
Phase 7 Success Criteria. After this plan, the product works for real against a
real `~/.codex/config.toml`.

Output:
- `_handle_use_zai` / `_handle_use_openai` real CLI handlers in
  `src/zai_codex_helper/cli/parser.py`, replacing the Phase 1 `_stub(...)` wiring
  (D-03).
- A shared `_apply_provider_pipeline` helper (the D-45 end-to-end write path) and
  a `_emit_restart_warning` helper (D-47), both in `parser.py`.
- `tests/test_use_zai_use_openai.py` — on-disk integration tests (write a REAL
  fixture to `tmp_path/.codex/config.toml`, drive `main(['use','zai'])`, read the
  file back, assert the on-disk state) + handler-dispatch unit tests.
</objective>

<execution_context>
@/Users/axisrow/Projects/zai-codex-helper/.claude/gsd-core/workflows/execute-plan.md
@/Users/axisrow/Projects/zai-codex-helper/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-use-zai-use-openai/07-CONTEXT.md

# Prior phase decisions this phase GLUES (read the actual source, not just the CONTEXT)
@src/zai_codex_helper/cli/parser.py
@src/zai_codex_helper/__main__.py
@src/zai_codex_helper/backends/toml.py
@src/zai_codex_helper/backends/base.py
@src/zai_codex_helper/backends/_backup.py
@src/zai_codex_helper/services/providers.py
@src/zai_codex_helper/services/paths.py
@src/zai_codex_helper/errors.py
@tests/conftest.py
@tests/test_restore.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wire `use zai` / `use openai` handlers with the shared D-45 pipeline + D-47 restart warning</name>
  <files>src/zai_codex_helper/cli/parser.py</files>

  <behavior>
    - `build_parser().parse_args(['use','zai']).func` is `_handle_use_zai` (NOT a stub closure named `handler`); same for `use openai` → `_handle_use_openai`.
    - `main(['use','zai'])` against an OpenAI-default seed under tmp HOME writes config.toml on disk with `model='glm-5.2'`, `model_provider='zai-moonbridge'`, `model_reasoning_effort='xhigh'`, and a `[model_providers.zai-moonbridge]` block with `base_url='http://127.0.0.1:38440/v1'` and `wire_api='responses'`; returns 0.
    - `main(['use','openai'])` against a Z.ai-default seed writes config.toml on disk with `model='gpt-5.5'`, NO `model_provider` key, AND the `[model_providers.zai-moonbridge]` block still present; returns 0.
    - On a FRESH install (no config.toml under HOME), `main(['use','zai'])` succeeds (seeds the file via the transform — D-45 step 3 "prefer seeding"), does NOT raise `no config to back up`.
    - Every successful write emits exactly one restart-warning block on STDERR (assertable via `capsys.readouterr().err`), and NOTHING of the warning on stdout.
    - The handlers do NOT catch `ZaiCodexHelperError`, do NOT call `sys.exit`; a forced postcondition violation propagates as `ZaiCodexHelperError` from the handler (main() formats it).
  </behavior>

  <read_first>
  - `.planning/phases/07-use-zai-use-openai/07-CONTEXT.md` — D-45 (pipeline order), D-46 (Paths.default + monkeypatchable seam), D-47 (stderr warning, plain text + ANSI), D-48 (idempotence tested end-to-end), D-49 (scope discipline — ONLY use zai/use openai + warning + tests).
  - `src/zai_codex_helper/cli/parser.py` — the existing `_handle_restore(args)->int` (D-31) is the EXACT template to copy: lazy imports of BackupCoordinator + Paths inside the handler body, `Paths.default()`, let `ZaiCodexHelperError` propagate, return 0, print a one-line confirmation. The `build_parser()` `use zai` / `use openai` sub-subs are currently `.set_defaults(func=_stub("use zai"))` / `_stub("use openai")` — swap these two lines for the real handlers.
  - `src/zai_codex_helper/__main__.py` — `main()` calls `args.func(args)` inside `try/except ZaiCodexHelperError` (D-11). Confirms handlers must NOT catch/exit themselves.
  - `src/zai_codex_helper/backends/toml.py` — `TomlBackend(paths)` binds `paths.config_toml`; `.read()` returns a live `tomlkit.TOMLDocument` (raises `FileNotFoundError` if missing — this drives the seed-if-missing decision); `.write_canonical(doc)` serializes via `tomlkit.dumps` and routes through `atomic_write`; `.backup_once()` is inherited from the ABC.
  - `src/zai_codex_helper/backends/_backup.py` — CRITICAL: `BackupCoordinator.backup_once` RAISES `ZaiCodexHelperError("no config to back up")` when `backend.path` does NOT exist (line ~112). This means on a fresh install the pipeline MUST seed config.toml BEFORE calling `backend.backup_once()`, or `use zai` on a clean machine errors out. The sentinel makes `backup_once` a no-op after the first run, so it is safe to call every time AFTER the seed-if-missing step.
  - `src/zai_codex_helper/services/providers.py` — `apply_zai(doc)->doc`, `apply_openai(doc)->doc`, `check_postconditions(doc)->None` (raises `ZaiCodexHelperError` on violation). Constants `ZAI_MODEL='glm-5.2'`, `ZAI_PROVIDER_ID='zai-moonbridge'`, `ZAI_REASONING_EFFORT='xhigh'`, `OPENAI_MODEL='gpt-5.5'`.
  - `src/zai_codex_helper/services/paths.py` — `Paths.default()` = `from_home(Path.home())` (D-23). Tests rely on the autouse `_isolate_home` fixture (conftest.py) pointing `HOME` at `tmp_path`, so `Path.home()` resolves under the sandbox.
  - `tests/test_restore.py` — the dispatch + on-disk test idiom to mirror (drive `main([...])`, seed `tmp_path/.codex/config.toml`, assert bytes on disk).
  </read_first>

  <action>
  Implement three module-level callables in `src/zai_codex_helper/cli/parser.py`, following the `_handle_restore` (D-31) pattern verbatim (lazy imports inside the body, `Paths.default()`, no catch / no sys.exit, return int). Do NOT touch any other module — everything this phase needs already exists (TomlBackend Phase 5, apply_zai/apply_openai/check_postconditions Phase 6, BackupCoordinator Phase 4, Paths Phase 2, ZaiCodexHelperError Phase 1).

  1. `_emit_restart_warning(stream)` helper (D-47, PROV-04): writes a HARD-TO-MISS restart warning to the given stream. The caller passes `sys.stderr` (so the warning is visible even when stdout is piped — D-47 "goes to stderr"). Plain text + ANSI (per CLAUDE.md D-04/D-05, no Rich). The warning text MUST convey three things: (a) the config was written, (b) the Codex Desktop App does NOT live-reload `config.toml`, (c) a restart is required for the new default to take effect. Make it impossible to miss: a leading warning glyph and/or an UPPERCASE prefix, then the explanation. Convey that the `codex` CLI picks up the change on its NEXT invocation (no restart needed for the CLI) but the Codex Desktop App needs a full restart. Keep it to a small fixed number of lines. Take the stream as a parameter (NOT a hard-coded `sys.stderr` inside the helper) so a test can capture it via `capsys` or pass a fake stream — this is the testability seam.

  2. `_apply_provider_pipeline(transform, warn_stream)` shared helper (D-45 — the end-to-end write path): takes a pure transform callable (`apply_zai` or `apply_openai`) and a stream for the warning, and runs the full pipeline against the REAL `~/.codex/config.toml` resolved via `Paths.default()`:
     a. `paths = Paths.default()` (D-46 — production entry point; tests inject via the autouse `_isolate_home` fixture which repoints `HOME`, exactly like `_handle_restore`).
     b. `backend = TomlBackend(paths)`.
     c. SEED-IF-MISSING (D-45 step 3, "prefer seeding an empty tomlkit doc so the transform populates it"): if `not backend.exists()`, write a fresh empty `tomlkit.document()` via `backend.write_canonical(tomlkit.document())`. This MUST happen BEFORE `backup_once`, because `BackupCoordinator.backup_once` raises `ZaiCodexHelperError("no config to back up")` when the source is absent — without this seed, `use zai` on a fresh install errors out instead of creating the config. (Confirm this by reading `backends/_backup.py` line ~112.)
     d. `backend.backup_once()` (D-45 step 2 — one-shot `.bak` via the sentinel-gated BackupCoordinator; no-op after the first run, safe to call every time).
     e. `doc = backend.read()` (D-45 step 4).
     f. `doc = transform(doc)` (D-45 step 5 — `apply_zai` or `apply_openai`; pure).
     g. `backend.write_canonical(doc)` (D-45 step 6 — atomic, crash-safe via Phase 3).
     h. `check_postconditions(doc)` (D-45 step 7 — raises `ZaiCodexHelperError` on violation; let it propagate to `main()` per D-11; run AFTER write so it validates the post-write state — the transform is pure and write_canonical is faithful, so the in-memory doc equals the on-disk doc).
     i. `_emit_restart_warning(warn_stream)` (D-45 step 8, D-47).
     Return None (the caller returns 0).
     Do NOT wrap the pipeline in `try/except ZaiCodexHelperError` — D-11/D-45: the handler does not catch. Do NOT call `sys.exit`.

  3. Two thin handlers that delegate to the shared pipeline (D-31 shape):
     - `_handle_use_zai(args: argparse.Namespace) -> int`: lazy-import `apply_zai`, call `_apply_provider_pipeline(apply_zai, sys.stderr)`, return 0.
     - `_handle_use_openai(args: argparse.Namespace) -> int`: lazy-import `apply_openai`, call `_apply_provider_pipeline(apply_openai, sys.stderr)`, return 0.
     (Lazy imports of the transform inside the handler body, matching `_handle_restore`'s lazy-import discipline so `parser.py` stays import-light and side-effect-free at module load.)

  4. Re-wire `build_parser()`: replace the two `.set_defaults(func=_stub("use zai"))` / `.set_defaults(func=_stub("use openai"))` calls on the `use zai` / `use openai` sub-subs with `.set_defaults(func=_handle_use_zai)` / `.set_defaults(func=_handle_use_openai)` (D-03). Leave the `restore` wiring and all other stubs untouched (D-49 — scope discipline: only `use`).

  Module-level imports: `import sys` is already present at the top of `parser.py`. Add `import tomlkit` lazily inside `_apply_provider_pipeline` (only needed for the seed-if-missing branch), consistent with the lazy-import style already used by `_handle_restore`. Do NOT add Rich or any new dependency (CLAUDE.md D-04/D-05).

  Naming: name the handlers `_handle_use_zai` / `_handle_use_openai` and the helpers `_apply_provider_pipeline` / `_emit_restart_warning` exactly as written — Task 2's tests assert these names (the `_handle_restore` test already asserts `args.func.__name__ == "_handle_restore"`; the new tests assert `"use zai"`/`"use openai"` dispatch to real named handlers, not stub closures).
  </action>

  <verify>
    <automated>cd /Users/axisrow/Projects/zai-codex-helper && python -m pytest tests/test_use_zai_use_openai.py -m "not e2e" -x -q</automated>
  </verify>

  <done>
  - `build_parser().parse_args(['use','zai']).func.__name__ == '_handle_use_zai'` and `['use','openai']` → `_handle_use_openai` (NOT the stub closure `handler`).
  - `_handle_use_zai` / `_handle_use_openai` follow the D-31 shape: resolve `Paths.default()`, delegate to `_apply_provider_pipeline`, return 0, do NOT catch `ZaiCodexHelperError`, do NOT call `sys.exit`.
  - The shared pipeline runs the D-45 order: seed-if-missing → backup_once → read → transform → write_canonical → check_postconditions → restart warning.
  - Seed-if-missing runs BEFORE `backup_once` (otherwise `use zai` on a fresh install raises `no config to back up`).
  - Restart warning goes to `sys.stderr`, is hard-to-miss, conveys the three D-47 facts, uses plain text + ANSI (no Rich).
  - No new runtime dependency added; no changes outside `parser.py`.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: On-disk integration tests pinning all 4 ROADMAP SCs + D-11 + handler-dispatch unit tests</name>
  <files>tests/test_use_zai_use_openai.py</files>

  <behavior>
    - SC-1 (PROV-01): seed an OpenAI-default config.toml into `tmp_path/.codex`, run `main(['use','zai'])`, READ the file back from disk, assert `model=='glm-5.2'`, `model_provider=='zai-moonbridge'`, `model_reasoning_effort=='xhigh'`, and the `[model_providers.zai-moonbridge]` block has `wire_api=='responses'` + the Moon Bridge base_url.
    - SC-2 (PROV-02): seed a Z.ai-default config.toml, run `main(['use','openai'])`, read back, assert `model=='gpt-5.5'`, that `model_provider` is ABSENT (use `assert 'model_provider' not in doc`), AND that the `[model_providers.zai-moonbridge]` block is STILL present (reversible).
    - SC-3 (PROV-04): after `main(['use','zai'])`, `capsys.readouterr().err` contains the restart warning (assert the substrings for "written", the Desktop-App-no-live-reload fact, and "restart"); assert the warning is NOT on stdout.
    - SC-4 (CONF-06, D-48): run `main(['use','zai'])` twice; snapshot config.toml bytes after each; assert byte-identical (`first_bytes == second_bytes`). Repeat for `use openai`. Also assert no duplicate `[model_providers.zai-moonbridge]` blocks accumulate (count occurrences of the header in the raw text == 1).
    - Fresh-install (D-45 step 3): with NO config.toml under `tmp_path/.codex`, `main(['use','zai'])` returns 0 and creates config.toml with the Z.ai default (does not raise `no config to back up`).
    - D-11 end-to-end: force a postcondition violation (e.g. seed a config that redefines a reserved provider id, then run `use zai` — `check_postconditions` raises) and assert `main()` returns 1 with one-line `error: ...` on stderr, no traceback; under `--debug` it re-raises.
    - Handler-dispatch (unit): `build_parser().parse_args(['use','zai']).func.__name__ == '_handle_use_zai'`; same for openai; both are real named handlers, not stub closures.
    - Comments + `[project_*]` trust blocks survive the write (seed a fixture with a comment and a `[project_*]` block, run `use zai`, read back, assert both survive — this is the CLAUDE.md load-bearing tomlkit guarantee exercised end-to-end through the real pipeline).
  </behavior>

  <read_first>
  - `tests/test_restore.py` — the test idiom to mirror EXACTLY: import `main` and `ZaiCodexHelperError` from `zai_codex_helper.__main__`; import `build_parser` from `zai_codex_helper.cli.parser`; a module-level `_write(path, data)` helper that does `path.parent.mkdir(parents=True, exist_ok=True); path.write_bytes(data)`; seed files directly under `tmp_path / ".codex"`; drive the CLI via `main([...])`; assert return code + on-disk bytes + `capsys.readouterr()`.
  - `tests/test_providers.py` lines 1-60 — the REALISTIC fixture-string style: a `REALISTIC_OPENAI_DEFAULT` / `REALISTIC_ZAI_DEFAULT` TOML string constant seeded via `tomlkit.parse(...)` or written to disk. Reuse the same canonical shape (flat `model` / `model_provider` / `model_reasoning_effort` keys; `[model_providers.zai-moonbridge]` with `wire_api="responses"`).
  - `tests/conftest.py` — the autouse `_isolate_home(tmp_path, monkeypatch)` fixture repoints `HOME` to `tmp_path` AND pre-creates `tmp_path/.codex`. This is why `Paths.default()` (which calls `Path.home()`) resolves under the sandbox in every test, with zero opt-in. Do NOT re-isolate; just request `tmp_path` (or rely on autouse) and seed under `tmp_path / ".codex" / "config.toml"`.
  - `.planning/phases/07-use-zai-use-openai/07-CONTEXT.md` "specifics" — the integration test MUST write a REAL fixture (comments + trust blocks) into `tmp_path/.codex`, run `use zai` via `main(["use","zai"])`, then read the file back and assert on-disk state (proving the end-to-end write, not just the in-memory transform). Idempotence test = write, snapshot bytes, write again, assert byte-identical.
  - `src/zai_codex_helper/services/providers.py` — the canonical constants to assert against (`ZAI_MODEL`, `ZAI_PROVIDER_ID`, `ZAI_REASONING_EFFORT`, `OPENAI_MODEL`, `ZAI_PROVIDER_BLOCK`). Import them so the tests assert against the single source of truth, not magic strings.
  - `pyproject.toml` `[tool.pytest.ini_options]` — markers are `unit` / `integration` / `smoke` / `e2e`; `addopts` already includes `-m "not e2e"`. Mark the on-disk tests `@pytest.mark.integration` (they write to tmp HOME) and the pure dispatch/parse tests `@pytest.mark.unit`.
  </read_first>

  <action>
  Create `tests/test_use_zai_use_openai.py` mirroring the `tests/test_restore.py` style (`from __future__ import annotations`, pytest import, `main` + `ZaiCodexHelperError` from `zai_codex_helper.__main__`, `build_parser` from `zai_codex_helper.cli.parser`, a module-level `_write(path, data)` helper). Import the canonical constants from `zai_codex_helper.services.providers` so assertions use the single source of truth.

  Module-level fixture strings (REALISTIC seeds with comments + a `[project_*]` trust block — the load-bearing tomlkit round-trip surface):
  - A `REALISTIC_OPENAI_DEFAULT` TOML string: a top-level comment, `model = "gpt-5.5"`, NO `model_provider`, NO `[model_providers.*]`, and a `[project_some-trust-hash]` block with a comment inside it.
  - A `REALISTIC_ZAI_DEFAULT` TOML string: the OpenAI seed AFTER `use zai` — `model = "glm-5.2"`, `model_provider = "zai-moonbridge"`, `model_reasoning_effort = "xhigh"`, a `[model_providers.zai-moonbridge]` block with `base_url`/`wire_api="responses"`/`env_key`, the same comment + `[project_*]` trust block.

  Tests (all run under the autouse `_isolate_home` fixture — request `tmp_path` and seed into `tmp_path / ".codex" / "config.toml"`):

  @pytest.mark.integration (on-disk end-to-end — the highest-signal tests):
  - `test_use_zai_makes_zai_default_on_disk_sc1`: seed `REALISTIC_OPENAI_DEFAULT` to disk, `rc = main(["use","zai"])`, assert `rc == 0`, then READ `config.toml` back from disk via `tomlkit.parse(...)` and assert `doc["model"] == ZAI_MODEL`, `doc["model_provider"] == ZAI_PROVIDER_ID`, `doc["model_reasoning_effort"] == ZAI_REASONING_EFFORT`, and `doc["model_providers"][ZAI_PROVIDER_ID]["wire_api"] == "responses"` (PROV-03 load-bearing), and the `base_url` matches `ZAI_PROVIDER_BLOCK["base_url"]`. This pins SC-1 / PROV-01 against the REAL on-disk file, not an in-memory transform.
  - `test_use_openai_reverts_and_preserves_zai_block_sc2`: seed `REALISTIC_ZAI_DEFAULT`, `rc = main(["use","openai"])`, assert `rc == 0`, read back, assert `doc["model"] == OPENAI_MODEL`, `assert "model_provider" not in doc` (key removed, Codex falls back to builtin OpenAI), AND `assert ZAI_PROVIDER_ID in doc["model_providers"]` (block survives — reversible). Pins SC-2 / PROV-02.
  - `test_use_then_round_trip_zai_openai_zai`: seed OpenAI default, `use zai` → assert Z.ai state on disk, `use openai` → assert OpenAI state + zai block survives, `use zai` again → assert Z.ai state on disk. Pins reversibility end-to-end.
  - `test_restart_warning_on_stderr_after_write_sc3`: seed OpenAI default, `main(["use","zai"])`, `out, err = capsys.readouterr()`, assert the warning's three D-47 facts are substrings of `err` (config written; Desktop App does not live-reload; restart required), and assert the warning is NOT in `out` (stdout). Repeat the stderr assertion once for `use openai`.
  - `test_use_zai_twice_byte_identical_sc4`: seed OpenAI default, `main(["use","zai"])`, `first = config_toml.read_bytes()`, `main(["use","zai"])`, `second = config_toml.read_bytes()`, `assert first == second`. Also assert the raw text has exactly ONE `[model_providers.zai-moonbridge]` header (count via `text.count("[model_providers.zai-moonbridge]") == 1`). Repeat the byte-identical assertion for `use openai`. Pins SC-4 / CONF-06 / D-48.
  - `test_use_zai_seeds_missing_config_then_writes`: with NO config.toml present under `tmp_path/.codex`, `rc = main(["use","zai"])`, assert `rc == 0` and the file now exists with the Z.ai default on disk (D-45 step 3 — seeding; must NOT raise `no config to back up`). Optionally assert a sibling `.bak` was created on this first run (the sentinel-gated one-shot fired against the freshly-seeded empty doc).
  - `test_comments_and_trust_block_survive_use_zai`: seed `REALISTIC_OPENAI_DEFAULT` (with its comment + `[project_*]` block), `main(["use","zai"])`, read back the RAW text, assert the original comment line and the `[project_*]` block header are still present (CLAUDE.md load-bearing tomlkit guarantee, exercised through the REAL pipeline — not a Phase 5 unit test).

  @pytest.mark.integration (D-11 error contract end-to-end through the new handlers):
  - `test_postcondition_violation_surfaces_via_main_d11`: seed a config that triggers `check_postconditions` to raise — the cleanest is a doc that redefines a reserved provider id (e.g. add a `[model_providers.openai]` block — `RESERVED_PROVIDER_IDS` catches it). `rc = main(["use","zai"])`, assert `rc == 1`, `out, err = capsys.readouterr()`, assert exactly one non-empty stderr line starting with `error:`, and `"Traceback" not in err`. (If the reserved-id block is preserved through `apply_zai`, `check_postconditions` raises on the post-write doc; if not, pick another forced violation — the point is to prove the handler does not catch and `main()` formats it.)
  - `test_debug_reraises_postcondition_violation`: same seed, `with pytest.raises(ZaiCodexHelperError): main(["--debug","use","zai"])`.

  @pytest.mark.unit (dispatch — no disk IO beyond parse):
  - `test_use_zai_is_real_handler_not_stub`: `args = build_parser().parse_args(["use","zai"])`, `assert args.cmd == "use"`, `assert args.provider == "zai"`, `assert args.func.__name__ == "_handle_use_zai"` (a stub closure is named `handler`; the real handler is a named function).
  - `test_use_openai_is_real_handler_not_stub`: same for `["use","openai"]` → `args.func.__name__ == "_handle_use_openai"`.
  - `test_use_help_exits_zero`: `with pytest.raises(SystemExit) as exc: build_parser().parse_args(["use","--help"])`, `assert exc.value.code == 0`; and top-level `--help` exit 0 lists the `use` command.

  Use `capsys` (built-in) for stdout/stderr capture and `tmp_path` for disk. Do NOT monkeypatch `Paths.default` — the autouse `_isolate_home` fixture already makes `Paths.default()` resolve under `tmp_path` (that is the D-46 "monkeypatchable seam": the production handler calls `Paths.default()` → `Path.home()` → the sandboxed `HOME`). Do NOT add any new conftest fixture.
  </action>

  <verify>
    <automated>cd /Users/axisrow/Projects/zai-codex-helper && python -m pytest tests/test_use_zai_use_openai.py -m "not e2e" -q && python -m pytest -m "not e2e" -q</automated>
  </verify>

  <done>
  - `tests/test_use_zai_use_openai.py` exists and all its tests pass under `-m "not e2e"`.
  - The four ROADMAP Phase 7 SCs are each pinned by at least one ON-DISK integration test (read config.toml back from disk after `main([...])` and assert the real file's state — NOT just the in-memory transform).
  - SC-4 idempotence is proven by a byte-identical double-write assertion (not by reasoning about upsert semantics).
  - The restart warning (SC-3 / PROV-04) is asserted present on STDERR and absent on STDOUT.
  - D-11 is proven end-to-end: a forced postcondition violation → `main()` returns 1 with one-line `error:` on stderr, no traceback; `--debug` re-raises.
  - Fresh-install seeding (D-45 step 3) is proven: no config.toml present → `use zai` creates it and returns 0.
  - Comments + `[project_*]` trust blocks survive the real write (load-bearing tomlkit guarantee).
  - The full suite (`pytest -m "not e2e"`) still passes — no regression to Phases 1–6.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user filesystem (`~/.codex/config.toml`) | The handler mutates the user's real config; a bug here corrupts state the user trusts. |
| CLI args → handler dispatch | `use zai` / `use openai` argv crosses into a handler that performs disk writes. |
| tomlkit document → on-disk file | The pure transform's output is serialized and written via atomic_write; a non-idempotent write accumulates duplicate blocks. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-01 | Tampering | `_apply_provider_pipeline` write path | high | mitigate | Every write routes through `TomlBackend.write_canonical` → `_write_via_atomic` → `atomic_write` (Phase 3, CONF-01); the post-write `check_postconditions` (Phase 6) raises on any malformed provider state, so a corrupted config is surfaced, not silently trusted. No new write path is introduced in this phase. |
| T-07-02 | Tampering | `Paths.default()` resolution | medium | mitigate | The handler resolves paths ONLY via `Paths.default()` (D-46/D-23) — never a hard-coded `~/.codex` literal. The autouse `_isolate_home` fixture (D-14) makes tests prove no real-HOME write occurs. This matches the audited `_handle_restore` (D-31) pattern. |
| T-07-03 | Denial of Service | `_apply_provider_pipeline` on missing config | medium | mitigate | `BackupCoordinator.backup_once` raises `no config to back up` when the source is absent; the pipeline MUST seed-if-missing BEFORE calling `backup_once` (D-45 step 3), so `use zai` on a fresh install succeeds instead of erroring. Tested by `test_use_zai_seeds_missing_config_then_writes`. |
| T-07-04 | Information Disclosure | `_emit_restart_warning` destination | low | accept | The warning text conveys only operational guidance (restart required) — no secrets, no API keys, no file contents. Routing to stderr (D-47) is a usability choice (visible when stdout is piped), not a disclosure risk. |
| T-07-05 | Repudiation | handler error propagation | low | mitigate | The handler does NOT catch `ZaiCodexHelperError` (D-11/D-45); `main()` formats it as a one-line `error: <msg>` + exit 1, and `--debug` re-raises the full traceback. A postcondition violation cannot be silently swallowed. Tested by `test_postcondition_violation_surfaces_via_main_d11`. |
| T-07-SC | Tampering | (no package-manager installs in this phase) | — | accept | This phase adds NO new runtime dependency (uses stdlib `sys`/`tomlkit`, both already in the project). No npm/pip/cargo install tasks; the package-legitimacy gate does not apply. |
</threat_model>

<verification>
Phase 7 is complete when, from the repo root:

1. `python -m pytest tests/test_use_zai_use_openai.py -m "not e2e" -q` is green (all SC + D-11 + dispatch tests pass).
2. `python -m pytest -m "not e2e" -q` is green (no regression to Phases 1–6 — the `restore` handler, TomlBackend, providers, paths, atomic_write, backup all still pass).
3. `ruff check src/zai_codex_helper/cli/parser.py tests/test_use_zai_use_openai.py` is clean (Phase 1 D-08 dev tool).
4. `python -c "from zai_codex_helper.cli.parser import build_parser; p=build_parser(); a=p.parse_args(['use','zai']); print(a.func.__name__)"` prints `_handle_use_zai` (NOT `handler` — the stub closure).
5. Manual end-to-end smoke (optional, against a throwaway HOME): `HOME=$(mktemp -d) python -m zai_codex_helper use zai && cat $HOME/.codex/config.toml` shows `model = "glm-5.2"`, `model_provider = "zai-moonbridge"`, `model_reasoning_effort = "xhigh"`, the `[model_providers.zai-moonbridge]` block, AND the restart warning printed to the terminal.
</verification>

<success_criteria>
- PROV-01: `use zai` writes `model="glm-5.2"` / `model_provider="zai-moonbridge"` / `model_reasoning_effort="xhigh"` to the REAL on-disk config.toml (proven by reading the file back, not by asserting on an in-memory doc).
- PROV-02: `use openai` reverts to `model="gpt-5.5"`, removes `model_provider`, AND preserves the `[model_providers.zai-moonbridge]` block (reversible — proven on disk).
- PROV-04: every successful write is followed by a hard-to-miss restart warning on STDERR conveying the three D-47 facts.
- CONF-06: `use zai` twice → byte-identical config.toml on disk (and `use openai` twice → byte-identical); no duplicate provider blocks accumulate.
- D-45: the pipeline order is seed-if-missing → backup_once → read → transform → write_canonical → check_postconditions → warning; handler does not catch / does not sys.exit.
- D-46: paths resolve via `Paths.default()`; tests inject via the autouse `_isolate_home` seam (no real-HOME write).
- D-47: warning on stderr, plain text + ANSI, no Rich.
- D-48: idempotence tested end-to-end (byte-identical double-write).
- D-49: ONLY `use zai`/`use openai` + warning + tests delivered; no setup/status/doctor/service/Moon-Bridge logic.
</success_criteria>

<output>
Create `.planning/phases/07-use-zai-use-openai/07-01-SUMMARY.md` when done.
</output>
