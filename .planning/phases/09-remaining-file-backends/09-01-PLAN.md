---
phase: 09-remaining-file-backends
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/zai_codex_helper/backends/yaml.py
  - tests/test_yaml_backend.py
autonomous: true
requirements: [SECR-02]
user_setup: []

must_haves:
  truths:
    - "YamlBackend.write_canonical produces a file whose stat mode is exactly 0o600 (the API key in ZAI_API_KEY never lands world-readable) — per D-56"
    - "YamlBackend routes every write through self._write_via_atomic (the ABC helper), never calls atomic_write directly — per D-29 structural delegation"
    - "YamlBackend uses yaml.safe_load and yaml.safe_dump only; no bare yaml.load / yaml.dump appears anywhere in the module — per D-61 (CLAUDE.md 'What NOT to Use')"
    - "The written YAML round-trips: read() after write_canonical returns an object equal to the input dict (safe_load(safe_dump(data)) == data for the canonical shape)"
    - "backup_once is inherited verbatim from ConfigBackend (D-30); YamlBackend does not override it"
  artifacts:
    - "src/zai_codex_helper/backends/yaml.py — YamlBackend class"
    - "tests/test_yaml_backend.py — @pytest.mark.unit tests pinning SC-1 (0600) and the safe_* discipline"
  key_links:
    - "YamlBackend.__init__ → ConfigBackend.__init__(paths, 'moonbridge_yml') binds the resolved Paths.moonbridge_yml"
    - "YamlBackend.write_canonical → self._write_via_atomic(dumped_str, 0o600) (explicit 0o600, NOT mode=None)"
---

<objective>
Deliver `YamlBackend`, the concrete `ConfigBackend` subclass for `~/.codex/moonbridge-zai.yml` (SC-1 of ROADMAP Phase 9; requirement SECR-02).

Purpose: `moonbridge-zai.yml` is the file that holds the user's `ZAI_API_KEY`. CLAUDE.md mandates that any file holding the key lands at `0600`. This backend is the single place that guarantee is enforced for the YAML file type. It is the SECR-02 requirement: "the key is stored in `~/.codex/moonbridge-zai.yml` with permissions `0600`." Phase 12 `setup` will call this backend; Phase 9 delivers only the backend + its proof.

Output: `src/zai_codex_helper/backends/yaml.py` (YamlBackend) + `tests/test_yaml_backend.py`.
</objective>

<execution_context>
@/Users/axisrow/Projects/zai-codex-helper/.claude/gsd-core/workflows/execute-plan.md
@/Users/axisrow/Projects/zai-codex-helper/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-remaining-file-backends/09-CONTEXT.md
@.planning/phases/05-tomlbackend/deferred-items.md

# Structural template (subclass the ABC exactly like this):
@src/zai_codex_helper/backends/base.py
@src/zai_codex_helper/backends/toml.py
@src/zai_codex_helper/backends/_atomic.py
@src/zai_codex_helper/services/paths.py

# Project library discipline:
@./.claude/CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: YamlBackend — yaml.safe_dump via _write_via_atomic at explicit 0o600</name>
  <files>src/zai_codex_helper/backends/yaml.py</files>

  <read_first>
    - src/zai_codex_helper/backends/base.py — the ConfigBackend ABC: __init__(paths, field), read()/exists()/write_canonical() abstract, _write_via_atomic(content, mode) private helper, backup_once() concrete+inherited (D-30).
    - src/zai_codex_helper/backends/toml.py — the EXACT structural template for a concrete backend: a class docstring citing the decisions, __init__ that calls super().__init__(paths, "<field_name>"), read() returning the parsed object, exists() one-liner, write_canonical() that serializes then calls self._write_via_atomic(serialized, mode). Mirror this structure line-for-line for YamlBackend.
    - src/zai_codex_helper/backends/_atomic.py — atomic_write(path, data, mode): the note that mode=None does NOT preserve (always yields 0o600 from tempfile) is why YamlBackend MUST pass an explicit 0o600 (the secrets path is safe because it is explicit, per D-56).
    - src/zai_codex_helper/services/paths.py — the field name to bind is `moonbridge_yml` (Paths.moonbridge_yml = home/.codex/moonbridge-zai.yml).
    - .planning/phases/09-remaining-file-backends/09-CONTEXT.md — D-56 (yaml.safe_dump args + 0o600 LOAD-BEARING), D-61 (yaml.safe_* only), D-62 (module path backends/yaml.py).
    - ./.claude/CLAUDE.md — "File Permissions" (moonbridge-zai.yml = 0600) and "What NOT to Use" (yaml.safe_* only; bare yaml.load/dump is a security risk).
  </read_first>

  <action>
    Create `src/zai_codex_helper/backends/yaml.py` defining `YamlBackend(ConfigBackend)` (per D-56).

    Module docstring: cite Phase 9, decisions D-56 (0600 LOAD-BEARING for the API key), D-61 (yaml.safe_* only), D-62 (location), and D-DEFERRED-01 awareness (why the explicit 0o600 is mandatory: atomic_write(mode=None) yields 0600 from the tempfile, so relying on mode=None is fragile; the secrets path uses the explicit arg so it is safe and self-documenting). Mirror the docstring discipline of `toml.py` (every load-bearing decision cited, every "What NOT to Use" alternative named-and-rejected).

    Library discipline (D-61): import `yaml` from PyYAML. The module MUST call `yaml.safe_load` and `yaml.safe_dump` ONLY. NEVER `yaml.load` or `yaml.dump` (arbitrary-object-construction risk; CLAUDE.md "What NOT to Use"). State this in the module docstring.

    Class `YamlBackend(ConfigBackend)`:
    - `__init__(self, paths: Paths) -> None`: call `super().__init__(paths, "moonbridge_yml")` (binds Paths.moonbridge_yml; mirror TomlBackend.__init__ exactly). The class hard-codes the field name; callers pass only the Paths instance.
    - `read(self) -> object`: return `yaml.safe_load(self._path.read_text(encoding="utf-8"))`. Returns the parsed YAML object (a dict for the canonical moonbridge-zai.yml shape). If the file does not exist, FileNotFoundError propagates (generic-backend contract; mirror TomlBackend.read's FileNotFoundError note).
    - `exists(self) -> bool`: return `self._path.exists()` (one-liner, mirror TomlBackend.exists).
    - `write_canonical(self, content: object, mode: int | None = 0o600) -> None`:
        * The DEFAULT for `mode` is `0o600` (NOT None). This is the D-56 LOAD-BEARING decision: moonbridge-zai.yml holds ZAI_API_KEY, so the default must already be the restricted mode. A caller MAY pass an explicit mode to override, but the default enforces the secret posture without relying on the caller to remember. State this in the docstring: "default 0o600 because this file holds the API key (D-56, SECR-02, CLAUDE.md File Permissions)."
        * Serialize: `serialized = yaml.safe_dump(content, sort_keys=False, default_flow_style=False, allow_unicode=True)`. These are the exact safe_dump args from CLAUDE.md and D-56. Encode considerations: safe_dump returns a str (utf-8 by default when allow_unicode=True), so pass the str to _write_via_atomic (which encodes str to utf-8 bytes — see _atomic.py).
        * Route through the ABC helper: `self._write_via_atomic(serialized, mode)`. NEVER call atomic_write directly (D-29 structural delegation). Mirror TomlBackend.write_canonical's routing comment.
        * Do NOT call backup_once from here (the ABC gates backup at a higher layer; mirror TomlBackend's note).
    - Do NOT override `backup_once` (inherited verbatim from ConfigBackend, D-30).

    `__all__ = ["YamlBackend"]`.

    Do NOT add setup/use/doctor logic (those are later phases). Do NOT add a `__main__`. This module is a primitive backend only (D-38 analog: generic IO, no semantic "what does the YAML mean" knowledge).
  </action>

  <verify>
    <automated>cd /Users/axisrow/Projects/zai-codex-helper && python -c "from zai_codex_helper.backends.yaml import YamlBackend; from zai_codex_helper.services.paths import Paths; b = YamlBackend(Paths.from_home('/tmp/__nonexistent_yaml__')); assert b.path.name == 'moonbridge-zai.yml'; print('import OK, path bound:', b.path)"</automated>
  </verify>
  <done>YamlBackend imports, subclasses ConfigBackend, binds Paths.moonbridge_yml, and uses yaml.safe_* only. The default mode for write_canonical is 0o600.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Pin SC-1 (0600) + safe_* discipline with unit tests</name>
  <files>tests/test_yaml_backend.py</files>

  <read_first>
    - tests/test_toml_backend.py — the established test pattern for a concrete backend: @pytest.mark.unit marker, tmp_path-based Paths.from_home(tmp_path) injection (NEVER Paths.default()), the structure of a round-trip / mode test. Mirror its imports and fixtures.
    - tests/conftest.py — confirm the autouse HOME-isolation fixture exists (D-14) so tests never touch real $HOME.
    - src/zai_codex_helper/backends/_atomic.py — to write a correct mode assertion: the destination mode after atomic_write(mode=0o600) is read via `oct(path.stat().st_mode & 0o777)` and must equal `'0o600'`.
  </read_first>

  <behavior>
    - Test test_yaml_write_canonical_lands_at_0600 (SC-1, SECR-02): inject Paths.from_home(tmp_path), YamlBackend(paths).write_canonical({"ZAI_API_KEY": "sk-test", "model": "glm-5.2"}). Assert `(tmp_path/'.codex'/'moonbridge-zai.yml').stat().st_mode & 0o777 == 0o600`. This is the load-bearing secrets assertion.
    - Test test_yaml_default_mode_is_restricted_even_when_not_passed (D-56 default): call write_canonical WITHOUT a mode arg; assert the file is still 0o600 (proves the default is 0o600, not None).
    - Test test_yaml_round_trip (D-56 safe_dump args): after write_canonical(data), call read() and assert the returned object equals the input dict (safe_load(safe_dump(data)) round-trip). Use a dict with unicode + nested keys to exercise allow_unicode=True and sort_keys=False.
    - Test test_yaml_uses_safe_load_and_safe_dump_only (D-61 discipline): grep the module source text for the forbidden bare calls. Read the file content into a string; assert `'yaml.load(' NOT in source and 'yaml.dump(' NOT in source and 'yaml.safe_load' in source and 'yaml.safe_dump' in source. This pins the security discipline against a future regression to bare load/dump.
    - Test test_yaml_backup_once_inherited_not_overridden (D-30): assert `'def backup_once' NOT in inspect.getsource(YamlBackend)` — the method is inherited from the ABC, not redefined (so the coordinator gate cannot be bypassed).
    - Test test_yaml_parent_dir_created: write_canonical into a fresh tmp_path where ~/.codex does not yet exist; assert the file appears (atomic_write creates parents — confirm the backend does not need a pre-mkdir).
  </behavior>

  <action>
    Write `tests/test_yaml_backend.py` with `@pytest.mark.unit` on every test. Use `Paths.from_home(tmp_path)` for isolation (mirror tests/test_toml_backend.py). For the grep-discipline test, read the module via `pathlib.Path(YamlBackend.__module__ source file)` or `(Path('src/zai_codex_helper/backends/yaml.py')).read_text()` and assert substring presence/absence. For the inherited-method test, use `inspect.getsource(YamlBackend)`.

    Run the suite. If RED on test_yaml_default_mode_is_restricted, the default arg is wrong — fix the YamlBackend.write_canonical signature default to 0o600 (Task 1 specifies this; the test enforces it). If RED on the safe_* test, a bare call slipped in — replace with the safe_ variant.
  </action>

  <verify>
    <automated>cd /Users/axisrow/Projects/zai-codex-helper && python -m pytest tests/test_yaml_backend.py -m unit -v 2>&1 | tail -20</automated>
  </verify>
  <done>All 6 unit tests pass. The 0o600 assertion (SC-1 / SECR-02) is green. The safe_* discipline test is green (no bare yaml.load/yaml.dump). The round-trip and default-mode tests are green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI/domain → YamlBackend → filesystem | The API key (ZAI_API_KEY value) transits this boundary as YAML content; it must land at 0600 on disk. |
| atomic_write temp → os.replace destination | The durability/atomicity boundary; a crash here must not leave a partial or world-readable file. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-09-01 | Information Disclosure | YamlBackend.write_canonical | critical | mitigate | Default `mode=0o600` on write_canonical (D-56); test_yaml_write_canonical_lands_at_0600 + test_yaml_default_mode_is_restricted pin it. The file holds ZAI_API_KEY (SECR-02); 0600 is the CLAUDE.md mandate. |
| T-09-01b | Tampering (input) | YamlBackend.read (yaml.safe_load) | high | mitigate | yaml.safe_* only (D-61); test_yaml_uses_safe_load_and_safe_dump_only pins no bare yaml.load (arbitrary-object-construction risk). |
| T-09-SC | Tampering | pip install (pyyaml) | low | accept | pyyaml>=6.0 declared Phase 1 (D-06); well-known maintained package; no [ASSUMED]/[SUS] package. No new runtime dep introduced in this plan. |
</threat_model>

<verification>
- `python -m pytest tests/test_yaml_backend.py -m unit -v` — all tests green.
- `python -c "from zai_codex_helper.backends.yaml import YamlBackend; from zai_codex_helper.services.paths import Paths; import tempfile,os; from pathlib import Path; d=Path(tempfile.mkdtemp()); b=YamlBackend(Paths.from_home(d)); b.write_canonical({'ZAI_API_KEY':'x'}); print('mode', oct((d/'.codex'/'moonbridge-zai.yml').stat().st_mode & 0o777))"` prints `mode 0o600`.
- `grep -n 'yaml.load\b\|yaml.dump\b' src/zai_codex_helper/backends/yaml.py` returns nothing (no bare load/dump); `grep -c 'yaml.safe_' src/zai_codex_helper/backends/yaml.py` >= 1.
- `python -m pytest` (full suite) still green — no regressions to other backends.
</verification>

<success_criteria>
- SC-1 (ROADMAP Phase 9): "YamlBackend writes the canonical moonbridge-zai.yml via yaml.safe_dump at 0600" — verified by test_yaml_write_canonical_lands_at_0600.
- SECR-02 (REQUIREMENTS): "key stored in ~/.codex/moonbridge-zai.yml with permissions 0600" — same test.
- D-56, D-61, D-62 honored.
- No setup/use/doctor/Moon-Bridge logic (out of scope — Phases 11/12).
</success_criteria>

<output>
Create `.planning/phases/09-remaining-file-backends/09-01-SUMMARY.md` when done.
</output>
