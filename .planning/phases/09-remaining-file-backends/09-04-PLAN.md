---
phase: 09-remaining-file-backends
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - src/zai_codex_helper/backends/plist.py
  - tests/test_plist_backend.py
autonomous: true
requirements: []
user_setup: []

must_haves:
  truths:
    - "PlistBackend(ConfigBackend) for ~/Library/LaunchAgents/dev.zai.moonbridge.plist via plistlib.load / plistlib.dump(fmt=plistlib.FMT_XML) (per D-59, D-61)"
    - "write_canonical(plist_dict) emits a FULL canonical plist (not a merge — plists are owned by the helper, written fresh) — per D-59, D-60"
    - "The canonical plist dict includes Label='dev.zai.moonbridge', KeepAlive=True, RunAtLoad=True, ProgramArguments=[absolute_binary_path, '-config', absolute_config_path] — per D-59, SC-4"
    - "ProgramArguments[0] (binary path) is ABSOLUTE and RESOLVED — NO literal '~' anywhere in the plist (launchd does not expand ~; the path comes from Paths-resolved ~/.codex/moon-bridge) — per D-59, SC-4"
    - "write_canonical routes the serialized XML through self._write_via_atomic (D-29 structural — never calls atomic_write directly)"
    - "backup_once is inherited verbatim from ConfigBackend (D-30); PlistBackend does not override it"
  artifacts:
    - "src/zai_codex_helper/backends/plist.py — PlistBackend class + canonical_plist() helper building the launchd-required dict"
    - "tests/test_plist_backend.py — @pytest.mark.unit tests pinning SC-4 (KeepAlive/RunAtLoad + absolute resolved binary path, no literal ~)"
  key_links:
    - "PlistBackend.__init__(paths) → self._path = paths.launchagents_dir / 'dev.zai.moonbridge.plist' (Paths exposes the directory; the backend appends the fixed filename)"
    - "PlistBackend.write_canonical(plist_dict) → plistlib.dumps(data, fmt=plistlib.FMT_XML) → self._write_via_atomic(xml_bytes, mode)"
---

<objective>
Deliver `PlistBackend`, the concrete `ConfigBackend` subclass for `~/Library/LaunchAgents/dev.zai.moonbridge.plist` that emits a launchd-correct LaunchAgent (SC-4 of ROADMAP Phase 9). Requirement coverage: Phase 9's requirement IDs (SECR-02, SEC-01) are carried by plans 09-01 and 09-02; SC-4 has no dedicated requirement ID in REQUIREMENTS traceability, so this plan cites `requirements: []` and the contract lives in the SC-4 success criterion below (PlistBackend feeds Phase 13 `install-service`/`uninstall-service` — SERV-01/SERV-03 are later-phase consumers).

Purpose: Phase 13 `install-service` will write the LaunchAgent plist that keeps Moon Bridge alive. launchd is strict and unforgiving: it does NOT expand `~` (a literal `~/` in `ProgramArguments` makes the agent fail to start the binary), it requires `Label` to be unique and stable (so `uninstall-service` can `bootout` the exact same registration), and the helper must own the plist wholesale (write the FULL canonical dict, not merge — there is no user content to preserve in a helper-owned plist). Phase 9 delivers the backend that emits the correct dict + proof; Phase 13 calls it and wires `launchctl bootstrap`/`bootout`.

Output: `src/zai_codex_helper/backends/plist.py` (PlistBackend + the canonical-plist dict builder) + `tests/test_plist_backend.py`.
</objective>

<execution_context>
@/Users/axisrow/Projects/zai-codex-helper/.claude/gsd-core/workflows/execute-plan.md
@/Users/axisrow/Projects/zai-codex-helper/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-remaining-file-backends/09-CONTEXT.md

# Structural template (subclass the ABC exactly like this):
@src/zai_codex_helper/backends/base.py
@src/zai_codex_helper/backends/toml.py
@src/zai_codex_helper/backends/_atomic.py
@src/zai_codex_helper/services/paths.py

# Project library discipline + launchd convention:
@./.claude/CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: PlistBackend — launchd-correct full-canonical plist (absolute path, no ~)</name>
  <files>src/zai_codex_helper/backends/plist.py</files>

  <read_first>
    - src/zai_codex_helper/backends/base.py — ConfigBackend ABC: `__init__(paths, field)` binds `self._path = Path(getattr(paths, field))`. NOTE: `Paths` exposes `launchagents_dir` (a DIRECTORY), not a plist path field. The ABC resolves `getattr(paths, field)` to a Path — for PlistBackend this means the backend cannot use the ABC's `__init__` directly with a single field name. PlistBackend MUST override `__init__` to call `super().__init__(paths, "launchagents_dir")` and then REASSIGN `self._path = paths.launchagents_dir / "dev.zai.moonbridge.plist"` (the ABC binds the directory; the subclass appends the fixed filename). Document this override clearly — it is the one backend that resolves a directory + fixed filename rather than a single Paths field. `read`/`exists`/`write_canonical` abstract; `_write_via_atomic(content, mode)` private helper; `backup_once` concrete+inherited (D-30).
    - src/zai_codex_helper/backends/toml.py — the structural template: module docstring citing decisions; `__init__` binds the path; `read` parses; `write_canonical` serializes then calls `self._write_via_atomic`. Mirror this for PlistBackend (with the `__init__` override noted above).
    - src/zai_codex_helper/services/paths.py — `Paths.launchagents_dir` = `home / "Library" / "LaunchAgents"` (the directory); `Paths.moonbridge_yml` = `home / ".codex" / "moonbridge-zai.yml"` (the absolute config path for `ProgramArguments`); `Paths.codex_dir` = `home / ".codex"` (the binary lives at `codex_dir / "moon-bridge"`). All resolved off the injected `home` — NO literal `~`.
    - .planning/phases/09-remaining-file-backends/09-CONTEXT.md — D-59 (PlistBackend; `read()` → `plistlib.load`; `write_canonical(plist_dict)` → `plistlib.dump(data, fh, fmt=plistlib.FMT_XML)` then atomic_write; FULL canonical plist not merge; canonical dict MUST include `Label="dev.zai.moonbridge"`, `KeepAlive=True`, `RunAtLoad=True`, `ProgramArguments=[absolute_binary_path, "-config", absolute_config_path]`; binary path ABSOLUTE and RESOLVED, NO literal `~` — launchd doesn't expand it; use Paths-resolved absolute `~/.codex/moon-bridge`), D-60 (PlistBackend always emits the FULL canonical plist — not a merge; plists owned by the helper), D-61 (`plistlib` stdlib — no new runtime dep), D-DEFERRED-01 (mode handling; plist is conventionally 0644 per CLAUDE.md — pass explicit `0o644` so the plist lands at the launchd-conventional mode rather than the 0600 mode=None would yield).
    - ./.claude/CLAUDE.md — "File Permissions & Backup Conventions" table: LaunchAgent plist is `0644` (launchd requirement). "LaunchAgent Management" section: `plistlib.dump(data, fh, fmt=plistlib.FMT_XML)` is the idiomatic emission; `ProgramArguments` should point at the built binary `~/.codex/moon-bridge` with `-config ~/.codex/moonbridge-zai.yml`, NOT `go run`; `~/Library/LaunchAgents/` (per-user), NOT `/Library/LaunchDaemons/` (system-wide/root). "What NOT to Use": `launchctl load`/`unload` is deprecated (Phase 13 uses `bootstrap`/`bootout` — not this backend's job).
  </read_first>

  <action>
    Create `src/zai_codex_helper/backends/plist.py` defining `PlistBackend(ConfigBackend)` (per D-59, D-60, D-61).

    Module docstring: cite Phase 9, D-59 (launchd-correct LaunchAgent plist), D-60 (full canonical, not merge — plists owned by the helper), D-61 (`plistlib` stdlib). State the two load-bearing launchd invariants: (1) NO literal `~` anywhere — launchd does not expand `~`, so a `~/...` path in `ProgramArguments` makes the agent fail to start the binary; the backend resolves every path to an ABSOLUTE path off the injected `Paths` (the `home` is already resolved to a real filesystem path by `Paths.from_home`, never a literal `~`). (2) `Label` is the unique stable identifier Phase 13's `launchctl bootout` will target — it MUST be the exact string `dev.zai.moonbridge`. State that this is the primitive Phase 13 `install-service`/`uninstall-service` will call; `launchctl bootstrap`/`bootout` wiring is Phase 13 (out of scope here — this module only writes the plist file).

    **Module-level constant `LABEL = "dev.zai.moonbridge"`** (D-59): the exact, stable launchd Label. Export in `__all__` so Phase 13's `install-service`/`uninstall-service` can import the single source of truth — `uninstall-service` MUST `bootout` the same Label `install-service` registered (ROADMAP Phase 13 SC-3: "Both commands share one plist Label constant, so uninstall never orphans a registered agent"). Do NOT inline the string in multiple places.

    **Module-level helper `canonical_plist(paths: Paths) -> dict`** (D-59, D-60): build and return the FULL canonical plist dict. This is the single place the launchd-required shape is defined; `write_canonical` and tests both call it.
    - `binary_path = str(paths.codex_dir / "moon-bridge")` — ABSOLUTE (resolved off the injected `home`, which `Paths.from_home` already turned into a real path). NO literal `~`. This is the built-binary invocation target (CLAUDE.md: point at `~/.codex/moon-bridge`, NOT `go run`).
    - `config_path = str(paths.moonbridge_yml)` — ABSOLUTE (resolved `~/.codex/moonbridge-zai.yml`). NO literal `~`.
    - Return the dict:
      - `Label`: `LABEL` (the constant),
      - `ProgramArguments`: `[binary_path, "-config", config_path]`,
      - `KeepAlive`: `True`,
      - `RunAtLoad`: `True`.
    - Do NOT add `WatchPaths`/`StartInterval`/`EnvironmentVariables`/`WorkingDirectory`/`StandardOutPath`/`StandardErrorPath` unless a later phase requires them — Phase 9 delivers the minimal launchd-correct dict (D-59 lists exactly the four keys; `KeepAlive`/`RunAtLoad` keep the bridge alive and start it at login). Adding unrequested keys is scope creep against D-59.

    **Class `PlistBackend(ConfigBackend)`:**
    - `__init__(self, paths: Paths) -> None`: the directory+filename override (see read_first). Call `super().__init__(paths, "launchagents_dir")` (binds the ABC's `self._path` to the directory), then reassign `self._path = paths.launchagents_dir / "dev.zai.moonbridge.plist"`. Store `self._paths = paths` (already set by the ABC) so `write_canonical` can call `canonical_plist(self._paths)` to resolve the binary/config paths. Document WHY the override: `Paths` exposes the directory, not a plist-path field; the backend owns the fixed filename (`dev.zai.moonbridge.plist` — matches `LABEL`).
    - `read(self) -> dict`: if the file exists, open it in binary read mode and `plistlib.load(fh)`; return the parsed dict. If the file does NOT exist, raise `FileNotFoundError` (unlike JsonBackend's `{}` baseline, a missing plist means "not installed" — the caller, Phase 13, distinguishes install vs reinstall on existence, so raising is the honest signal; do NOT silently return `{}`). Document this choice.
    - `exists(self) -> bool`: `self._path.exists()`.
    - `write_canonical(self, content: dict | None = None, mode: int | None = 0o644) -> None`:
      * `content` is the plist dict to write. If `content is None`, default to `canonical_plist(self._paths)` (the canonical shape — the common case for `install-service`). If a dict is passed, write THAT (allows a caller to write a customized plist, though Phase 13 will use the canonical default).
      * Default `mode=0o644` (per CLAUDE.md "File Permissions & Backup Conventions": LaunchAgent plist is `0644`, a launchd requirement). Pass `0o644` EXPLICITLY rather than relying on `mode=None` — D-DEFERRED-01 means `mode=None` yields `0o600` from the tempfile; `0o600` is more restrictive than launchd's conventional `0644` and while it likely still works, the explicit `0o644` matches the documented convention (CLAUDE.md) and avoids surprising a user who inspects the plist perms. Document this choice.
      * Validate `content` is a `dict` containing AT LEAST the `Label` key (defensive — a plist without `Label` is launchd-invalid; raise `ValueError` if `Label` is missing). Do NOT validate the full canonical shape here (a caller passing a custom dict may legitimately omit `KeepAlive`); only guard the load-bearing `Label`.
      * Serialize: `plistlib.dumps(content, fmt=plistlib.FMT_XML)` returns bytes (XML plist text). Use `plistlib.dumps` (not `dump` to a filehandle) so the bytes flow through `atomic_write` as a single payload — routing through `_write_via_atomic` is D-29 structural, and `dumps` avoids managing a separate filehandle.
      * Route through the ABC helper: `self._write_via_atomic(xml_bytes, mode)`. Do NOT call `atomic_write` directly (D-29). Do NOT call `backup_once` (higher-layer gate). Do NOT call `launchctl` — Phase 13's job.

    `__all__ = ["PlistBackend", "canonical_plist", "LABEL"]`.

    Do NOT add `launchctl bootstrap`/`bootout` logic, `install-service`/`uninstall-service` command wiring, or service-state tracking (Phase 13). This module is the plist-emission primitive only.
  </action>

  <verify>
    <automated>cd /Users/axisrow/Projects/zai-codex-helper && python -c "from zai_codex_helper.backends.plist import PlistBackend, canonical_plist, LABEL; from zai_codex_helper.services.paths import Paths; p = Paths.from_home('/tmp/__nonexistent_plist__'); b = PlistBackend(p); assert b.path.name == 'dev.zai.moonbridge.plist'; assert b.path.parent == p.launchagents_dir; d = canonical_plist(p); assert d['Label'] == 'dev.zai.moonbridge'; assert d['KeepAlive'] is True; assert d['RunAtLoad'] is True; assert d['ProgramArguments'][0] == str(p.codex_dir / 'moon-bridge'); assert d['ProgramArguments'][1] == '-config'; assert d['ProgramArguments'][2] == str(p.moonbridge_yml); assert '~' not in d['ProgramArguments'][0]; assert '~' not in d['ProgramArguments'][2]; assert LABEL == 'dev.zai.moonbridge'; print('import + canonical_plist OK')"</automated>
  </verify>
  <done>PlistBackend imports, subclasses ConfigBackend, resolves `launchagents_dir / dev.zai.moonbridge.plist`, exports the stable `LABEL`, and `canonical_plist` emits the launchd-required dict with absolute resolved paths (no literal `~`).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Pin SC-4 (KeepAlive/RunAtLoad + absolute resolved path, no ~) with unit tests</name>
  <files>tests/test_plist_backend.py</files>

  <read_first>
    - tests/test_toml_backend.py — the established pattern: `from __future__ import annotations`, `@pytest.mark.unit`, `tmp_path` injection via `Paths.from_home(tmp_path)`, round-trip / correctness test structure, module docstring explaining what SC the file pins.
    - tests/conftest.py — the autouse `_isolate_home` fixture (HOME → tmp_path, pre-creates `.codex`). NOTE: the fixture does NOT pre-create `Library/LaunchAgents/` — `atomic_write`'s `dest.parent.mkdir(parents=True, exist_ok=True)` handles directory creation, so tests do not need to pre-create the dir.
    - src/zai_codex_helper/backends/plist.py — Task 1 output: `PlistBackend`, `canonical_plist`, `LABEL`, the full-canonical `write_canonical` (no merge), the `__init__` directory+filename override.
  </read_first>

  <behavior>
    - Test test_canonical_plist_has_required_keys (D-59, SC-4): build `Paths.from_home(tmp_path)`; call `canonical_plist(paths)`; assert the dict has keys `Label`, `ProgramArguments`, `KeepAlive`, `RunAtLoad`. Assert `Label == "dev.zai.moonbridge"` (the `LABEL` constant). Assert `KeepAlive is True` and `RunAtLoad is True` (boolean True, not truthy — launchd is strict about the XML bool type; `plistlib` emits `<true/>` for Python `True`).
    - Test test_canonical_plist_program_arguments_absolute_no_tilde (SC-4 core — the single highest-signal test): call `canonical_plist(paths)`; assert `ProgramArguments` is a 3-element list `[binary, "-config", config]`; assert `ProgramArguments[0] == str(paths.codex_dir / "moon-bridge")` (absolute resolved); assert `ProgramArguments[1] == "-config"`; assert `ProgramArguments[2] == str(paths.moonbridge_yml)` (absolute resolved); assert `"~" not in ProgramArguments[0]` AND `"~" not in ProgramArguments[2]` (NO literal tilde — launchd does not expand it). Use the injected `tmp_path` home so the "absolute" assertion is concrete (e.g. starts with `/tmp/...` or the macOS temp root — the point is it is a resolved path, not `~/`).
    - Test test_canonical_plist_label_is_stable_constant (SC-4 / Phase 13 SC-3 enabler): assert `canonical_plist(paths)["Label"] == LABEL` (imported from the module) — the Label is the single source of truth that Phase 13 uninstall will `bootout`.
    - Test test_plist_write_emits_full_canonical_xml (D-59, D-60 full-canonical-not-merge): instantiate `PlistBackend(paths)`; `write_canonical()` (no arg — defaults to `canonical_plist`). Read the file text. Assert it is valid XML plist: contains `<plist version="1.0">`, `<dict>`, `<key>Label</key>`, `<string>dev.zai.moonbridge</string>`, `<key>KeepAlive</key>`, `<true/>`, `<key>RunAtLoad</key>`, `<true/>`, `<key>ProgramArguments</key>`, `<array>`. Assert the binary path appears as a `<string>` inside `<array>` and contains NO literal `~`.
    - Test test_plist_write_then_read_round_trips (D-59 read): `write_canonical()`; `d = read()`; assert `d["Label"] == "dev.zai.moonbridge"`; assert `d["KeepAlive"] is True`; assert `d["RunAtLoad"] is True`; assert `d["ProgramArguments"]` matches `canonical_plist(paths)["ProgramArguments"]` (`plistlib.load` round-trips the XML plist back to the same dict).
    - Test test_plist_write_overwrites_not_merges (D-60 full-canonical): seed an EXISTING plist at the path with a DIFFERENT Label (e.g. `{"Label": "com.example.other", "KeepAlive": False}`) via `plistlib.dumps` + `write_bytes`. `write_canonical()` (canonical default). Re-read. Assert the Label is now `dev.zai.moonbridge` (the canonical shape REPLACED the seed — NOT merged; `KeepAlive` is `True`, not the seed's `False`). This pins D-60: the helper owns the plist wholesale, writing fresh each time.
    - Test test_plist_write_into_nonexistent_launchagents_dir (atomic_write creates the dir): do NOT pre-create `tmp_path / "Library" / "LaunchAgents"`. `write_canonical()`. Assert the file exists at `tmp_path / "Library" / "LaunchAgents" / "dev.zai.moonbridge.plist"` (`atomic_write`'s `dest.parent.mkdir(parents=True, exist_ok=True)` creates the chain).
    - Test test_plist_read_raises_when_absent (D-59 honest-signal read): do NOT seed the plist. Assert `read()` raises `FileNotFoundError` (the caller, Phase 13, distinguishes install vs reinstall on this signal — do NOT silently return `{}`).
    - Test test_plist_write_rejects_dict_without_label (defensive guard): assert `write_canonical({"KeepAlive": True})` raises `ValueError` (a plist without `Label` is launchd-invalid — fail loudly).
    - Test test_plist_write_accepts_custom_dict_with_label (D-59 caller-supplied plist): `write_canonical({"Label": "dev.zai.moonbridge", "Custom": "value"})`; re-read; assert `d["Label"] == "dev.zai.moonbridge"` and `d["Custom"] == "value"` (a caller MAY pass a customized plist as long as `Label` is present).
    - Test test_plist_lands_at_0644 (D-DEFERRED-01 explicit mode, CLAUDE.md convention): `write_canonical()`; assert `(path.stat().st_mode & 0o777) == 0o644` (the launchd-conventional mode, not the 0600 that `mode=None` would yield).
    - Test test_plist_backup_once_inherited_not_overridden (D-30): assert `'def backup_once'` is NOT in `inspect.getsource(PlistBackend)`.
    - Test test_plist_path_under_launchagents_dir (D-59 location): instantiate `PlistBackend(paths)`; assert `backend.path.parent == paths.launchagents_dir`; assert `backend.path.name == "dev.zai.moonbridge.plist"` (NOT under `/Library/LaunchDaemons/` — per-user LaunchAgent, CLAUDE.md "What NOT to Use").
  </behavior>

  <action>
    Write `tests/test_plist_backend.py` with `@pytest.mark.unit` on every test and `from __future__ import annotations` at the top. Inject `Paths.from_home(tmp_path)` (mirror `tests/test_toml_backend.py`). For the "no literal `~`" assertions, use `"~" not in path_string` (substring check — the resolved path from `Paths.from_home(tmp_path)` is a real `/...` path with no `~`). For the XML-shape assertions, read the file TEXT (`path.read_text()`) and assert substrings (`"<true/>"`, `"<key>Label</key>"`, etc.); `plistlib.dumps(fmt=plistlib.FMT_XML)` emits canonical Apple XML plist with `<true/>` for Python `True`. For the round-trip test, `plistlib.load(path.open("rb"))` parses the XML back to the dict. For the overwrite-not-merge test, seed a different-Label plist via `path.write_bytes(plistlib.dumps({"Label": "com.example.other", "KeepAlive": False}, fmt=plistlib.FMT_XML))` then call `write_canonical()` and assert the canonical Label replaced the seed.

    Run the suite. If RED on `test_canonical_plist_program_arguments_absolute_no_tilde`, the path is being built with a literal `~` — re-check `canonical_plist` uses `str(paths.codex_dir / "moon-bridge")` (the `Paths` object is already resolved off `home`, no `~`). If RED on `test_plist_write_overwrites_not_merges`, the write path is reading-and-merging instead of writing fresh — re-check `write_canonical` does NOT call `read()` + merge; it writes `content` (or `canonical_plist`) wholesale. If RED on `test_plist_lands_at_0644`, the explicit `mode=0o644` default is not being passed through — re-check the signature default and that `_write_via_atomic` receives it.
  </action>

  <verify>
    <automated>cd /Users/axisrow/Projects/zai-codex-helper && python -m pytest tests/test_plist_backend.py -m unit -v 2>&1 | tail -25</automated>
  </verify>
  <done>All unit tests pass. SC-4 is green: canonical plist has Label/KeepAlive/RunAtLoad + ProgramArguments with absolute resolved binary path (no literal ~), write emits full canonical XML (not merge), write overwrites a seeded plist (full-canonical-not-merge), read raises when absent, lands at 0644.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Phase 13 caller → PlistBackend → filesystem (`~/Library/LaunchAgents/dev.zai.moonbridge.plist`) | The plist is helper-owned (no user content to preserve); written fresh each time. launchd is strict about Label uniqueness and path resolution. |
| `canonical_plist` path resolution → launchd invocation | A literal `~` in `ProgramArguments` would make launchd fail to start the binary (launchd does NOT expand `~`); the backend MUST resolve to absolute paths off the injected `Paths`. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-09-04 | Denial of Service (agent fails to start) | canonical_plist ProgramArguments (absolute path, no ~) | high | mitigate | `canonical_plist` builds `ProgramArguments[0]` and `[2]` via `str(paths.codex_dir / "moon-bridge")` and `str(paths.moonbridge_yml)` — both resolved off the injected `home` (already a real `/...` path from `Paths.from_home`), NEVER a literal `~`. `test_canonical_plist_program_arguments_absolute_no_tilde` pins `"~" not in` both paths. A `~`-prefixed path would silently break `launchctl bootstrap` (launchd does not expand `~`); this is the single most load-bearing correctness invariant for the service. |
| T-09-04b | Tampering (orphaned agent) | LABEL constant stability | high | mitigate | `LABEL = "dev.zai.moonbridge"` is a single module-level constant exported in `__all__`; `canonical_plist` and `write_canonical` both use it. Phase 13 `uninstall-service` will import the SAME constant to `bootout` the exact registration (ROADMAP Phase 13 SC-3). `test_canonical_plist_label_is_stable_constant` pins `canonical_plist(...)["Label"] == LABEL`. A drifted Label would orphan the agent (uninstall cannot find it). |
| T-09-04c | Tampering (launchd-invalid plist) | write_canonical Label guard | medium | mitigate | `write_canonical` raises `ValueError` if the supplied dict lacks `Label` — a plist without `Label` is launchd-invalid. `test_plist_write_rejects_dict_without_label` pins this. The full canonical shape is NOT validated (a caller may legitimately customize), but the load-bearing `Label` is. |
| T-09-04d | Tampering (wrong file location) | PlistBackend.__init__ path resolution | medium | mitigate | The backend resolves `paths.launchagents_dir / "dev.zai.moonbridge.plist"` — per-user `~/Library/LaunchAgents/`, NEVER `/Library/LaunchDaemons/` (system-wide, requires root — CLAUDE.md "What NOT to Use"). `test_plist_path_under_launchagents_dir` pins `backend.path.parent == paths.launchagents_dir`. |
| T-09-04-SC | Tampering | pip install | low | accept | PlistBackend uses only stdlib `plistlib` — no new package introduced (D-61). |
</threat_model>

<verification>
- `python -m pytest tests/test_plist_backend.py -m unit -v` — all tests green.
- Absolute-path proof: `canonical_plist(paths)["ProgramArguments"][0]` contains no `~` and equals `str(paths.codex_dir / "moon-bridge")`.
- Full-canonical proof: seed a different-Label plist; `write_canonical()` replaces it (not merges); re-read Label is `dev.zai.moonbridge`.
- launchd-keys proof: emitted XML contains `<key>Label</key>`, `<key>KeepAlive</key><true/>`, `<key>RunAtLoad</key><true/>`, `<key>ProgramArguments</key><array>`.
- `python -m pytest` (full suite) still green — no regressions.
</verification>

<success_criteria>
- SC-4 (ROADMAP Phase 9): "PlistBackend emits a LaunchAgent plist with `KeepAlive`/`RunAtLoad` and an absolute resolved binary path (no literal `~`)" — verified by `test_canonical_plist_has_required_keys` + `test_canonical_plist_program_arguments_absolute_no_tilde` + `test_plist_write_emits_full_canonical_xml`.
- D-59 honored: `read()` → `plistlib.load`; `write_canonical` → `plistlib.dumps(fmt=plistlib.FMT_XML)` → `_write_via_atomic`; canonical dict has Label/KeepAlive/RunAtLoad/ProgramArguments with absolute resolved paths.
- D-60 honored: FULL canonical plist, not a merge (`test_plist_write_overwrites_not_merges`).
- D-61 honored: stdlib `plistlib` only, no new runtime dep.
- CLAUDE.md honored: plist at `0o644` (launchd convention); per-user `~/Library/LaunchAgents/` (not `/Library/LaunchDaemons/`).

Artifacts this phase produces:
- `src/zai_codex_helper/backends/plist.py` — `PlistBackend` class + `canonical_plist(paths)` helper + `LABEL` constant.
- `tests/test_plist_backend.py` — `@pytest.mark.unit` tests pinning SC-4 (KeepAlive/RunAtLoad + absolute path no ~ + full canonical).
</success_criteria>

<output>
Create `.planning/phases/09-remaining-file-backends/09-04-SUMMARY.md` when done.
</output>
