---
phase: 01-project-skeleton-packaging-foundation
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - pyproject.toml
  - tests/conftest.py
  - tests/test_cli_help.py
  - tests/test_markers.py
  - tests/test_home_isolation.py
  - tests/test_smoke_install.py
  - tests/test_error_contract.py
autonomous: true
requirements:
  - PKG-01
  - PKG-02
  - PKG-04
  - PKG-05
user_setup: []

must_haves:
  truths:
    # PKG-04 (the harness itself — load-bearing for all 14 later phases)
    - "`pytest -q` discovers and runs tests marked `@pytest.mark.unit`, `@pytest.mark.integration`, `@pytest.mark.smoke` (e2e excluded by default via addopts `-m 'not e2e'`) with zero `PytestUnknownMarkWarning` warnings."
    - "`pytest --markers` lists all four markers: unit, integration, smoke, e2e."
    - "Every test (including unit) runs with `HOME` pointing into a per-test `tmp_path` and a `tmp_path/.codex` directory created — a test that writes to `$HOME/.codex/test_marker` provably does NOT touch the developer's real `~/.codex`."
    # PKG-01
    - "`pip install .` (real, non-editable) exits 0 and afterwards `python -c \"import zai_codex_helper; print(zai_codex_helper.__version__)\"` prints `0.1.0` (src/-layout forces install-before-import)."
    # PKG-02
    - "After install, `zai-codex-helper --help` (run via subprocess against the installed console script OR `python -m zai_codex_helper --help`) prints a line beginning with `usage: zai-codex-helper`, exits 0, and emits no `Traceback`."
    - "`zai-codex-helper` invoked with no subcommand exits non-zero with no `Traceback` in stderr."
    # PKG-05
    - "A handler raising `ZaiCodexHelperError` results in `main()` printing exactly one `error: <message>` line to stderr and returning exit code 1, with no traceback — UNLESS `--debug` is passed, in which case the exception propagates (traceback shown)."
  artifacts:
    - tests/conftest.py
    - tests/test_cli_help.py
    - tests/test_markers.py
    - tests/test_home_isolation.py
    - tests/test_smoke_install.py
    - tests/test_error_contract.py
  key_links:
    - "conftest.py @pytest.fixture(autouse=True) _isolate_home  (D-14 — the iron guarantee no test corrupts real ~/.codex)"
    - "[tool.pytest.ini_options] markers + addopts  (D-13/D-20 — marker registration + e2e exclusion)"
    - "test_error_contract.py ↔ __main__.main() / ZaiCodexHelperError  (proves the D-11/PKG-05 contract wired in Plan 01)"
    - "test_smoke_install.py ↔ pip install . + import zai_codex_helper  (proves PKG-01 src-layout + dynamic version)"
---

<objective>
Prove the Walking Skeleton against all 4 ROADMAP success criteria (PKG-01/02/04/05) with a pytest harness that every later phase inherits. Install the dev extras, add the autouse HOME-isolation fixture (D-14 — the project's "don't corrupt the user's files" ideology made testable), register the tier markers (D-13), and write the smoke/unit tests that fail loud if the skeleton regresses. After this plan, `pytest -q` is green and the foundation is locked.

Purpose: The autouse HOME-isolation fixture is load-bearing for the entire project's safety contract — even a buggy test in Phase 14 must not write to the developer's real `~/.codex`. The marker registry (`--strict-markers`) turns a typo'd `@pytest.mark.unnit` into a hard error instead of a silently-skipped test. The smoke test for `pip install .` → `--help` is the single highest-signal check that the whole packaging chain (src-layout, dynamic version, console script, entry point) actually works end-to-end.

Output: `tests/conftest.py` (autouse `_isolate_home`), five test files covering PKG-01 (install+import smoke), PKG-02 (--help smoke), PKG-04 (markers + HOME isolation), PKG-05 (error contract), plus a final pyproject.toml touch-up only if Plan 01's deferred `pythonpath` is needed.
</objective>

<execution_context>
@/Users/axisrow/Projects/zai-codex-helper/.claude/gsd-core/workflows/execute-plan.md
@/Users/axisrow/Projects/zai-codex-helper/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@/Users/axisrow/Projects/zai-codex-helper/.planning/PROJECT.md
@/Users/axisrow/Projects/zai-codex-helper/.planning/ROADMAP.md
@/Users/axisrow/Projects/zai-codex-helper/.planning/STATE.md
@/Users/axisrow/Projects/zai-codex-helper/.claude/CLAUDE.md
@/Users/axisrow/Projects/zai-codex-helper/.planning/phases/01-project-skeleton-packaging-foundation/01-CONTEXT.md
@/Users/axisrow/Projects/zai-codex-helper/.planning/phases/01-project-skeleton-packaging-foundation/01-RESEARCH.md
@/Users/axisrow/Projects/zai-codex-helper/.planning/phases/01-project-skeleton-packaging-foundation/01-VALIDATION.md
@/Users/axisrow/Projects/zai-codex-helper/.planning/phases/01-project-skeleton-packaging-foundation/01-01-PLAN.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Install dev extras + add autouse HOME-isolation fixture (D-14) to tests/conftest.py</name>
  <read_first>
    - /Users/axisrow/Projects/zai-codex-helper/pyproject.toml (the file Plan 01 created — confirm [project.optional-dependencies] dev and [tool.pytest.ini_options] markers/addopts are present before relying on them)
    - /Users/axisrow/Projects/zai-codex-helper/.planning/phases/01-project-skeleton-packaging-foundation/01-CONTEXT.md (D-14 autouse HOME-isolation — the iron guarantee; D-13 markers in pyproject; D-15 smoke + marker-resolution tests)
    - /Users/axisrow/Projects/zai-codex-helper/.planning/phases/01-project-skeleton-packaging-foundation/01-RESEARCH.md (Pattern 3 "autouse HOME-isolation fixture" + "Code Examples → tests/" + Pitfall 6 "HOME-изоляция не работает на macOS" + Open Question 2 "pythonpath = [src]")
    - /Users/axisrow/Projects/zai-codex-helper/.planning/phases/01-project-skeleton-packaging-foundation/01-VALIDATION.md ("Wave 0 Requirements" checklist)
  </read_first>
  <files>
    tests/conftest.py
    pyproject.toml
  </files>
  <action>
    First install the dev extras so the harness resolves: `pip install -e ".[dev]"` (editable so source edits are picked up without reinstall; brings pytest>=8.0, pytest-httpserver>=1.1, build, hatchling>=1.21, ruff>=0.6 AND registers the `zai-codex-helper` console script + puts `zai_codex_helper` on sys.path via hatchling dev-mode-dirs). If this command fails, STOP and surface the error — do not paper over it.

    Create `tests/conftest.py` with the autouse HOME-isolation fixture from RESEARCH Pattern 3 (verified against docs.pytest.org/en/stable/reference/reference.html#monkeypatch):
    - `import pytest`.
    - `@pytest.fixture(autouse=True)` `def _isolate_home(tmp_path, monkeypatch):` — docstring citing D-14 ("Изолировать КАЖДЫЙ тест от реального $HOME").
    - Body: `monkeypatch.setenv("HOME", str(tmp_path))`; `(tmp_path / ".codex").mkdir(parents=True, exist_ok=True)`; `yield tmp_path`.
    - `autouse=True` means EVERY test (unit/integration/smoke) gets this isolation with zero opt-in — this is the point. No test should ever need to request it explicitly (though it may, as `test_home_isolation.py` does, to assert against the yielded value).

    Create `tests/` directory if it does not exist (it doesn't — greenfield).

    Conditional pyproject.toml edit (ONLY if needed): after installing, run `python -c "import zai_codex_helper"` from the repo root. If it raises `ModuleNotFoundError`, add `pythonpath = ["src"]` under `[tool.pytest.ini_options]` (RESEARCH A4 fallback). If import succeeds (expected with editable install), do NOT add `pythonpath` — leave Plan 01's pyproject untouched. Make at most this one targeted Edit; do not rewrite other sections.

    Do NOT register markers in conftest.py — they are registered in `[tool.pytest.ini_options] markers` (D-13, already done in Plan 01). Duplicate registration is harmless but forbidden by the single-config-file decision.
  </action>
  <verify>
    <automated>cd /Users/axisrow/Projects/zai-codex-helper &amp;&amp; pip install -e ".[dev]" &gt;/dev/null 2&gt;&amp;1; python -c "import zai_codex_helper; print('import OK', zai_codex_helper.__version__)"; python -c "import tests.conftest; print('conftest OK')"; grep -q 'autouse=True' tests/conftest.py &amp;&amp; grep -q 'setenv("HOME"' tests/conftest.py &amp;&amp; echo 'fixture OK'; pytest --collect-only -q 2&gt;&amp;1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `pip install -e ".[dev]"` exits 0 (verify `echo $?` immediately after).
    - `python -c "import zai_codex_helper"` succeeds (no ModuleNotFoundError) from repo root.
    - `zai-codex-helper --help` exits 0 and prints `usage: zai-codex-helper` (the editable install registered the console script).
    - `tests/conftest.py` exists and contains a fixture decorated with `@pytest.fixture(autouse=True)` whose body calls `monkeypatch.setenv("HOME", ...)` and creates a `.codex` subdir, then yields (D-14).
    - `pytest --collect-only` runs without `PytestUnknownMarkWarning` (markers resolve from Plan 01's `[tool.pytest.ini_options]`).
    - `pyproject.toml` is either untouched (import worked) or has exactly one added `pythonpath = ["src"]` line under `[tool.pytest.ini_options]` (A4 fallback) — no other changes.
  </acceptance_criteria>
  <done>Dev extras installed; console script registered; autouse HOME-isolation fixture in place; pytest collects without marker warnings; `import zai_codex_helper` works from repo root.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Write smoke + marker + HOME-isolation tests (PKG-01, PKG-02, PKG-04)</name>
  <read_first>
    - /Users/axisrow/Projects/zai-codex-helper/tests/conftest.py (Task 1 fixture — tests reference `_isolate_home` and rely on autouse HOME isolation)
    - /Users/axisrow/Projects/zai-codex-helper/.planning/phases/01-project-skeleton-packaging-foundation/01-RESEARCH.md (Code Examples → test_cli_help.py, test_markers.py, test_home_isolation.py — validated test shapes)
    - /Users/axisrow/Projects/zai-codex-helper/.planning/phases/01-project-skeleton-packaging-foundation/01-CONTEXT.md (D-14, D-15)
    - /Users/axisrow/Projects/zai-codex-helper/.planning/phases/01-project-skeleton-packaging-foundation/01-VALIDATION.md (Per-Task Verification Map — PKG-01/02/04 commands)
  </read_first>
  <files>
    tests/test_cli_help.py
    tests/test_markers.py
    tests/test_home_isolation.py
    tests/test_smoke_install.py
  </files>
  <action>
    Create four test files. Use `subprocess.run([sys.executable, "-m", "zai_codex_helper", ...], capture_output=True, text=True)` for CLI smoke tests (RESEARCH A5 — `python -m` executes `__main__.py` and works regardless of whether the console script is on PATH; it exercises the real entry point).

    `tests/test_cli_help.py` (PKG-02, `@pytest.mark.smoke`):
    - `test_help_exits_zero()`: run `python -m zai_codex_helper --help`; assert `returncode == 0`; assert `"usage:" in result.stdout.lower()`; assert `"Traceback" not in result.stderr` (the smoke check that the whole packaging+entry-point chain works).
    - `test_no_subcommand_errors()`: run `python -m zai_codex_helper` (no args); assert `returncode != 0` (argparse exits 2 when subparsers are `required=True`); assert `"Traceback" not in result.stderr` (clean error, not a crash — Pitfall 4 guard).

    `tests/test_markers.py` (PKG-04, `@pytest.mark.unit`):
    - `test_markers_registered()`: run `python -m pytest --markers` via subprocess; for each of `unit`, `integration`, `smoke`, `e2e` assert `f"@pytest.mark.{marker}" in result.stdout` (D-13/D-15 — fails loud if a marker is unregistered, which would also trip `--strict-markers`).

    `tests/test_home_isolation.py` (PKG-04, `@pytest.mark.unit`):
    - `test_home_isolated_to_tmp(_isolate_home)`: explicitly request the autouse fixture to get its yielded `tmp_path`; assert `Path(os.environ["HOME"]) == _isolate_home`; assert `(_isolate_home / ".codex").is_dir()` (D-14).
    - `test_real_codex_not_touched(_isolate_home, tmp_path)`: write a marker file at `Path(os.environ["HOME"]) / ".codex" / "test_marker"`; assert it exists under `tmp_path`; assert the developer's REAL `~/.codex/test_marker` does NOT exist (use `os.path.expanduser("~")` resolved BEFORE the fixture swapped HOME — capture it at module import time into a module-level constant `REAL_HOME = Path(os.environ["HOME"])` at the top of the file, so the assertion compares against the pre-isolation home). This is the load-bearing assertion that the autouse fixture actually protects the developer's real config (Pitfall 6 guard).

    `tests/test_smoke_install.py` (PKG-01, `@pytest.mark.smoke`):
    - `test_package_importable_after_install()`: run `python -c "import zai_codex_helper; print(zai_codex_helper.__version__)"` via subprocess; assert exit 0; assert `"0.1.0" in result.stdout` (proves src-layout + dynamic version produced an importable package with the right version — PKG-01 success criterion 2).
    - Do NOT run `pip install .` inside the test (slow, mutates environment, already done by Task 1's editable install). Assert importability against the installed package, which is the observable consequence of a correct install. If a future phase wants a true non-editable-install smoke test, it belongs in CI (Phase 15), not here (D-20).

    All tests use `subprocess.run([sys.executable, ...])` for CLI/marker checks (real process boundary, not in-process — matches RESEARCH Code Examples and avoids argparse's `SystemExit` interacting with pytest's exit handling). Mark each test with the appropriate `@pytest.mark.{unit|smoke}` decorator.
  </action>
  <verify>
    <automated>cd /Users/axisrow/Projects/zai-codex-helper &amp;&amp; pytest tests/test_cli_help.py tests/test_markers.py tests/test_home_isolation.py tests/test_smoke_install.py -v 2&gt;&amp;1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/test_cli_help.py -v` has 2 passing tests: `--help` exits 0 with `usage:` in stdout and no Traceback; no-subcommand exits non-zero with no Traceback.
    - `pytest tests/test_markers.py -v` passes and verifies all 4 markers appear in `pytest --markers` output.
    - `pytest tests/test_home_isolation.py -v` has 2 passing tests: HOME points to tmp_path with `.codex` created; AND the real `~/.codex/test_marker` is NOT created (the developer's real config is provably untouched — Pitfall 6 guard holds).
    - `pytest tests/test_smoke_install.py -v` passes: `import zai_codex_helper` succeeds and prints `0.1.0` (PKG-01).
    - `pytest -q` overall shows zero `PytestUnknownMarkWarning` in its output.
    - Every test file has at least one `@pytest.mark.{unit|smoke}` decorator (no unmarked tests — enforces tier discipline from day one).
    - `ruff check tests/` exits 0; `ruff format --check tests/` exits 0.
  </acceptance_criteria>
  <done>Four test files exist, all green under `pytest -q`; the autouse HOME-isolation fixture is proven to protect the real `~/.codex`; all 4 markers resolve; `import zai_codex_helper` returns `0.1.0`; `--help` and no-subcommand behave per PKG-02.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Write the D-11/PKG-05 error-contract test (ZaiCodexHelperError → one-line + exit 1, --debug → traceback)</name>
  <read_first>
    - /Users/axisrow/Projects/zai-codex-helper/src/zai_codex_helper/__main__.py (Plan 01 Task 3 — confirm `main()` and `ZaiCodexHelperError` exist and the try/except + `--debug` re-raise branch is present)
    - /Users/axisrow/Projects/zai-codex-helper/src/zai_codex_helper/cli/parser.py (Plan 01 Task 3 — confirm `build_parser()` and the stub dispatch via `set_defaults(func=...)`)
    - /Users/axisrow/Projects/zai-codex-helper/.planning/phases/01-project-skeleton-packaging-foundation/01-CONTEXT.md (D-11 the CONTRACT: expected error → one-line message + non-zero exit, no traceback; `--debug` → full traceback. D-12 mechanism is Plan 01's `ZaiCodexHelperError` + try/except in main)
    - /Users/axisrow/Projects/zai-codex-helper/.planning/phases/01-project-skeleton-packaging-foundation/01-RESEARCH.md (Pattern 2 "D-11 error contract" + Validation Architecture PKG-05 test map)
  </read_first>
  <files>
    tests/test_error_contract.py
  </files>
  <behavior>
    - Test 1 (PKG-05 happy-contract): a handler that raises `ZaiCodexHelperError("boom")` causes `main(["<cmd>", ...])` (in-process) to print exactly `error: boom` to stderr (captured via `capsys` or `contextlib.redirect_stderr`) and return `1`. No traceback in the captured stderr.
    - Test 2 (PKG-05 --debug): the same handler raising `ZaiCodexHelperError("boom")` but with `--debug` prepended to argv causes `main(...)` to RE-RAISE the exception (assert via `pytest.raises(ZaiCodexHelperError)`) — i.e. `--debug` disables the friendly-message branch and surfaces the full traceback.
    - Test 3 (sanity): `main(["--help"])` raises `SystemExit(0)` (argparse's normal help behavior) — confirms the parser is wired and that the error-contract `try` does not swallow argparse's help exit.
  </behavior>
  <action>
    Create `tests/test_error_contract.py` marked `@pytest.mark.unit`.

    Import the REAL contract surface: `from zai_codex_helper.__main__ import main, ZaiCodexHelperError`. Do NOT copy `main()`'s logic into the test — the test must exercise the real `try/except` branch in `__main__.main()`. The parser is the ONLY thing swapped, via `monkeypatch`, which is the legitimate handler-injection seam Phase 7 will use.

    Define a module-local raising handler: `def raising(args): raise ZaiCodexHelperError("boom")`.

    Build a minimal replacement parser mirroring `build_parser()`'s structure: an `argparse.ArgumentParser` with a `--debug` (`action="store_true"`) flag and one subcommand `cmd` whose `func=raising` is set via `set_defaults(func=raising)`. Attach it with `monkeypatch.setattr("zai_codex_helper.__main__.build_parser", lambda: that_parser)`.

    Test 1 (PKG-05 happy-contract): call `rc = main(["cmd"])` and assert `rc == 1`. Capture stderr via `capsys` and assert `captured.err == "error: boom\n"` and `"Traceback" not in captured.err`.

    Test 2 (PKG-05 --debug): call `with pytest.raises(ZaiCodexHelperError): main(["--debug", "cmd"])` — `--debug` disables the friendly-message branch and re-raises.

    Test 3 (--help sanity): with the monkeypatch removed for this case (use the REAL `build_parser`), call `with pytest.raises(SystemExit) as exc: main(["--help"])` and assert `exc.value.code == 0` — argparse's normal help behavior is not swallowed by the error contract.
  </action>
  <verify>
    <automated>cd /Users/axisrow/Projects/zai-codex-helper &amp;&amp; pytest tests/test_error_contract.py -v 2&gt;&amp;1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `tests/test_error_contract.py` has at least 3 tests, all passing under `pytest -v`.
    - Test "expected error → one-line + exit 1, no traceback": a `ZaiCodexHelperError("boom")` raised from a handler causes `main()` to return `1` and print exactly `error: boom` to stderr (assert the captured stderr equals or ends with `error: boom`), with no `Traceback` in stderr.
    - Test "--debug → traceback": with `--debug` in argv, the same raise propagates out of `main()` (assert via `pytest.raises(ZaiCodexHelperError)`).
    - Test "--help → SystemExit(0)": `main(["--help"])` raises `SystemExit` with code `0` (argparse help behavior is not swallowed by the error contract).
    - The test calls the REAL `zai_codex_helper.__main__.main` (import it; do NOT copy its logic). The parser is the only thing swapped (via monkeypatch), which is the legitimate Phase 7 handler-injection seam.
    - `pytest -q` (full suite: all 7+ tests across the 5 files) exits 0 with everything green.
    - `ruff check tests/` exits 0; `ruff format --check tests/` exits 0.
    - `ruff check src/` exits 0; `ruff format --check src/` exits 0 (whole-repo lint gate).
  </acceptance_criteria>
  <done>PKG-05 error-contract test is green: `ZaiCodexHelperError` → `error: <msg>` + exit 1 + no traceback; `--debug` → re-raise; `--help` → SystemExit(0). Full `pytest -q` suite green. `ruff check .` and `ruff format --check .` green across src/ and tests/.</done>
</task>

</tasks>

<artifacts_produced>
## Artifacts this phase produces

**Test harness symbols (inherited by all 14 later phases):**
- `tests/conftest.py::_isolate_home` — `@pytest.fixture(autouse=True)`; sets `HOME=tmp_path`, creates `tmp_path/.codex`, yields tmp_path. The iron guarantee no test corrupts the developer's real `~/.codex` (D-14).
- `tests/test_cli_help.py::test_help_exits_zero` — `@pytest.mark.smoke`; PKG-02 `--help` exits 0 with usage, no traceback.
- `tests/test_cli_help.py::test_no_subcommand_errors` — `@pytest.mark.smoke`; PKG-02 no-subcommand → non-zero, no traceback (Pitfall 4 guard).
- `tests/test_markers.py::test_markers_registered` — `@pytest.mark.unit`; PKG-04 all 4 markers resolve in `pytest --markers`.
- `tests/test_home_isolation.py::test_home_isolated_to_tmp` — `@pytest.mark.unit`; PKG-04 HOME points to tmp_path, `.codex` created.
- `tests/test_home_isolation.py::test_real_codex_not_touched` — `@pytest.mark.unit`; PKG-04 the developer's real `~/.codex` is provably untouched (Pitfall 6 guard).
- `tests/test_smoke_install.py::test_package_importable_after_install` — `@pytest.mark.smoke`; PKG-01 `import zai_codex_helper` succeeds, prints `0.1.0`.
- `tests/test_error_contract.py` (3 tests, `@pytest.mark.unit`) — PKG-05: `ZaiCodexHelperError` → one-line `error:` + exit 1 + no traceback; `--debug` → re-raise; `--help` → SystemExit(0).

**Files:**
- `tests/conftest.py`, `tests/test_cli_help.py`, `tests/test_markers.py`, `tests/test_home_isolation.py`, `tests/test_smoke_install.py`, `tests/test_error_contract.py`
- `pyproject.toml` — conditionally gains `pythonpath = ["src"]` under `[tool.pytest.ini_options]` ONLY if Task 1's import check fails (RESEARCH A4 fallback); otherwise untouched from Plan 01.

**Not produced:** No e2e test (Phase 15), no pytest-httpserver integration test (Phase 14), no real-subcommand tests (each command's phase).
</artifacts_produced>

<verification>
Phase gate (run before declaring Phase 1 done):
- `pip install -e ".[dev]"` exits 0.
- `pytest -q` exits 0 — all tests across all 5 files green, zero warnings.
- `pytest --markers` lists unit, integration, smoke, e2e.
- `ruff check .` exits 0 (whole repo: src/ + tests/).
- `ruff format --check .` exits 0.
- `pip install .` (non-editable, as a user would) exits 0, then `python -c "import zai_codex_helper; print(zai_codex_helper.__version__)"` prints `0.1.0`, then `zai-codex-helper --help` prints `usage: zai-codex-helper` and exits 0. (This is the PKG-01 smoke check from a clean user perspective; run it in a throwaway venv if you want to be strict about isolation.)
- `zai-codex-helper` (no subcommand) exits non-zero with no traceback.
- `zai-codex-helper status` prints `status: not implemented in this phase` to stderr, exit 0.
- Manual matrix note (VALIDATION.md "Manual-Only"): `import zai_codex_helper` is verified on local Python 3.12 now; the full 3.10/3.11/3.12/3.13 matrix is Phase 15 CI (D-20).
</verification>

<success_criteria>
Plan 02 succeeds when `pytest -q` is green and every ROADMAP success criterion has a passing test proving it: (1) `pip install .` → console script + `--help` exit 0 (PKG-01/02, test_smoke_install + test_cli_help), (2) `import zai_codex_helper` works post-install (PKG-01/02, test_smoke_install), (3) pytest discovers tier-marked tests and the autouse HOME fixture isolates every test from real `~/.codex` (PKG-04, test_markers + test_home_isolation), (4) a runtime error prints one-line + non-zero exit with traceback hidden unless `--debug` (PKG-05, test_error_contract). The Walking Skeleton is locked.
</success_criteria>

<output>
Create `.planning/phases/01-project-skeleton-packaging-foundation/01-02-SUMMARY.md` when done.
</output>
