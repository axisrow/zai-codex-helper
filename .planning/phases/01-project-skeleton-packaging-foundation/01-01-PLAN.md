---
phase: 01-project-skeleton-packaging-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - src/zai_codex_helper/__init__.py
  - src/zai_codex_helper/__main__.py
  - src/zai_codex_helper/cli/__init__.py
  - src/zai_codex_helper/cli/parser.py
  - src/zai_codex_helper/services/__init__.py
  - src/zai_codex_helper/backends/__init__.py
  - README.md
autonomous: true
requirements:
  - PKG-01
  - PKG-02
  - PKG-05
user_setup: []

must_haves:
  truths:
    # PKG-01
    - "After `pip install .` (or `pip install -e .`), `python -c \"import zai_codex_helper; print(zai_codex_helper.__version__)\"` prints `0.1.0` and exits 0 (src/-layout forces install-before-import)."
    - "The built wheel contains the import package under top-level `zai_codex_helper/` (NOT `src/zai_codex_helper/`)."
    # PKG-02
    - "After install, `zai-codex-helper --help` prints a line beginning with `usage: zai-codex-helper` to stdout and exits 0 with no traceback."
    - "`zai-codex-helper` invoked with NO subcommand exits non-zero (argparse error to stderr) with no traceback."
    - "Each stub subcommand (`setup`, `use zai`, `use openai`, `status`, `doctor`, `install-service`, `uninstall-service`) prints a `not implemented in this phase` message to stderr and exits 0."
    # PKG-05 (contract wired in Plan 01; tested in Plan 02)
    - "An expected error raised as `ZaiCodexHelperError` from a handler is caught in `main()`, printed as `error: <message>` (one line) to stderr, and returns exit code 1 — UNLESS `--debug` is passed, in which case it re-raises and Python prints the full traceback."
    # D-09 architectural skeleton
    - "`src/zai_codex_helper/cli/`, `services/`, `backends/` each exist as importable packages whose `__init__.py` docstring names the layer's responsibility."
  artifacts:
    - pyproject.toml
    - src/zai_codex_helper/__init__.py
    - src/zai_codex_helper/__main__.py
    - src/zai_codex_helper/cli/__init__.py
    - src/zai_codex_helper/cli/parser.py
    - src/zai_codex_helper/services/__init__.py
    - src/zai_codex_helper/backends/__init__.py
  key_links:
    - "[project.scripts] zai-codex-helper = zai_codex_helper.__main__:main  (console script → entry point)"
    - "[tool.hatch.build.targets.wheel] packages = [src/zai_codex_helper]  (src-layout → wheel top-level import)"
    - "[tool.hatch.version] path = src/zai_codex_helper/__init__.py  (dynamic version source-of-truth)"
    - "__main__.py:main() try/except ZaiCodexHelperError  (D-11/PKG-05 contract enforcement point)"
    - "cli/parser.py:build_parser() add_subparsers(dest='cmd', required=True)  (dispatch contract Phase 7 plugs into)"
---

<objective>
Build the installable, runnable Walking Skeleton: a correctly-configured `pyproject.toml` (PEP 621 + hatchling + src-layout + dynamic version), the three-layer package skeleton (`cli/` + `services/` + `backends/`), an argparse CLI with stub subcommands, and a `main()` that enforces the D-11 error contract. After this plan, `pip install .` produces a working `zai-codex-helper --help` and an importable `zai_codex_helper` package — the foundation every later phase plugs into.

Purpose: This is the foundation of a 15-phase roadmap. Every architectural decision here is load-bearing: the src-layout + `[tool.hatch.build.targets.wheel] packages` setting determines whether `import zai_codex_helper` works post-install; the `add_subparsers(dest="cmd", required=True)` + `set_defaults(func=...)` pattern is the dispatch contract Phase 7 plugs real `use zai`/`use openai` handlers into; the `ZaiCodexHelperError` + single `try/except` in `main()` is the D-11/PKG-05 error contract every services/backends layer throws into. Get the shapes exactly right per D-01..D-20.

Output: `pyproject.toml`, `src/zai_codex_helper/{__init__,__main__}.py`, three-layer skeleton (`cli/{__init__,parser}.py`, `services/__init__.py`, `backends/__init__.py`), updated `README.md`. Plan 02 adds the pytest harness that proves this skeleton against PKG-01/02/04/05.
</objective>

<execution_context>
@/Users/axisrow/Projects/zai-codex-helper/.claude/gsd-core/workflows/execute-plan.md
@/Users/axisrow/Projects/zai-codex-helper/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@/Users/axisrow/Projects/zai-codex-helper/.planning/PROJECT.md
@/Users/axisrow/Projects/zai-codex-helper/.planning/ROADMAP.md
@/Users/axisrow/Projects/zai-codex-helper/.planning/STATE.md
@/Users/axisrow/Projects/zai-codex-helper/.claude/CLAUDE.md
@/Users/axisrow/Projects/zai-codex-helper/.planning/phases/01-project-skeleton-packaging-foundation/01-CONTEXT.md
@/Users/axisrow/Projects/zai-codex-helper/.planning/phases/01-project-skeleton-packaging-foundation/01-RESEARCH.md
@/Users/axisrow/Projects/zai-codex-helper/.planning/phases/01-project-skeleton-packaging-foundation/01-VALIDATION.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create pyproject.toml (PEP 621 + hatchling + src-layout + dynamic version + ruff config)</name>
  <read_first>
    - /Users/axisrow/Projects/zai-codex-helper/.claude/CLAUDE.md (authoritative stack + "What NOT to Use": argparse/no-Rich/ruff/tomlkit-pyyaml-httpx deps; section "Installation" pins the exact dep list)
    - /Users/axisrow/Projects/zai-codex-helper/.planning/phases/01-project-skeleton-packaging-foundation/01-CONTEXT.md (locked decisions D-01, D-06, D-07, D-08, D-16, D-17, D-18, D-19, D-20)
    - /Users/axisrow/Projects/zai-codex-helper/.planning/phases/01-project-skeleton-packaging-foundation/01-RESEARCH.md (section "Code Examples → pyproject.toml (полный, минимальный-но-корректный для Phase 1)" — the validated exact shape; section "Pitfall 1" and "Pitfall 2" for the two load-bearing config keys)
    - /Users/axisrow/Projects/zai-codex-helper/LICENSE (confirm MIT, Copyright (c) 2026 axisrow — do NOT overwrite per D-19)
    - /Users/axisrow/Projects/zai-codex-helper/.gitignore (confirm it covers .env/.venv/.pytest_cache/.ruff_cache/*.egg-info/build/dist — already verified, do NOT duplicate)
  </read_first>
  <files>pyproject.toml</files>
  <action>
    Create `pyproject.toml` at the repo root. Copy the validated shape verbatim from RESEARCH.md "Code Examples → pyproject.toml" — every key below is load-bearing and verified against hatch/ruff/pytest docs (do NOT improvise alternatives):

    `[build-system]`: `requires = ["hatchling>=1.21"]`, `build-backend = "hatchling.build"` (D-implicit in CLAUDE.md).

    `[project]`: `name = "zai-codex-helper"` (dist name; normalized to import `zai_codex_helper`); `dynamic = ["version"]` (D-16 — NEVER a static `version = "0.1.0"` here, that creates two sources of truth); `description = "Manage the Codex ⇄ Moon Bridge ⇄ Z.ai link without hand-editing TOML/YAML/shell."`; `readme = "README.md"`; `requires-python = ">=3.10"` (D-17); `license = "MIT"`; `license-files = ["LICENSE"]` (D-19 — LICENSE already exists, this REFERENCES it, does not overwrite; if hatchling rejects `license-files`, fall back to the classifier alone per RESEARCH A2, but try `license-files` first); `authors = [{ name = "axisrow" }]`; classifiers = the exact list from RESEARCH including `Development Status :: 3 - Alpha`, `Environment :: Console`, `License :: OSI Approved :: MIT License`, `Operating System :: MacOS :: MacOS X` (D-18 — soft macOS-only, NO hard platform-block / environment markers), `Programming Language :: Python :: 3.10/3.11/3.12/3.13`, `Topic :: Software Development :: Build Tools`; `dependencies = ["tomlkit>=0.12,<1", "pyyaml>=6.0", "httpx>=0.27"]` (D-06 — declared now because the package ships to PyPI at milestone end; NOT imported in Phase 1).

    `[project.scripts]`: `zai-codex-helper = "zai_codex_helper.__main__:main"` (PKG-02 console script → the `main()` Task 3 creates).

    `[project.optional-dependencies] dev = ["pytest>=8.0", "pytest-httpserver>=1.1", "build", "hatchling>=1.21", "ruff>=0.6"]` (D-07 — pytest-httpserver included now despite being needed only in Phase 14, per D-07 rationale).

    Dependency-list discipline (D-04, D-05): the dependency lists above are the COMPLETE set — do NOT add `rich` (D-04: output is plain text; Rich was only free via the dropped Typer) and do NOT add any color/terminal-formatting library (D-05: the future `[✓]`/`[!]`/`[✗]` markers for Phase 14 `doctor` will be emitted via manual ANSI codes from stdlib, not Rich). Phase 1 emits no colored output at all — this citation fixes the contract now so later phases do not re-introduce Rich.

    `[project.urls] Homepage = "https://github.com/axisrow/zai-codex-helper"`.

    `[tool.hatch.version] path = "src/zai_codex_helper/__init__.py"` (D-16 — hatchling regex-source reads `__version__`).

    `[tool.hatch.build.targets.wheel] packages = ["src/zai_codex_helper"]` — THIS IS THE LOAD-BEARING KEY (Pitfall 1): without it, hatchling either fails to find the package in src-layout or puts it in the wheel under `src/zai_codex_helper/`, breaking `import zai_codex_helper` post-install. Do NOT omit.

    `[tool.ruff] line-length = 88`, `target-version = "py310"` (D-17 floor; explicit is safer than deriving from requires-python).

    `[tool.ruff.lint] select = ["E4", "E7", "E9", "F", "I", "UP", "B"]`, `ignore = []` (pycodestyle-subset + pyflakes + isort + pyupgrade + bugbear — D-08).

    `[tool.ruff.format] quote-style = "double"`, `indent-style = "space"`.

    `[tool.pytest.ini_options] minversion = "8.0"`, `testpaths = ["tests"]`, `markers = [...]` with exactly the 4 entries `unit`/`integration`/`smoke`/`e2e` and their help strings from RESEARCH, `addopts = ["--strict-markers", "-m", "not e2e"]` (D-13 + D-20 e2e excluded from default run). Plan 02 populates `tests/`; this config is inert until then but must exist so Plan 02's `pytest` invocation resolves markers. Do NOT add `pythonpath = ["src"]` (RESEARCH A4 — `pip install -e ".[dev]"` adds src/ via dev-mode-dirs; add it later only if Plan 02 hits ModuleNotFoundError).

    Do NOT touch LICENSE (D-19) or .gitignore (already sufficient).
  </action>
  <verify>
    <automated>cd /Users/axisrow/Projects/zai-codex-helper &amp;&amp; python -c "import tomllib; d=tomllib.loads(open('pyproject.toml').read()); assert d['build-system']['build-backend']=='hatchling.build'; assert d['project']['name']=='zai-codex-helper'; assert d['project']['dynamic']==['version']; assert d['project']['requires-python']=='>=3.10'; assert d['project']['scripts']=={'zai-codex-helper':'zai_codex_helper.__main__:main'}; assert 'version' not in d['project']; assert d['tool']['hatch']['build']['targets']['wheel']['packages']==['src/zai_codex_helper']; assert d['tool']['hatch']['version']['path']=='src/zai_codex_helper/__init__.py'; assert 'tomlkit>=0.12,<1' in d['project']['dependencies']; assert set(d['tool']['pytest']['ini_options']['markers']) &gt;= {'unit','integration','smoke','e2e'} or all('pytest.mark.'+m in str(d['tool']['pytest']['ini_options']['markers']) for m in ('unit','integration','smoke','e2e')); print('pyproject.toml OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `pyproject.toml` parses as valid TOML (tomllib.load succeeds).
    - `[build-system] build-backend == "hatchling.build"`.
    - `[project] name == "zai-codex-helper"`, `dynamic == ["version"]`, and there is NO static `version` key (D-16 single-source-of-truth).
    - `[project] requires-python == ">=3.10"`.
    - `[project.scripts] == {"zai-codex-helper": "zai_codex_helper.__main__:main"}` (PKG-02 entry point).
    - `[project.optional-dependencies] dev` contains exactly: pytest>=8.0, pytest-httpserver>=1.1, build, hatchling>=1.21, ruff>=0.6.
    - `[project] dependencies` contains tomlkit>=0.12,<1, pyyaml>=6.0, httpx>=0.27 (D-06 — declared, not imported in Phase 1).
    - `[tool.hatch.build.targets.wheel] packages == ["src/zai_codex_helper"]` (Pitfall 1 guard — load-bearing for import to work post-install).
    - `[tool.hatch.version] path == "src/zai_codex_helper/__init__.py"` (D-16).
    - `[tool.pytest.ini_options] markers` lists all of unit, integration, smoke, e2e; `addopts` contains `--strict-markers` and `-m not e2e` (D-13/D-20).
    - `LICENSE` is NOT modified (D-19) — verify `git diff --quiet LICENSE` after the task.
    - No `pythonpath = ["src"]` line is present (deferred per RESEARCH A4).
  </acceptance_criteria>
  <done>pyproject.toml exists at repo root with every load-bearing key matching the validated RESEARCH shape; tomllib parse + key assertions pass; LICENSE untouched.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Create the three-layer package skeleton (cli/services/backends) with layer-role docstrings</name>
  <read_first>
    - /Users/axisrow/Projects/zai-codex-helper/.claude/CLAUDE.md (section "Stack Patterns by Variant" + Architecture — three-layer CLI → pure services → file backends)
    - /Users/axisrow/Projects/zai-codex-helper/.planning/STATE.md (Decisions: "three-layer config-patching CLI", "compiler whose target is the user's filesystem")
    - /Users/axisrow/Projects/zai-codex-helper/.planning/phases/01-project-skeleton-packaging-foundation/01-CONTEXT.md (D-09 three-layer skeleton, D-10 main() location, D-16 __version__ source)
    - /Users/axisrow/Projects/zai-codex-helper/.planning/phases/01-project-skeleton-packaging-foundation/01-RESEARCH.md (section "Recommended Project Structure" + "Code Examples → src/zai_codex_helper/__init__.py")
  </read_first>
  <files>
    src/zai_codex_helper/__init__.py
    src/zai_codex_helper/services/__init__.py
    src/zai_codex_helper/backends/__init__.py
    src/zai_codex_helper/cli/__init__.py
  </files>
  <action>
    Create the four package `__init__.py` files establishing the three-layer architectural contract (D-09). The package is greenfield — no existing `src/`.

    `src/zai_codex_helper/__init__.py`: module docstring `"""zai-codex-helper — manage the Codex ⇄ Moon Bridge ⇄ Z.ai link."""` followed by EXACTLY `__version__ = "0.1.0"` (D-16 — simple assignment, NO type hint like `__version__: str = "0.1.0"` which can break hatchling's regex-source per Pitfall 2; this is the single source of truth read by both hatchling metadata and future `status`/PROV-05 in Phase 8). Add `__all__ = ["__version__"]`.

    `src/zai_codex_helper/cli/__init__.py`: docstring naming the layer's role — the presentation/command layer: parses argv via argparse, dispatches to handlers, formats user-facing output. State that it contains NO business logic and NO direct file IO (those live in services/ and backends/ respectively). Phase 1 holds only the parser + stubs.

    `src/zai_codex_helper/services/__init__.py`: docstring naming the layer's role — pure domain services: desired-state computation and transforms (apply_zai/apply_openai) as pure functions with NO side effects. Phase 1: empty (Phase 6/7 adds transforms).

    `src/zai_codex_helper/backends/__init__.py`: docstring naming the layer's role — the file-IO boundary: every disk mutation lives behind a `ConfigBackend` ABC (read/exists/write_canonical/backup_once — arrives Phase 4). Concrete backends (TomlBackend/YamlBackend/JsonBackend/ShellBackend/PlistBackend) arrive in their own phases (5/9). Phase 1: empty.

    Do NOT create any backend classes, service functions, or real handlers here — only the empty packages with their role docstrings. Do NOT add `__version__` anywhere except `__init__.py` (D-16 single source).
  </action>
  <verify>
    <automated>cd /Users/axisrow/Projects/zai-codex-helper &amp;&amp; python -c "import zai_codex_helper, zai_codex_helper.cli, zai_codex_helper.services, zai_codex_helper.backends; assert zai_codex_helper.__version__=='0.1.0'; assert all(m.__doc__ for m in (zai_codex_helper.cli, zai_codex_helper.services, zai_codex_helper.backends)); print('package skeleton OK')" 2>&amp;1 | grep -v "No module named" || echo "Note: import requires Task 3 main() + pip install -e .; will pass after Plan 02 installs. Verifying files exist instead:" &amp;&amp; ls src/zai_codex_helper/__init__.py src/zai_codex_helper/cli/__init__.py src/zai_codex_helper/services/__init__.py src/zai_codex_helper/backends/__init__.py</automated>
  </verify>
  <acceptance_criteria>
    - `src/zai_codex_helper/__init__.py` contains the exact line `__version__ = "0.1.0"` (no type hint — Pitfall 2 guard) and a module docstring.
    - `grep -c '__version__' src/zai_codex_helper/**/*.py` returns exactly 1 (single source of truth, D-16).
    - `src/zai_codex_helper/cli/__init__.py`, `services/__init__.py`, `backends/__init__.py` each have a non-empty module docstring (`__doc__` truthy) describing that layer's responsibility per D-09.
    - No backend class, service function, or handler implementation exists in any of these files (Phase 1 = skeleton only): `grep -rn 'class .*Backend' src/zai_codex_helper/` returns nothing; `grep -rn 'def apply_' src/zai_codex_helper/services/` returns nothing.
    - All four files exist at the paths listed in `<files>`.
  </acceptance_criteria>
  <done>Three-layer skeleton (cli/services/backends) exists as importable packages with role docstrings; `__version__ = "0.1.0"` lives only in __init__.py; no premature implementations.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Create the argparse CLI parser + main() entry point enforcing the D-11 error contract</name>
  <read_first>
    - /Users/axisrow/Projects/zai-codex-helper/.claude/CLAUDE.md (CLI framework = argparse per D-01; "What NOT to Use" bans Typer/Click)
    - /Users/axisrow/Projects/zai-codex-helper/.planning/phases/01-project-skeleton-packaging-foundation/01-CONTEXT.md (D-01 argparse OVERRIDE, D-02 root parser + subparsers as stubs, D-03 use zai/use openai sub-subs, D-10 main() location, D-11 error CONTRACT, D-12 mechanism choice)
    - /Users/axisrow/Projects/zai-codex-helper/.planning/phases/01-project-skeleton-packaging-foundation/01-RESEARCH.md (Pattern 1 "argparse subcommand dispatch", Pattern 2 "D-11 error contract", "Code Examples → __main__.py", Pitfall 4 "add_subparsers without required=True", "Error-handling mechanism options" Option A recommendation)
  </read_first>
  <files>
    src/zai_codex_helper/cli/parser.py
    src/zai_codex_helper/__main__.py
  </files>
  <action>
    Create the argparse CLI following RESEARCH Pattern 1 + Pattern 2 verbatim in shape (these are verified against docs.python.org/3/library/argparse.html#sub-commands).

    `src/zai_codex_helper/cli/parser.py` exposes `build_parser() -> argparse.ArgumentParser`:
    - `ArgumentParser(prog="zai-codex-helper", description="Manage the Codex ⇄ Moon Bridge ⇄ Z.ai link.")`.
    - Root flags (research Pattern 1): `--debug` (`action="store_true"`, help "show full traceback on error"), `--yes`/`-y` (`action="store_true"`, "answer yes to all prompts (non-interactive)"), `--dry-run` (`action="store_true"`, "preview changes without writing").
    - `subparsers = parser.add_subparsers(dest="cmd", required=True, metavar="<command>")` — `required=True` is LOAD-BEARING (Pitfall 4): without it, `zai-codex-helper` with no subcommand raises `AttributeError: 'Namespace' object has no attribute 'func'` instead of argparse's clean error+exit 2.
    - `use` subcommand with its OWN nested subparsers: `p_use = subparsers.add_parser("use", help="switch the default Codex provider")`; `use_sub = p_use.add_subparsers(dest="provider", required=True, metavar="<provider>")`; `use_sub.add_parser("zai", help="make Z.ai (glm-5.2 xhigh) the default").set_defaults(func=_stub("use zai"))`; `use_sub.add_parser("openai", help="revert to OpenAI").set_defaults(func=_stub("use openai"))` (D-03 — nested sub-subs ready for Phase 7 to swap `_stub` for real handlers).
    - The remaining 5 subcommands (`setup`, `status`, `doctor`, `install-service`, `uninstall-service`) each via `subparsers.add_parser(name, help=...)` + `.set_defaults(func=_stub(name))`.
    - `_stub(name: str)` returns a closure `handler(args: argparse.Namespace) -> int` that prints `f"{name}: not implemented in this phase"` to `sys.stderr` and returns `0` (RESEARCH Open Question 1 + A6 — stubs return 0 + stderr message so smoke `--help` stays exit 0 and stub calls do no harm; the contract does not dictate a non-zero stub exit).

    `src/zai_codex_helper/__main__.py` exposes `main(argv: list[str] | None = None) -> int` (D-10 — entry point lives here, NOT in cli/__init__.py; `[project.scripts]` already points at `zai_codex_helper.__main__:main`):
    - Define `class ZaiCodexHelperError(Exception)` with a docstring stating the D-11 contract (one-line message + non-zero exit, no traceback unless --debug). This is RESEARCH Option A (D-12 mechanism), chosen because it is the cleanest fit for the three-layer architecture (services/backends throw it, main catches in one place).
    - `parser = build_parser()`, `args = parser.parse_args(argv)`.
    - `try: return args.func(args)` — dispatch via the `set_defaults(func=...)` contract (Phase 7 replaces stubs by swapping `func`).
    - `except ZaiCodexHelperError as e:` — if `getattr(args, "debug", False)` is true, `raise` (re-raise → Python prints full traceback for debugging); otherwise `print(f"error: {e}", file=sys.stderr)` and `return 1` (D-11/PKG-05: one-line message, non-zero exit, no traceback).
    - Unhandled exceptions (real bugs) are NOT caught — Python prints the traceback itself. Do NOT add `sys.excepthook` (RESEARCH Anti-Pattern: breaks pytest, hides cause).
    - End with `if __name__ == "__main__": sys.exit(main())`.

    Import `build_parser` from `zai_codex_helper.cli.parser` (absolute import — works both as `python -m zai_codex_helper` and via the console script after install).

    Update `README.md` from its current 1-line stub to a short description: the project name, one-sentence purpose (manage Codex ⇄ Moon Bridge ⇄ Z.ai link without hand-editing TOML/YAML/shell), the install one-liner `pip install .`, and a usage example showing `zai-codex-helper --help` and `zai-codex-helper use zai` (noting commands are stubs until later phases). Keep it brief.
  </action>
  <verify>
    <automated>cd /Users/axisrow/Projects/zai-codex-helper &amp;&amp; python -c "from zai_codex_helper.cli.parser import build_parser; p=build_parser(); import argparse; assert isinstance(p, argparse.ArgumentParser); assert p.prog=='zai-codex-helper'; ns=p.parse_args(['use','zai']); assert ns.cmd=='use' and ns.provider=='zai' and callable(ns.func); from zai_codex_helper.__main__ import main, ZaiCodexHelperError; assert issubclass(ZaiCodexHelperError, Exception); print('parser+main OK')" 2>&amp;1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `src/zai_codex_helper/cli/parser.py` defines `build_parser()` returning an `argparse.ArgumentParser` with `prog == "zai-codex-helper"`.
    - Root parser has `--debug`, `--yes`/`-y`, `--dry-run` flags.
    - `add_subparsers(dest="cmd", required=True, ...)` is used (grep for `required=True` in parser.py — Pitfall 4 guard).
    - All 6 top-level commands are registered: `use`, `setup`, `status`, `doctor`, `install-service`, `uninstall-service` (verify via `build_parser().parse_args(['status'])` does not error).
    - `use` has nested `zai` and `openai` sub-subs: `build_parser().parse_args(['use','zai']).provider == 'zai'` and `...['use','openai']).provider == 'openai'`.
    - Every subcommand has `func` set via `set_defaults` (callable): `callable(build_parser().parse_args(['setup']).func)` is True.
    - `src/zai_codex_helper/__main__.py` defines `class ZaiCodexHelperError(Exception)` and `def main(argv=None) -> int`.
    - `main()` body contains a `try`/`except ZaiCodexHelperError` that re-raises when `args.debug` is truthy and otherwise prints `error: {e}` to stderr and returns 1 (D-11/PKG-05 contract). Grep for both `raise` and `return 1` inside the except branch.
    - `__main__.py` ends with `if __name__ == "__main__": sys.exit(main())`.
    - No `sys.excepthook` assignment exists in `__main__.py` (RESEARCH Anti-Pattern guard).
    - README.md is no longer just `# zai-codex-helper` — it contains the install command and a `--help` usage example.
  </acceptance_criteria>
  <done>`build_parser()` produces the full stub subcommand tree with `set_defaults(func=...)` dispatch; `main()` enforces the D-11 error contract via `ZaiCodexHelperError` + `--debug` re-raise; console-script entry point is wired and importable. After `pip install -e ".[dev]"` (Plan 02 Task 1), `zai-codex-helper --help` will exit 0.</done>
</task>

</tasks>

<artifacts_produced>
## Artifacts this phase produces

**Package symbols (importable after install):**
- `zai_codex_helper` — top-level import package
- `zai_codex_helper.__version__` — `"0.1.0"` string (D-16 single source of truth, read by hatchling + future `status`/PROV-05)
- `zai_codex_helper.cli` — presentation layer package (docstring only in Phase 1)
- `zai_codex_helper.cli.build_parser()` — `() -> argparse.ArgumentParser`; returns the root parser with all stub subcommands
- `zai_codex_helper.cli._stub(name)` — stub-handler factory returning `(args: Namespace) -> int` (prints "not implemented in this phase" to stderr, returns 0)
- `zai_codex_helper.services` — pure-domain layer package (docstring only in Phase 1)
- `zai_codex_helper.backends` — file-IO layer package (docstring only in Phase 1)
- `zai_codex_helper.__main__.main(argv=None)` — `() -> int`; console-script entry point enforcing the D-11 error contract
- `zai_codex_helper.__main__.ZaiCodexHelperError` — `Exception` subclass; expected-error sentinel caught in `main()`

**Console script:**
- `zai-codex-helper` → `zai_codex_helper.__main__:main` (declared in `[project.scripts]`)

**Files:**
- `pyproject.toml` — PEP 621 metadata + hatchling build + ruff config + pytest marker config (Plan 02 finalizes the pytest sections)
- `src/zai_codex_helper/{__init__,__main__}.py`
- `src/zai_codex_helper/cli/{__init__,parser}.py`
- `src/zai_codex_helper/{services,backends}/__init__.py`
- `README.md` — short install + usage description

**Not produced in Phase 1 (arrives in later phases — listed to anchor downstream expectations):**
- No `Paths` (Phase 2), no atomic-write helper (Phase 3), no `ConfigBackend` ABC (Phase 4), no `TomlBackend` (Phase 5), no transforms (Phase 6), no real `use` handlers (Phase 7), no `conftest.py`/tests (Plan 02 of this phase), no CI (Phase 15).
</artifacts_produced>

<verification>
After this plan (and `pip install -e ".[dev]"` from Plan 02 Task 1), all of these pass — they are the smoke checks Plan 02 formalizes into tests:
- `pip install -e ".[dev]"` exits 0.
- `python -c "import zai_codex_helper; print(zai_codex_helper.__version__)"` prints `0.1.0`.
- `zai-codex-helper --help` prints `usage: zai-codex-helper ...` to stdout, exit 0, no `Traceback` in stderr.
- `zai-codex-helper` (no subcommand) exits non-zero with an argparse error (no `Traceback`).
- `zai-codex-helper status` prints `status: not implemented in this phase` to stderr, exit 0.
- `zai-codex-helper use zai` prints `use zai: not implemented in this phase` to stderr, exit 0.
- `ruff check src/` exits 0; `ruff format --check src/` exits 0.
</verification>

<success_criteria>
Plan 01 succeeds when the Walking Skeleton is installable and runnable: `pyproject.toml` matches the validated RESEARCH shape exactly (especially `[tool.hatch.build.targets.wheel] packages` and `[tool.hatch.version] path`), the three-layer package imports with role docstrings, `build_parser()` produces the full stub subcommand tree, `main()` enforces the D-11 error contract, and the console-script entry point is correctly declared. The 4 ROADMAP success criteria are proven by Plan 02's tests; this plan delivers the artifact those tests target.
</success_criteria>

<output>
Create `.planning/phases/01-project-skeleton-packaging-foundation/01-01-SUMMARY.md` when done.
</output>
