---
phase: 04-backup-coordinator-configbackend-abc
plan: 02
type: execute
wave: 2
depends_on:
  - 04-01
files_modified:
  - src/zai_codex_helper/cli/parser.py
  - tests/test_restore.py
autonomous: true
requirements: [CONF-04]
user_setup: []

must_haves:
  truths:
    - "Running `zai-codex-helper restore` restores the user's config from the one-time .bak (copies config.toml.zai-codex-helper.bak back over the live config.toml via the atomic mechanism) when a .bak exists (per D-31, SC-2)"
    - "Running `zai-codex-helper restore` with NO .bak prints exactly one line `error: no backup to restore` to stderr and exits 1 with NO traceback (per D-11, D-31)"
    - "Running `zai-codex-helper --debug restore` with NO .bak re-raises ZaiCodexHelperError so Python emits the full traceback (per D-11)"
    - "`restore` is a real argparse subcommand (not a _stub) whose handler raises ZaiCodexHelperError on failure and returns 0 on success — it does NOT call sys.exit / print-errors / catch ZaiCodexHelperError itself (main() owns the D-11 formatting)"
    - "`restore` resolves paths via Paths.default() (never hard-codes ~/.codex) and delegates to BackupCoordinator.restore(paths, backend) — the coordinator itself is NOT redefined here (it lands in Plan 04-01)"
  artifacts:
    - "src/zai_codex_helper/cli/parser.py — `restore` subparser registered with a real handler (the first non-stub subcommand)"
    - "tests/test_restore.py — SC-2 + D-11 CLI proof (end-to-end via main()/build_parser, isolated HOME)"
  key_links:
    - "cli handler -> BackupCoordinator.restore(paths, backend) (defined in Plan 04-01, _backup.py; this plan only CALLS it)"
    - "cli handler -> Paths.default() (Phase 2; resolves config_toml + codex_dir off the injected HOME)"
    - "ZaiCodexHelperError raised by BackupCoordinator.restore -> __main__.main() try/except (D-11 contract; one-line stderr + exit 1, --debug re-raises)"
---

<objective>
Wire the `restore` subcommand into the CLI — the first REAL (non-stub) subcommand — so a user can roll their config back to the one-time backup with `zai-codex-helper restore`. This plan owns ONLY the CLI handler + its tests; the `BackupCoordinator.restore(paths, backend)` method it calls is defined in Plan 04-01 (Wave 1).

Purpose: ROADMAP SC-2 ("A `restore` command rolls the user's config back to the last one-time backup") is a user-facing outcome, not a coordinator internal. Plan 04-01 proved `BackupCoordinator.restore` works at the services layer; Plan 04-02 closes the loop by exposing it on the CLI surface, where the D-11 error contract (one-line stderr + exit 1, `--debug` re-raises) is the load-bearing UX. This is also the milestone where the CLI graduates from all-stubs to its first real command (CONTEXT D-31).

Output:
- `src/zai_codex_helper/cli/parser.py` — `restore` registered as a real subparser with a handler that constructs `Paths.default()`, builds a path-only backend, calls `BackupCoordinator.restore(paths, backend)`, and returns 0 on success.
- `tests/test_restore.py` — end-to-end CLI tests (drive `main(argv)` or `build_parser().parse_args(...)` with `HOME` isolated) pinning SC-2 (restore rolls back), D-11 (no-backup → exit 1 + one-line stderr, no traceback), and the `--debug` re-raise.

Implements: D-31 (restore subcommand — first real handler; calls BackupCoordinator.restore; autonomous, no prompt in Phase 4), D-11 (error contract — ZaiCodexHelperError propagates to main(); restore handler does NOT catch/print/exit itself). CONF-04 (restore command — rollback to last one-time backup).
</objective>

<execution_context>
@/Users/axisrow/Projects/zai-codex-helper/.claude/gsd-core/workflows/execute-plan.md
@/Users/axisrow/Projects/zai-codex-helper/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-backup-coordinator-configbackend-abc/04-CONTEXT.md

# Prior-plan SUMMARY genuinely needed (this plan calls BackupCoordinator.restore)
@.planning/phases/04-backup-coordinator-configbackend-abc/04-01-SUMMARY.md

# Existing source this plan reads and extends
@src/zai_codex_helper/cli/parser.py
@src/zai_codex_helper/__main__.py
@src/zai_codex_helper/services/paths.py
@src/zai_codex_helper/backends/_backup.py
@tests/conftest.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Register `restore` as the first real CLI subcommand + tests (SC-2, D-11, D-31)</name>
  <files>src/zai_codex_helper/cli/parser.py, tests/test_restore.py</files>
  <read_first>
    - .planning/phases/04-backup-coordinator-configbackend-abc/04-CONTEXT.md (D-31 restore contract verbatim — first real subcommand, calls BackupCoordinator.restore, autonomous, no prompt; D-11 error contract — one-line stderr + exit 1, --debug re-raises)
    - .planning/phases/04-backup-coordinator-configbackend-abc/04-01-PLAN.md (Task 2 — the EXACT BackupCoordinator.restore signature this plan calls: `@staticmethod restore(paths: Paths, backend) -> None`; reads `backend.path` for the live file, looks for the sibling `.bak`, raises `ZaiCodexHelperError("no backup to restore")` when absent, otherwise copies `.bak`→live via atomic_write. DO NOT redefine the coordinator — only CALL it.)
    - src/zai_codex_helper/cli/parser.py (build_parser pattern; subparsers with `dest="cmd", required=True`; `set_defaults(func=...)` dispatch; the `_stub(name)` factory this plan replaces for `restore`; note `restore` is NOT in the current stub loop, so it must be ADDED as a new subparser)
    - src/zai_codex_helper/__main__.py (main() try/except — `ZaiCodexHelperError` → `print(f"error: {e}", file=sys.stderr); return 1`, `--debug` re-raises. The handler MUST let ZaiCodexHelperError propagate; it must NOT call sys.exit / print errors / catch ZaiCodexHelperError itself.)
    - src/zai_codex_helper/services/paths.py (`Paths.default()` production entry point and `Paths.from_home(home: str | Path)` factory — `home` is REQUIRED, no default; `paths.config_toml` and `paths.codex_dir` fields the handler resolves)
    - src/zai_codex_helper/backends/_backup.py (after Plan 04-01 lands: `BackupCoordinator.restore(paths, backend)`; `BAK_SUFFIX`; confirm the backend needs only a `.path` attribute pointing at the live file)
    - tests/conftest.py (autouse `_isolate_home` fixture — already redirects HOME to tmp_path; tests rely on this so Paths.default() resolves under tmp_path)
  </read_first>
  <behavior>
    - Test 1 (SC-2 happy path — restore rolls back): in the isolated HOME, seed `~/.codex/config.toml` with content "LIVE_CHANGED", seed `~/.codex/config.toml.zai-codex-helper.bak` with content "ORIGINAL_BACKUP"; invoke the CLI end-to-end (`main(["restore"])` or `build_parser().parse_args(["restore"])` then the handler) ; assert exit code 0; assert `~/.codex/config.toml` now reads "ORIGINAL_BACKUP" (byte-identical to the .bak). This proves restore rolls the user's config back to the last one-time backup (SC-2).
    - Test 2 (D-11 no-backup — exit 1, one-line stderr, no traceback): in a fresh isolated HOME with a live `config.toml` but NO `.bak`, invoke `main(["restore"])`; assert it returns 1; assert stderr is exactly the single line `error: no backup to restore` (use `capsys` and split lines — assert exactly one non-empty stderr line); assert stdout is empty; assert NO traceback text (no "Traceback (most recent call last)", no "ZaiCodexHelperError") appears in stderr or stdout.
    - Test 3 (D-11 --debug re-raises): in a fresh isolated HOME with no `.bak`, invoke `main(["--debug", "restore"])`; assert it raises `ZaiCodexHelperError` (the handler/`main` re-raises under `--debug` rather than swallowing) — use `pytest.raises(ZaiCodexHelperError)`. This proves `--debug` surfaces the full traceback for debugging (D-11).
    - Test 4 (parser registration — `restore` is a real subcommand, not a stub): `build_parser().parse_args(["restore"])` succeeds (no argparse error) and the parsed Namespace has `args.cmd == "restore"` and `args.func` is NOT the `_stub("restore")` handler (assert `args.func.__name__ != "handler"` OR that calling it with a no-.bak HOME raises ZaiCodexHelperError rather than printing "not implemented in this phase"). Also assert `zai-codex-helper --help` exit-0 (smoke) still holds and the `restore` subcommand appears in help text.
    - Test 5 (restore is autonomous — no prompt, no TTY interaction): invoke the handler in a non-interactive context (no stdin faking); assert it neither reads from stdin nor prompts. (Per D-31: autonomous in Phase 4 — restores unconditionally, the "are you sure" UX is a later phase. This test is a guard against accidentally adding a confirm prompt.)
  </behavior>
  <action>
    Edit `src/zai_codex_helper/cli/parser.py` to REGISTER `restore` as the first real subcommand (per D-31). Use stdlib only (`argparse`) plus imports of `Paths` and `BackupCoordinator`. Concrete changes:

    - Add imports at the top of the module: `from zai_codex_helper.services.paths import Paths` and `from zai_codex_helper.backends._backup import BackupCoordinator`. (These resolve at runtime because Plan 04-01 ships `_backup.py` and Phase 2 ships `paths.py`; both are committed before Wave 2 runs. If a lazy-import is preferred to keep `parser.py` import-light, import inside the handler — either is acceptable; pick one and keep `python -c "from zai_codex_helper.cli.parser import build_parser"` side-effect-free.)
    - Define a module-level handler function `_handle_restore(args: argparse.Namespace) -> int` (name it explicitly — NOT produced by `_stub`). Body, in order:
      1. `paths = Paths.default()` — the production entry point (Phase 2 D-23: `Paths.default()` returns `Paths.from_home(Path.home())`). The autouse `_isolate_home` fixture in tests injects `HOME=tmp_path`, so `Paths.default()` resolves under tmp_path in tests and under the real user home in production. Never hard-code `~/.codex`. (Note: `Paths.from_home(home)` requires a `home` argument — do NOT call `Paths.from_home()` with no args; use `Paths.default()` for the production entry point.)
      2. Build a minimal path-only backend whose `.path` attribute is `paths.config_toml`. Because `TomlBackend` lands in Phase 5 and `BackupCoordinator.restore` only reads `backend.path` (per 04-01 Task 2), define a tiny `_LiveConfigBackend` (or a local dataclass / `types.SimpleNamespace`) inside `parser.py` exposing a single `path` attribute set to `paths.config_toml`. Do NOT subclass `ConfigBackend` — that requires implementing 3 abstractmethods and pulls in tomlkit (Phase 5). The coordinator's contract is "reads `backend.path`"; a path-only object satisfies it. Document this as a Phase-4 expedient: replaced by `TomlBackend` in Phase 5.
      3. Call `BackupCoordinator.restore(paths, backend)` — let any `ZaiCodexHelperError` PROPAGATE (do NOT catch it here; `__main__.main()` formats it per D-11). On success, print a one-line confirmation to stdout (e.g. `f"restored {paths.config_toml}"`) and `return 0`.
      4. The handler must NOT call `sys.exit`, must NOT `print(..., file=sys.stderr)` for errors, and must NOT wrap the call in `try/except ZaiCodexHelperError`. The D-11 contract is owned by `main()`.
    - Inside `build_parser()`, add the `restore` subparser BEFORE (or after — position is cosmetic, but keep it visibly separate from the stub loop) the stub loop. Concrete: `p_restore = subparsers.add_parser("restore", help="restore config from the one-time backup")` then `p_restore.set_defaults(func=_handle_restore)`. The help text must read "restore config from the one-time backup" (or equivalent — cite SC-2). Do NOT include `restore` in the `for name in (...)` stub loop.
    - Keep the existing `_stub` factory intact (other commands still stub) and the `dest="cmd", required=True` subparser contract (RESEARCH Pitfall 4 — no-arg invocation still exits 2 cleanly).
    - Update the `cli/parser.py` module docstring: note that `restore` is the FIRST real (non-stub) subcommand (Phase 4, D-31), and that the others remain stubs until their phases. Keep the existing dispatch-contract prose.

    Create `tests/test_restore.py` with the 5 `@pytest.mark.unit` tests listed in `<behavior>`. Drive the CLI end-to-end: import `main` from `zai_codex_helper.__main__` and call `main(["restore"])` (capture stdout/stderr via `capsys`; capture exit code from the int return; capture the `--debug` re-raise via `pytest.raises(ZaiCodexHelperError)`). Rely on the autouse `_isolate_home` fixture from `tests/conftest.py` to redirect HOME to `tmp_path` — then seed `tmp_path/.codex/config.toml` and `tmp_path/.codex/config.toml.zai-codex-helper.bak` directly with `pathlib.Path` + `write_bytes()` (bytes, not text — unambiguous). For Test 4 (parser registration), call `build_parser().parse_args(["restore"])` directly and assert `args.cmd == "restore"` and that dispatching `args.func` reaches the real handler (assert it does NOT print the stub message; the cleanest assertion is that under a no-.bak HOME it raises `ZaiCodexHelperError`, which `_stub` never does). Assert byte-identity of the restored file via `read_bytes()`. For the "no traceback" assertion (Test 2), assert `"Traceback" not in err` and `"ZaiCodexHelperError" not in err`. Keep the test file stdlib + pytest only (no extra deps).
  </action>
  <verify>
    <automated>python -m pytest tests/test_restore.py -v --no-header && python -m pytest -q --no-header && python -m ruff check src/zai_codex_helper/cli/parser.py tests/test_restore.py && python -m ruff format --check src/zai_codex_helper/cli/parser.py tests/test_restore.py</automated>
  </verify>
  <acceptance_criteria>
    - SC-2 happy path: with a live `config.toml` ("LIVE_CHANGED") and a sibling `.bak` ("ORIGINAL_BACKUP"), `main(["restore"])` returns 0 and `~/.codex/config.toml` reads "ORIGINAL_BACKUP" byte-for-byte.
    - D-11 no-backup: `main(["restore"])` with no `.bak` returns 1; stderr is exactly the single line `error: no backup to restore`; stdout empty; NO `Traceback` / `ZaiCodexHelperError` text in either stream.
    - D-11 --debug: `main(["--debug", "restore"])` with no `.bak` raises `ZaiCodexHelperError` (proving the traceback path).
    - Parser: `build_parser().parse_args(["restore"])` yields `args.cmd == "restore"`; `args.func` is the real `_handle_restore` (dispatching it under a no-.bak HOME raises `ZaiCodexHelperError`, not the stub "not implemented" message); `python -m zai_codex_helper --help` exits 0 and lists `restore`.
    - Autonomous: the handler reads no stdin / issues no prompt (D-31).
    - Structural: the handler does NOT call `sys.exit`, does NOT print to stderr, does NOT catch `ZaiCodexHelperError` (D-11 owned by `main()`).
    - No regressions: full `pytest -q` green; `ruff check` + `ruff format --check` clean on both touched files.
  </acceptance_criteria>
  <done>
    `zai-codex-helper restore` is a real subcommand that rolls the user's config back to the one-time `.bak` (SC-2), raises `ZaiCodexHelperError` on no-backup so `main()` formats `error: no backup to restore` + exit 1 (D-11, D-31), and re-raises under `--debug`. The handler delegates to `BackupCoordinator.restore(paths, backend)` (Plan 04-01) and resolves paths via `Paths.default()` — it does not redefine the coordinator, does not catch/print/exit. Five `@pytest.mark.unit` tests green; full suite green; ruff clean.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user CLI invocation → cli handler | The user invokes `restore` with an untrusted `$HOME` / filesystem state. The `.bak` may be missing, the live config may be open in another process, or HOME may point somewhere unexpected. |
| cli handler → BackupCoordinator.restore | The handler passes a path-only backend whose `.path` is `paths.config_toml`. The coordinator reads `.path` and performs the `.bak`→live copy. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-06 | Tampering | `restore` overwrites the wrong file if HOME is spoofed | medium | mitigate | Handler resolves paths via `Paths.default()` (Phase 2, D-22 — Paths is frozen and resolves only under the injected home); tests run under the autouse `_isolate_home` fixture so `restore` never touches the real `$HOME/.codex`. |
| T-04-07 | Denial of Service | `restore` with no `.bak` crashes with a confusing traceback | medium | mitigate | `BackupCoordinator.restore` raises `ZaiCodexHelperError("no backup to restore")` (Plan 04-01); the handler lets it propagate; `main()` formats it as one-line stderr + exit 1 (D-11). Test 2 asserts no traceback reaches the user without `--debug`. |
| T-04-08 | Information Disclosure | restore confirmation message leaks absolute path on shared screen | low | accept | The success line prints `paths.config_toml` (an absolute path under `$HOME`). This is the user's own config path shown to the user; consistent with Phase 1's plain-text output (D-04). No secret in the path (the key lives in moonbridge-zai.yml, not config.toml). |
| T-04-09 | Repudiation | no record that a restore happened | low | accept | v1 restore is autonomous (D-31) and does not log. The sentinel (`~/.codex/.zai-codex-helper.backed-up`) still marks "backed up"; `status` (Phase 8) may surface backup state later. Out of scope for Phase 4. |
| T-04-SC2 | Tampering | pip installs (none new — stdlib argparse only) | low | accept | Plan 04-02 adds NO runtime dependencies (stdlib `argparse`, `pathlib` only); pyproject.toml unchanged. Verified by `git diff --stat pyproject.toml` empty. |
</threat_model>

<verification>
- `python -m pytest tests/test_restore.py -v` — 5 unit tests green, pinning SC-2 (restore rolls back) and D-11 (no-backup → one-line stderr + exit 1, no traceback; `--debug` re-raises).
- `python -m pytest -q` — full suite green (no regressions across Phases 1-4; Plan 04-01's coordinator tests still pass).
- `python -m ruff check src/zai_codex_helper/cli/parser.py tests/test_restore.py` — clean.
- `python -m ruff format --check src/zai_codex_helper/cli/parser.py tests/test_restore.py` — clean.
- `python -c "from zai_codex_helper.cli.parser import build_parser; p = build_parser(); a = p.parse_args(['restore']); assert a.cmd == 'restore'; print('ok')"` — `restore` is a registered subcommand.
- `python -c "from zai_codex_helper.cli.parser import _handle_restore; print(_handle_restore.__name__)"` — handler is a real named function, not a `_stub` closure.
- `git diff --stat pyproject.toml` — empty (zero new deps; stdlib only).
</verification>

<success_criteria>
- SC-2 ("A `restore` command rolls the user's config back to the last one-time backup"): DELIVERED — `main(["restore"])` with a `.bak` present restores the live config byte-identically and exits 0.
- D-31 honored verbatim: `restore` is the first real (non-stub) subcommand; it calls `BackupCoordinator.restore(paths, backend)`; it is autonomous (no interactive prompt in Phase 4).
- D-11 honored verbatim: the no-backup path raises `ZaiCodexHelperError` which `main()` formats as `error: no backup to restore` (stderr, exit 1, no traceback); `--debug` re-raises.
- CONF-04 ("restore command — rollback to last backup"): DELIVERED.
- The plan does NOT redefine `BackupCoordinator` (that is Plan 04-01's exclusive scope); it only calls `BackupCoordinator.restore`.
- The restore error path uses `ZaiCodexHelperError` (not `SystemExit`, not `print(..., stderr)` + `sys.exit`, not a bare `raise SystemExit`).
</success_criteria>

<output>
Create `.planning/phases/04-backup-coordinator-configbackend-abc/04-02-SUMMARY.md` when done.

## Artifacts this phase (Plan 02) produces

**Symbols:**
- `_handle_restore(args: argparse.Namespace) -> int` — `src/zai_codex_helper/cli/parser.py` (module-level real handler; the first non-stub subcommand handler).
- A minimal path-only backend (e.g. `_LiveConfigBackend` or `SimpleNamespace`) defined in `parser.py` exposing `.path = paths.config_toml` — a Phase-4 expedient replaced by `TomlBackend` in Phase 5 (the coordinator only reads `backend.path`).

**Files modified:**
- `src/zai_codex_helper/cli/parser.py` — adds `restore` subparser with `set_defaults(func=_handle_restore)`; adds `Paths` + `BackupCoordinator` imports; updates module docstring to note `restore` is the first real subcommand.

**Files created:**
- `tests/test_restore.py` — 5 `@pytest.mark.unit` tests (SC-2 happy path; D-11 no-backup one-line stderr + exit 1 + no traceback; `--debug` re-raise; parser registration / not-a-stub; autonomous no-prompt guard).

**NOT produced (later phases):** `TomlBackend` and the real `.bak`/config.toml read/write logic (Phase 5); interactive "are you sure" prompt on restore (later UX phase — D-31 keeps Phase 4 autonomous); restoring `.zshrc` / `moonbridge-zai.yml` / plist (generalize the coordinator to a file list in a later phase); `use zai`/`setup`/`doctor` handlers (Phases 6/7/12/14).
</output>
